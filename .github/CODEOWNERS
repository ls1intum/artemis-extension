# Code owners for this repository.
#
# Every path is owned, so every pull request requires approval from one of the
# listed admins before it can be merged. The catch-all is deliberate: a rule that
# covered only the CI and release files left the code they ship unreviewed, which
# is the wrong way round.
#
# One approval from any of the three is enough.

*                               @krusche @FelixTJDietrich @Predixx
