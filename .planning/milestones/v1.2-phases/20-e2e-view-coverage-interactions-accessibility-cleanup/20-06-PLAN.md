---
phase: 20-e2e-view-coverage-interactions-accessibility-cleanup
plan: 06
type: execute
wave: 4
depends_on: [20-04, 20-05]
files_modified:
  - iris-thaumantias/src/provider/artemisWebviewProvider.ts
  - iris-thaumantias/src/shared/messageContracts.ts
  - iris-thaumantias/src/auth/auth.ts
  - iris-thaumantias/src/views/app/commands/navigationCommands.ts
  - iris-thaumantias/package.json
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "No duplicate legacy postMessage sends exist in artemisWebviewProvider.ts — the 3 backward-compat sends are removed"
    - "Legacy command message types (BuildLogParsedMessage, GitIdentityInfoMessage, PlantUmlRenderedMessage, PlantUmlErrorMessage) in messageContracts.ts are either removed or migrated to typed format"
    - "Auth migration fallback for old storage key (artemis-auth-cookie) is removed from auth.ts"
    - "knip reports zero unused exports after cleanup (npm run knip:exports)"
    - "All existing tests (Vitest + Mocha) still pass after cleanup"
  artifacts:
    - path: "iris-thaumantias/src/provider/artemisWebviewProvider.ts"
      provides: "Clean provider without duplicate legacy sends"
      contains: "websocketUpdate"
    - path: "iris-thaumantias/src/shared/messageContracts.ts"
      provides: "Clean message contracts without legacy command-field types"
    - path: "iris-thaumantias/src/auth/auth.ts"
      provides: "Clean auth without old storage key migration fallback"
  key_links:
    - from: "iris-thaumantias/src/provider/artemisWebviewProvider.ts"
      to: "iris-thaumantias/src/views/webview/react/hooks/useWebSocketUpdates.ts"
      via: "websocketUpdate typed messages (only format after cleanup)"
      pattern: "websocketUpdate"
---

<objective>
Remove migration-era code, legacy fallbacks, and unused exports. This is the final cleanup before v1.2 ships.

Purpose: Eliminate technical debt accumulated during the v1.0 React migration. After cleanup, the codebase has one canonical message format, no backward-compatibility duplicate sends, and zero unused exports per knip.
Output: Clean source files, passing tests, knip reporting zero unused exports.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@iris-thaumantias/src/provider/artemisWebviewProvider.ts
@iris-thaumantias/src/shared/messageContracts.ts
@iris-thaumantias/src/auth/auth.ts
@iris-thaumantias/src/views/app/commands/navigationCommands.ts
@iris-thaumantias/src/views/webview/react/hooks/useWebSocketUpdates.ts
@iris-thaumantias/src/views/webview/react/components/ReconnectBanner/ReconnectBanner.tsx
@iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.tsx
@iris-thaumantias/src/views/app/commands/plantUmlCommands.ts

<interfaces>
<!-- Legacy code locations confirmed by research -->

artemisWebviewProvider.ts lines ~989, ~1009, ~1047:
Three blocks sending duplicate legacy postMessage format alongside typed format:
```typescript
// Also send legacy message for backward compatibility
this._view.webview.postMessage({ command: 'newResult', result: result });
this._view.webview.postMessage({ command: 'newSubmission', submission: submission });
this._view.webview.postMessage({ command: 'submissionProcessing', ... });
```
The React views use `message.type === 'websocketUpdate'` (in useWebSocketUpdates.ts), NOT `message.command`. These legacy sends are dead code — safe to remove.

messageContracts.ts lines ~554-600:
Legacy Command Messages section with `command` field types: BuildLogParsedMessage, GitIdentityInfoMessage, PlantUmlRenderedMessage, PlantUmlErrorMessage, ShowClonedRepoNoticeMessage.
**WARNING:** PlantUmlRenderedMessage and PlantUmlErrorMessage are ACTIVELY USED by:
  - ProblemStatement.tsx: `typedMessage.command === 'plantUmlRendered'` (line 99)
  - ProblemStatement.tsx: `typedMessage.command === 'plantUmlError'` (line 119)
  - plantUmlCommands.ts: sends `{ command: 'plantUmlRendered', ... }` and `{ command: 'plantUmlError', ... }`
These are NOT dead legacy code — they are active message types using the `command` field format. Do NOT remove them unless you also migrate ProblemStatement.tsx and plantUmlCommands.ts to use `type` field format.

**WARNING:** ReconnectBanner.tsx uses `message.command === 'websocketDisconnected'` and `'websocketConnected'` — but the typed message contracts define these with `type` field. Check if the provider sends both formats for these.

auth.ts line ~104:
```typescript
// 3. Check old storage location (artemis-auth-cookie) - fallback for migration
const stored = await this.context.secrets.get(AuthManager.SECRET_KEY);
```
This is a migration fallback from before the storage key was renamed. Safe to remove — the new key (artemis-auth-token) is the primary.

navigationCommands.ts line ~87:
```typescript
// This is a legacy command - extract text field with runtime check
```
The `openRulesInEditor` handler uses runtime type checking for `message.text`. This is not "legacy code" to remove — it's a functioning command that handles an untyped field. The comment should be updated but the code should stay. Only update the comment to remove the "legacy" label.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy backward-compat sends and auth migration fallback</name>
  <files>iris-thaumantias/src/provider/artemisWebviewProvider.ts, iris-thaumantias/src/auth/auth.ts, iris-thaumantias/src/views/app/commands/navigationCommands.ts</files>
  <action>
**artemisWebviewProvider.ts (CLEAN-01, CLEAN-02):**
1. Remove the 3 duplicate legacy postMessage blocks. Search for `// Also send legacy message for backward compatibility` — there are exactly 3 occurrences (around lines 989, 1009, 1047).
2. For each occurrence, delete the comment line AND the `this._view.webview.postMessage({ command: ... })` call that follows it.
3. Keep the typed `this._view.webview.postMessage({ type: 'websocketUpdate', payload: { ... } })` sends — those are the canonical format used by `useWebSocketUpdates.ts`.
4. Verify: after removal, `_handleNewResult`, `_handleNewSubmission`, and `_handleSubmissionProcessing` each send exactly ONE postMessage (the typed format).
5. Also remove the comment on line ~490 `// Build recent course nodes (same structure as legacy dashboard)` — update to just `// Build recent course nodes`.

**auth.ts (CLEAN-02):**
1. Remove the migration fallback block (around line 104):
   ```typescript
   // 3. Check old storage location (artemis-auth-cookie) - fallback for migration
   const stored = await this.context.secrets.get(AuthManager.SECRET_KEY);
   return stored || undefined;
   ```
2. Replace the entire block with just `return undefined;` (no more fallback to old key).
3. Verify the `getCookieHeader()` method still has the memory cookie check (step 1) and the new storage key check (step 2). Only step 3 (old key) is removed.
4. Check if `AuthManager.SECRET_KEY` is used anywhere else in the file. If it's only used in this migration fallback, also remove the `SECRET_KEY` static field definition (it's the old constant). If used elsewhere, keep it.

**navigationCommands.ts (CLEAN-01):**
1. Update the comment on line ~87 from `// This is a legacy command - extract text field with runtime check` to `// Extract text field with runtime type check` — the code is not legacy, only the comment label was inaccurate.

**DO NOT touch:**
- PlantUML message types in messageContracts.ts — they are actively used by ProblemStatement.tsx and plantUmlCommands.ts
- ReconnectBanner.tsx — its `message.command` usage needs investigation but is out of scope for this basic cleanup
- The `command` field types for plantUml — these are active, not legacy
  </action>
  <verify>
    <automated>cd iris-thaumantias && npm run test:unit 2>&1 | tail -5</automated>
  </verify>
  <done>
- 3 legacy backward-compat postMessage sends removed from artemisWebviewProvider.ts
- Auth migration fallback for old storage key removed from auth.ts
- navigationCommands.ts comment updated (no longer labeled "legacy")
- All unit tests pass
- PlantUML message types left intact (actively used)
  </done>
</task>

<task type="auto">
  <name>Task 2: Run knip audit and remove unused exports/imports/files</name>
  <files>iris-thaumantias/src/shared/messageContracts.ts, iris-thaumantias/package.json</files>
  <action>
1. Run `cd iris-thaumantias && npx knip --reporter compact` to get the initial list of unused items.

2. Analyze knip output. For each reported item:
   - **Unused exports:** Remove the `export` keyword (if the symbol is used internally) or delete the entire symbol (if truly unused)
   - **Unused files:** Verify the file is genuinely unused (not loaded dynamically or by configuration), then delete
   - **Unused dependencies:** Remove from package.json dependencies/devDependencies
   - **Unused types/interfaces:** Remove if no consumer exists

3. **messageContracts.ts cleanup:** From the legacy section (~line 554+):
   - `BuildLogParsedMessage` — check if any code imports/uses it. If only defined but never imported elsewhere, remove it.
   - `GitIdentityInfoMessage` — check if any code imports/uses it. If unused, remove.
   - `ShowClonedRepoNoticeMessage` — check if any code imports/uses it. If unused, remove.
   - **DO NOT remove:** `PlantUmlRenderedMessage`, `PlantUmlErrorMessage` — actively used by ProblemStatement.tsx
   - If the only remaining types in the "Legacy" section are PlantUml types, update the section comment from "Legacy Command Messages" to "Command Messages (Extension → Webview)" — they're not legacy, just using a different field name.

4. **Iterate:** After each removal pass, re-run `npx knip --reporter compact` to check for newly exposed unused items (removing one export may reveal others).

5. **Known false positives to ignore:**
   - `activate` and `deactivate` from `extension.ts` — called by VS Code runtime, not imported
   - Entry point exports that knip correctly identifies as entry exports
   - Test-only imports (knip config ignores `test/` already)

6. **After cleanup, run all tests:**
   - `npm run test:unit` — Vitest tests
   - `npm run compile-tests && npm run test:host` — Mocha host tests (if script exists)
   - `npx tsc --noEmit` — TypeScript compilation

7. Final verification: `npx knip --exports` should report zero unused exports (or only known false positives that are documented).

**CRITICAL:** Do NOT remove code that is dynamically referenced, registered via VS Code APIs, or used in configuration files. When in doubt, keep it and document why knip flags it.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx knip --exports --reporter compact 2>&1 | head -20 && npm run test:unit 2>&1 | tail -5</automated>
  </verify>
  <done>
- knip audit complete — unused exports, imports, and files removed
- Legacy message types that are genuinely unused are removed from messageContracts.ts
- Actively used PlantUML message types retained
- All tests pass after cleanup
- knip reports zero unused exports (or only documented false positives)
  </done>
</task>

</tasks>

<verification>
1. `grep -r "Also send legacy" iris-thaumantias/src/` returns no results — all 3 backward-compat sends removed
2. `grep "old storage location" iris-thaumantias/src/auth/auth.ts` returns no results — migration fallback removed
3. `cd iris-thaumantias && npm run test:unit` passes
4. `cd iris-thaumantias && npx tsc --noEmit` passes
5. `cd iris-thaumantias && npx knip --exports --reporter compact` reports zero unused exports (or only false positives)
6. PlantUML message types still exist in messageContracts.ts and ProblemStatement.tsx still compiles
7. ReconnectBanner.tsx still functions (not touched)
</verification>

<success_criteria>
- CLEAN-01: 3 legacy backward-compat postMessage sends removed, legacy comment in navigationCommands updated
- CLEAN-02: Auth migration fallback for old storage key removed
- CLEAN-03: knip reports zero unused exports after full audit and cleanup
- All existing tests (Vitest + Mocha) pass after cleanup
- No actively used code was removed (PlantUML messages, ReconnectBanner preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/20-e2e-view-coverage-interactions-accessibility-cleanup/20-06-SUMMARY.md`
</output>
