---
phase: 07-cleanup-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - iris-thaumantias/src/views/app/viewRouter.ts
  - iris-thaumantias/src/views/app/appStateManager.ts
  - iris-thaumantias/src/views/app/viewActionService.ts
  - iris-thaumantias/src/views/app/webViewMessageHandler.ts
  - iris-thaumantias/src/provider/artemisWebviewProvider.ts
  - iris-thaumantias/src/views/webview/react/App.tsx
  - iris-thaumantias/package.json
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - "All legacy generateXxxHtml() functions are removed from the codebase"
    - "The coexistence router (_reactViews map) no longer exists"
    - "ViewRouter always returns React webview HTML for every app state"
    - "All legacy view directories (14 directories under src/views/) are deleted"
    - "All legacy component directories (11 directories under src/views/components/) are deleted"
    - "Legacy CSS files and utils under src/views/ are deleted"
    - "Extension compiles and builds successfully after removal"
  artifacts:
    - path: "iris-thaumantias/src/views/app/viewRouter.ts"
      provides: "React-only ViewRouter without coexistence fallback"
      contains: "getReactWebviewHtml"
    - path: "iris-thaumantias/src/views/webview/react/App.tsx"
      provides: "React view router with all 14 view cases"
      contains: "switch"
  key_links:
    - from: "iris-thaumantias/src/views/app/viewRouter.ts"
      to: "iris-thaumantias/src/utils/webviewHelpers.ts"
      via: "getReactWebviewHtml call"
      pattern: "getReactWebviewHtml"
    - from: "iris-thaumantias/src/provider/artemisWebviewProvider.ts"
      to: "iris-thaumantias/src/views/app/viewRouter.ts"
      via: "getHtml() call"
      pattern: "getHtml"
---

<objective>
Remove all legacy HTML generation code and replace the coexistence ViewRouter with React-only routing.

Purpose: Strip ~1,780 lines of dead legacy code that was superseded by React views in Phases 3-6. The coexistence router was a temporary migration tool — now that all views are React, eliminate the fallback path entirely.

Output: A clean codebase where ViewRouter always serves React HTML, with no legacy view classes, components, or CSS remaining.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-simple-views-migration/03-01-SUMMARY.md
@iris-thaumantias/src/views/app/viewRouter.ts
@iris-thaumantias/esbuild.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy view/component directories and simplify ViewRouter to React-only</name>
  <files>
    iris-thaumantias/src/views/app/viewRouter.ts
    iris-thaumantias/src/views/aiChecker/ (DELETE)
    iris-thaumantias/src/views/courseDetail/ (DELETE)
    iris-thaumantias/src/views/courseList/ (DELETE)
    iris-thaumantias/src/views/dashboard/ (DELETE)
    iris-thaumantias/src/views/examConduction/ (DELETE)
    iris-thaumantias/src/views/examExerciseDetail/ (DELETE)
    iris-thaumantias/src/views/examStart/ (DELETE)
    iris-thaumantias/src/views/exerciseDetail/ (DELETE)
    iris-thaumantias/src/views/gitCredentials/ (DELETE)
    iris-thaumantias/src/views/irisChat/ (DELETE)
    iris-thaumantias/src/views/login/ (DELETE)
    iris-thaumantias/src/views/recommendedExtensions/ (DELETE)
    iris-thaumantias/src/views/serviceStatus/ (DELETE)
    iris-thaumantias/src/views/struggleDetection/ (DELETE)
    iris-thaumantias/src/views/components/ (DELETE entire directory — 11 legacy component dirs: askIris, backLink, badge, button, container, dropdown, helpPopup, input, listItem, serviceHealth, sideMenu)
    iris-thaumantias/src/views/utils/ (DELETE — legacy CSS loader and HTML utils)
    iris-thaumantias/src/views/index.ts (DELETE if exists — legacy barrel export)
  </files>
  <action>
    **Step 1: Delete all 14 legacy view directories** under `iris-thaumantias/src/views/`:
    - aiChecker, courseDetail, courseList, dashboard, examConduction, examExerciseDetail, examStart, exerciseDetail, gitCredentials, irisChat, login, recommendedExtensions, serviceStatus, struggleDetection
    - Each contains a legacy `*View.ts` class with `generateHtml()` methods and associated CSS files.
    - Use `rm -rf` for each directory.

    **Step 2: Delete all 11 legacy component directories** under `iris-thaumantias/src/views/components/`:
    - askIris, backLink, badge, button, container, dropdown, helpPopup, input, listItem, serviceHealth, sideMenu
    - These are the legacy HTML-string component classes (not to be confused with React components in `src/views/webview/react/components/`).
    - Delete the entire `src/views/components/` directory.

    **Step 3: Delete legacy utilities** under `iris-thaumantias/src/views/utils/`:
    - Contains CSS loading utilities (`readCssFiles`, etc.) used only by legacy HTML generation.
    - Delete the entire `src/views/utils/` directory.

    **Step 4: Delete `iris-thaumantias/src/views/index.ts`** if it exists (legacy barrel export for view classes).

    **Step 5: Rewrite ViewRouter to React-only routing.** Replace the entire `viewRouter.ts` with a simplified version:
    - Remove ALL legacy view class imports (LoginView, DashboardView, CourseListView, etc.)
    - Remove ALL legacy view instance fields (_loginView, _dashboardView, etc.)
    - Remove the `_reactViews` Map entirely
    - Remove the constructor logic that instantiates legacy view classes
    - Keep the `_stateToViewName()` method
    - Simplify `getHtml()` to ALWAYS call `getReactWebviewHtml(webview, extensionUri, viewName)` — no coexistence check, no legacy fallback, no switch statement with legacy HTML generation
    - Remove `_extensionContext` from constructor if no longer needed (check if `getReactWebviewHtml` needs extensionUri)
    - Keep `_appStateManager` and `_webview` dependencies
    - The `AppStateManager` dependency stays since it provides `currentState`

    **Step 6: Update imports in files that reference deleted legacy views:**
    - Check `iris-thaumantias/src/views/app/webViewMessageHandler.ts` — if it imports legacy view classes, update to remove those imports. The message handler dispatches commands but should not import view classes directly.
    - Check `iris-thaumantias/src/views/app/viewActionService.ts` — update if it references legacy view instances.
    - Check `iris-thaumantias/src/provider/artemisWebviewProvider.ts` — ensure it only uses ViewRouter (which now always returns React HTML).
    - Run `npx tsc --noEmit` after changes to catch any broken imports.

    **Step 7: Remove legacy webview-components entry point:**
    - Delete `iris-thaumantias/src/views/webview/components.ts` — this was the legacy non-React webview bundle entry point.
    - Note: The esbuild config references this file. It will be removed from esbuild in Plan 02. For now, the file deletion is fine since it only fails at build time, not at TypeScript check time. If TypeScript check complains, comment out the webviewCtx in esbuild.js temporarily.

    **Important: Do NOT delete:**
    - `src/views/webview/react/` (the React codebase — this is what we're keeping)
    - `src/views/app/` directory (contains appStateManager, webViewMessageHandler, commands/ — these are extension host logic, NOT legacy views)
    - Any file under `src/views/webview/react/components/` (React shared components)
    - Any file under `src/views/webview/react/views/` (React view components)

    Commit message: `refactor(07): remove legacy view classes, components, and coexistence router`
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify no legacy view directories remain under src/views/ (only app/ and webview/ should remain)</manual>
  </verify>
  <done>All 14 legacy view directories, 11 legacy component directories, legacy utils, and legacy barrel export deleted. ViewRouter simplified to always return React HTML without coexistence fallback. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Run dead code detection and clean migration artifacts</name>
  <files>
    iris-thaumantias/package.json
    iris-thaumantias/src/views/app/appStateManager.ts
    iris-thaumantias/src/provider/artemisWebviewProvider.ts
    (various files with TODO/FIXME migration comments)
  </files>
  <action>
    **Step 1: Run knip for dead code detection:**
    ```bash
    cd iris-thaumantias && npx knip 2>&1
    ```
    - Review output for unused exports, unused files, and unused dependencies.
    - Pay special attention to:
      - Legacy-only npm dependencies that were used only by HTML generation
      - Unused exports from models or types that were only consumed by deleted legacy views
      - Orphaned CSS files that weren't in the deleted directories

    **Step 2: Remove unused npm dependencies identified by knip.**
    - Run `npm uninstall <package>` for each truly unused dependency.
    - Be careful: do NOT remove dependencies that are used dynamically or registered by string name in package.json contributes section.
    - Verify each removal with `npx tsc --noEmit` after.

    **Step 3: Clean up migration-related TODO/FIXME comments.**
    - Search for: `grep -rn "TODO.*migrat\|FIXME.*migrat\|TODO.*legacy\|FIXME.*legacy\|TODO.*coexist\|TODO.*React\|FIXME.*React" src/`
    - Remove or update comments that reference:
      - Migration scaffolding
      - Legacy code that no longer exists
      - Coexistence patterns that are now permanent
      - "Will be migrated" notes
    - Leave comments that are still relevant (e.g., general architecture TODOs).

    **Step 4: Clean up AppStateManager if it has legacy-only state.**
    - Review `appStateManager.ts` for state that was only used by legacy views (e.g., cached HTML data, legacy view state).
    - The `coursesData`, `currentCourseData`, `currentExerciseData` are still needed by React (sent via postMessage). Keep those.
    - Remove any state or methods that only existed to support the coexistence pattern.

    **Step 5: Review and clean artemisWebviewProvider.ts:**
    - Remove any legacy-only code paths (e.g., code that checked whether to use React or legacy HTML).
    - Ensure `_postMessageSafe` and ready-signal handshake are clean without migration-era workarounds.
    - Remove backward-compatibility message format handling if it only existed for legacy views (keep if React views still use legacy format like IrisChat does).

    **Step 6: Remove `useReact` feature flags or migration toggles** if any exist in the codebase.
    - Search: `grep -rn "useReact\|featureFlag\|migrationToggle\|_reactViews" src/`
    - Delete all found instances.

    Commit message: `refactor(07): remove dead code, unused deps, and migration artifacts`
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit && npm run compile 2>&1 | tail -5</automated>
    <manual>Run `npx knip` again to verify no additional dead code remains after cleanup</manual>
  </verify>
  <done>Dead code detected and removed. Unused npm dependencies uninstalled. All migration-related TODO/FIXME comments cleaned. No useReact flags or _reactViews references remain. Extension compiles and builds successfully.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run compile` succeeds (all bundles generated)
3. No directories exist under `src/views/` except `app/` and `webview/`
4. `grep -rn "generateHtml" src/views/` returns zero results (outside of webview/react/)
5. `grep -rn "_reactViews" src/` returns zero results
6. `npx knip` reports no significant unused code
</verification>

<success_criteria>
- All legacy HTML generation code is deleted from the codebase
- ViewRouter always serves React HTML without coexistence fallback
- Extension compiles and builds without errors
- Dead code and migration artifacts are cleaned
</success_criteria>

<output>
After completion, create `.planning/phases/07-cleanup-optimization/07-01-SUMMARY.md`
</output>
