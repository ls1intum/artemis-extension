---
phase: 07-cleanup-optimization
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - iris-thaumantias/test/views/ (legacy test files)
  - iris-thaumantias/docs/DEVELOPER-GUIDE.md
  - iris-thaumantias/docs/diagrams/extension-architecture.mmd
  - iris-thaumantias/docs/diagrams/message-flow.mmd
  - iris-thaumantias/docs/diagrams/store-interactions.mmd
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-03

must_haves:
  truths:
    - "Legacy test files that tested HTML generation are removed or converted"
    - "Developer guide documents how to add new views, message contracts, and stores"
    - "Mermaid diagrams in docs/diagrams/ illustrate extension architecture and message flow"
    - "All documentation references current file paths (no references to deleted legacy files)"
    - "npm run compile and test suite pass after test cleanup"
  artifacts:
    - path: "iris-thaumantias/docs/DEVELOPER-GUIDE.md"
      provides: "Comprehensive developer documentation for the React architecture"
      contains: "## Adding a New View"
      min_lines: 150
    - path: "iris-thaumantias/docs/diagrams/extension-architecture.mmd"
      provides: "Extension architecture diagram in Mermaid format"
      contains: "graph"
    - path: "iris-thaumantias/docs/diagrams/message-flow.mmd"
      provides: "Message flow diagram between extension host and webview"
      contains: "sequenceDiagram"
  key_links:
    - from: "iris-thaumantias/docs/DEVELOPER-GUIDE.md"
      to: "iris-thaumantias/docs/diagrams/"
      via: "Mermaid diagram file references"
      pattern: "diagrams/"
---

<objective>
Clean up legacy tests and create comprehensive developer documentation for the React architecture.

Purpose: With legacy code removed and the build pipeline optimized, test files still reference deleted HTML generation methods, and no developer guide exists for the React architecture. This plan cleans up tests and creates documentation that explains how to work with the codebase going forward.

Output: Clean test suite without legacy HTML tests, and a comprehensive developer guide with Mermaid architecture diagrams.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cleanup-optimization/07-01-SUMMARY.md
@.planning/phases/07-cleanup-optimization/07-02-SUMMARY.md
@.planning/phases/07-cleanup-optimization/07-03-SUMMARY.md
@iris-thaumantias/test/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up legacy test files and verify test suite</name>
  <files>
    iris-thaumantias/test/views/ (audit and clean)
    iris-thaumantias/test/views/app/ (audit and clean)
    iris-thaumantias/test/views/components/ (audit and clean — if exists)
    iris-thaumantias/test/views/exerciseDataTransformer.test.ts (keep — tests business logic)
    iris-thaumantias/test/views/markdownProcessor.test.ts (keep — tests business logic)
  </files>
  <action>
    **Step 1: Audit all test files under `iris-thaumantias/test/`** for references to deleted legacy code:
    ```bash
    cd iris-thaumantias && grep -rn "generateHtml\|LoginView\|DashboardView\|CourseListView\|CourseDetailView\|ExerciseDetailView\|ExamExerciseDetailView\|ButtonComponent\|ListItemComponent\|ContainerComponent" test/
    ```

    **Step 2: Categorize each test file:**
    - **DELETE:** Tests that ONLY test HTML string generation (e.g., "generates correct HTML for login view")
    - **CONVERT:** Tests that verify business logic but import from deleted files (update imports to new locations)
    - **KEEP:** Tests that don't reference legacy code (API tests, model tests, service tests, utility tests)

    **Step 3: Delete tests that only test HTML generation.**
    These are tests under `test/views/` that import legacy view classes and call `generateHtml()`.
    - Delete `test/views/app/` if it only tests the old ViewRouter's HTML generation or legacy message routing.
    - Delete `test/views/components/` if it only tests legacy HTML component classes.
    - Keep `test/views/exerciseDataTransformer.test.ts` — tests business logic (data transformation), not HTML.
    - Keep `test/views/markdownProcessor.test.ts` — tests business logic, not HTML.

    **Step 4: Update remaining test files** if they import from deleted paths:
    - If any test imports from `src/views/login/loginView` (deleted), remove the import and delete the test case.
    - If any test imports from `src/views/components/` (deleted), same treatment.
    - Run `npx tsc --noEmit` after changes to verify.

    **Step 5: Run the test suite to verify nothing is broken:**
    ```bash
    cd iris-thaumantias && npm test 2>&1 | tail -30
    ```
    - All remaining tests should pass.
    - If tests fail due to missing imports from deleted files, fix or remove those test cases.

    **Step 6: Verify test compilation:**
    ```bash
    cd iris-thaumantias && npm run compile-tests 2>&1 | tail -10
    ```

    Commit message: `test(07): remove legacy HTML generation tests, keep business logic tests`
  </action>
  <verify>
    <automated>cd iris-thaumantias && npm run compile-tests 2>&1 | tail -5 && npm test 2>&1 | tail -10</automated>
    <manual>No test file should import from deleted legacy view directories</manual>
  </verify>
  <done>Legacy HTML generation tests removed. Business logic tests preserved. Test suite compiles and passes. No test file references deleted legacy code.</done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive developer guide with Mermaid architecture diagrams</name>
  <files>
    iris-thaumantias/docs/DEVELOPER-GUIDE.md (CREATE)
    iris-thaumantias/docs/diagrams/extension-architecture.mmd (CREATE)
    iris-thaumantias/docs/diagrams/message-flow.mmd (CREATE)
    iris-thaumantias/docs/diagrams/store-interactions.mmd (CREATE)
  </files>
  <action>
    **Step 1: Create `iris-thaumantias/docs/diagrams/` directory.**

    **Step 2: Create Mermaid diagram files** (user decision: "Mermaid diagrams in separate files (docs/diagrams/), linked from the guide"):

    **extension-architecture.mmd:** Graph showing the extension's layered architecture:
    - Extension Host layer: extension.ts → providers (ArtemisWebviewProvider, ChatWebviewProvider)
    - Services layer: AuthManager, ArtemisApiService, ArtemisWebsocketService, TelemetryManager
    - Webview layer: React App → Views → Stores → Components
    - Communication: postMessage between extension host and webview

    **message-flow.mmd:** Sequence diagram showing message flow:
    - Extension host → webview (init data, state updates)
    - Webview → extension host (commands, ready signal)
    - Ready-signal handshake pattern
    - Example: login flow, data loading flow

    **store-interactions.mmd:** Diagram showing Zustand store relationships:
    - List all stores (post-consolidation)
    - Show which views consume which stores
    - Show persistence configuration per store

    **Step 3: Create `iris-thaumantias/docs/DEVELOPER-GUIDE.md`** with the following sections (user decisions: "Comprehensive developer guide in English", "Conventions list (not step-by-step tutorial) for how to add new views"):

    **Table of Contents:**
    1. Architecture Overview (with link to extension-architecture.mmd)
    2. Project Structure (current directory layout after cleanup)
    3. Adding a New View (conventions, required files, registration steps)
    4. Message Contracts (types, patterns, how to add new messages, link to message-flow.mmd)
    5. Store Architecture (which stores exist, what they manage, DevTools, link to store-interactions.mmd)
    6. Build Pipeline (dev, compile, package, analyze commands)
    7. Testing (how to run tests, add new tests)
    8. Conventions (naming, file organization, CSS Modules, VS Code theming)

    **Section details:**

    **Architecture Overview:** High-level description of extension host vs webview architecture. Dual webview pattern (ArtemisWebviewProvider for main views, ChatWebviewProvider for Iris chat). React rendering via data-view attribute routing.

    **Project Structure:** Generate current `tree -L 3` equivalent for `src/` directory. Annotate key directories. Show that `src/views/` now contains only `app/` (extension host logic) and `webview/react/` (React components).

    **Adding a New View:** Conventions list:
    - Create directory under `src/views/webview/react/views/ViewName/`
    - Required files: `ViewNameView.tsx`, `ViewNameView.module.css`, `types.ts`, `index.ts`
    - Register in `App.tsx` switch statement
    - Add message contracts in `src/shared/messageContracts.ts`
    - Add Zustand store if needed (with DevTools middleware)
    - Update `_stateToViewName()` in viewRouter.ts
    - Add case to message handler if view sends commands

    **Message Contracts:** Document the typed message pattern:
    - Extension → Webview: `{ type: 'messageType', payload: {...} }` or legacy `{ command: 'commandName', ...payload }`
    - Webview → Extension: `{ type: 'command', command: 'commandName', payload: {...} }`
    - Ready signal: `{ type: 'ready' }`
    - Where contracts are defined: `src/shared/messageContracts.ts`
    - How to add a new message type

    **Store Architecture:** List current stores (post-consolidation from Plan 03), their responsibilities, persistence config, and consuming views.

    **Build Pipeline:** Document:
    - `npm run compile` — development build
    - `npm run package` — production build (minified, with metafile)
    - `npm run dev` — coordinated watch mode
    - `npm run analyze` — bundle size visualization
    - `npm run build:analyze` — build + analyze in one command

    **Testing:** Document test structure, how to run tests, test compilation.

    **Conventions:** Naming conventions (files, components, stores), CSS Modules with camelCase class names, VS Code CSS variable usage (`var(--vscode-*)`), no path aliases.

    **Step 4: Add "Last updated" date** to the developer guide.

    **Step 5: Verify all file references in documentation** point to existing files (not deleted legacy paths).

    Commit message: `docs(07): create developer guide with Mermaid architecture diagrams`
  </action>
  <verify>
    <automated>cd iris-thaumantias && test -f docs/DEVELOPER-GUIDE.md && test -f docs/diagrams/extension-architecture.mmd && test -f docs/diagrams/message-flow.mmd && test -f docs/diagrams/store-interactions.mmd && echo "PASS: all doc files exist"</automated>
    <manual>Review DEVELOPER-GUIDE.md for accuracy of file paths and architecture descriptions</manual>
  </verify>
  <done>Developer guide created with 8 sections covering architecture, new view conventions, message contracts, store architecture, build pipeline, testing, and conventions. Three Mermaid diagram files created in docs/diagrams/. All file references point to current (not legacy) paths.</done>
</task>

</tasks>

<verification>
1. `npm run compile-tests` succeeds
2. `npm test` passes (no failures from deleted legacy tests)
3. `docs/DEVELOPER-GUIDE.md` exists with > 150 lines
4. `docs/diagrams/` contains 3 .mmd files
5. No test file imports from deleted legacy view directories
6. No documentation references deleted file paths
</verification>

<success_criteria>
- Legacy HTML generation tests removed, business logic tests preserved
- Test suite compiles and passes
- Comprehensive developer guide covers adding views, messages, stores, build, testing
- Mermaid diagrams illustrate architecture, message flow, and store interactions
- All documentation references current file structure
</success_criteria>

<output>
After completion, create `.planning/phases/07-cleanup-optimization/07-04-SUMMARY.md`
</output>
