---
phase: 04-main-ui-views
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts
  - iris-thaumantias/src/shared/messageContracts.ts
  - iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx
  - iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.module.css
  - iris-thaumantias/src/views/webview/react/views/CourseList/types.ts
  - iris-thaumantias/src/views/webview/react/views/CourseList/index.ts
  - iris-thaumantias/src/views/webview/react/views/index.ts
  - iris-thaumantias/src/views/webview/react/App.tsx
  - iris-thaumantias/src/views/app/viewRouter.ts
  - iris-thaumantias/src/provider/artemisWebviewProvider.ts
autonomous: true
requirements: [VIEW-01]

must_haves:
  truths:
    - "CourseList view renders through React with active and archived courses"
    - "Search filters courses by title, semester, or description in real time"
    - "Sort dropdown reorders courses (title, semester, exercises)"
    - "Type filter toggles between all, active, and archived courses"
    - "Breadcrumbs show 'Courses' as current page when on CourseList"
    - "Skeleton loading shows during data fetch"
    - "Clicking a course navigates to course detail via postMessage"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts"
      provides: "Zustand store for course list state"
    - path: "iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx"
      provides: "CourseList React view component"
  key_links:
    - from: "iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx"
      to: "iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts"
      via: "Zustand hook subscription"
      pattern: "useCourseListStore"
    - from: "iris-thaumantias/src/views/app/viewRouter.ts"
      to: "getReactWebviewHtml"
      via: "_reactViews map"
      pattern: "'course-list', true"
---

<objective>
Migrate the CourseList view to React with Zustand store, client-side search/sort/filter, breadcrumb navigation, and archived courses support.

Purpose: CourseList is the primary course browsing view. Migration establishes search/filter patterns reused in CourseDetail and validates breadcrumb navigation.
Output: CourseList React view with Zustand store, registered in coexistence router.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-main-ui-views/04-CONTEXT.md
@.planning/phases/04-main-ui-views/04-RESEARCH.md
@.planning/phases/04-main-ui-views/04-01-SUMMARY.md

@iris-thaumantias/src/views/courseList/courseListView.ts
@iris-thaumantias/src/shared/messageContracts.ts
@iris-thaumantias/src/views/webview/react/App.tsx
@iris-thaumantias/src/views/webview/react/views/index.ts
@iris-thaumantias/src/views/webview/react/components/index.ts
@iris-thaumantias/src/views/webview/react/stores/useNavigationStore.ts
@iris-thaumantias/src/views/app/viewRouter.ts
@iris-thaumantias/src/views/app/appStateManager.ts
@iris-thaumantias/src/provider/artemisWebviewProvider.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CourseList message contracts and Zustand store</name>
  <files>
    iris-thaumantias/src/shared/messageContracts.ts
    iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts
    iris-thaumantias/src/views/webview/react/views/CourseList/types.ts
  </files>
  <action>
    **1. Add CourseList message contracts to `messageContracts.ts`:**
    Extension-to-webview:
    - `CourseListInitMessage { type: 'courseListInit'; payload: { courses: CourseData[]; archivedCourses?: ArchivedCourse[] } }` where `CourseData = { course: { id: number; title: string; description?: string; semester?: string; color?: string; exercises?: Exercise[]; numberOfStudents?: number; instructorGroupName?: string } }` and `ArchivedCourse = { id: number; title: string; semester?: string; color?: string }`.
    - `ArchivedCoursesLoadedMessage { type: 'archivedCoursesLoaded'; payload: { archivedCourses: ArchivedCourse[] } }`

    Webview-to-extension commands:
    - `ReloadCoursesCommand { type: 'command'; command: 'reloadCourses' }`
    - `LoadArchivedCoursesCommand { type: 'command'; command: 'loadArchivedCourses' }`
    - `ViewArchivedCourseCommand { type: 'command'; command: 'viewArchivedCourse'; payload: { courseId: number } }`

    Add to union types and update `isExtensionMessage` guard. Note: `ViewCourseDetailsCommand` was already added in Plan 01.

    **2. Create CourseList types (`views/CourseList/types.ts`):**
    - `CourseListViewProps { vscodeApi: VsCodeApi }`
    - `CourseListPersistedState { searchTerm?: string; typeFilter?: string; semesterFilter?: string; sortBy?: string }` (persist filter state across hide/show)
    - Re-export `CourseData`, `ArchivedCourse` types.

    **3. Create CourseList Zustand store (`stores/useCourseListStore.ts`):**
    Interface `CourseListState`:
    - `courses: CourseData[]`
    - `archivedCourses: ArchivedCourse[]`
    - `archivedLoaded: boolean`
    - `isLoading: boolean`
    - `error: string | null`
    - `searchTerm: string`
    - `typeFilter: 'all' | 'active' | 'archived'`
    - `semesterFilter: string` (default 'all')
    - `sortBy: string` (default 'semester-desc')
    - Actions: `setCourses(courses, archived?)`, `setArchivedCourses(archived)`, `setLoading(loading)`, `setError(error)`, `setSearchTerm(term)`, `setTypeFilter(filter)`, `setSemesterFilter(filter)`, `setSortBy(sort)`, `clearFilters()`, `loadCourses(vscodeApi)`, `loadArchivedCourses(vscodeApi)`
    - Derived: `filteredCourses()` getter — applies search, type filter, semester filter, sort. Implement semester comparison logic (WS24/25, SS25 format) matching the legacy `compareSemesters` function.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>CourseList message contracts added. Zustand store created with search/sort/filter logic and semester comparison. Types defined with persisted state interface.</done>
</task>

<task type="auto">
  <name>Task 2: CourseList React view with router wiring</name>
  <files>
    iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx
    iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.module.css
    iris-thaumantias/src/views/webview/react/views/CourseList/index.ts
    iris-thaumantias/src/views/webview/react/views/index.ts
    iris-thaumantias/src/views/webview/react/App.tsx
    iris-thaumantias/src/views/app/viewRouter.ts
    iris-thaumantias/src/provider/artemisWebviewProvider.ts
  </files>
  <action>
    **1. Create CourseListView.tsx:**
    Match the legacy courseListView.ts layout:
    - **Back link + controls:** BackLink to dashboard (using existing BackLink component), reload button (IconButton reload preset), settings button (IconButton settings preset). These use the Phase 2 shared components.
    - **Header container:** "All Courses" title with subtitle "Browse and manage your enrolled courses". Search input (TextInput component, type search). Controls grid: Type filter dropdown (All/Active/Archived), Semester filter dropdown (populated from course data), Sort dropdown (Title A-Z/Z-A, Newest/Oldest First, Most/Least Exercises), Clear Filters button (disabled when no filters active). Use Phase 2 Dropdown and Button components.
    - **Active courses container:** Container with "Active Courses" header + badge count. List of course items using ListItem component. Each item shows: color indicator bar, course title, semester badge, description, exercise count + ID. Clickable — sends `viewCourseDetails` command with courseData. Subtle hover state using `var(--vscode-list-hoverBackground)`.
    - **Load archived button:** Show "Load Archived Courses" button when archived not yet loaded. Sends `loadArchivedCourses` command.
    - **Archived courses section:** Container with "Archived Courses" header + badge count. Archived course items (color indicator, title, semester, "Archived" label). Clickable — sends `viewArchivedCourse` command.
    - **Search results info:** Show "Found X active courses and Y archived courses matching..." when filters active. Show "No courses found matching your criteria" when 0 results.
    - **Filtering:** All client-side. Search filters by title, semester, description (case-insensitive). Type filter shows/hides active/archived. Semester filter matches exact semester. Sort applies via store's filteredCourses getter. Use `useCourseListStore` selectors.
    - **State persistence:** Persist searchTerm, typeFilter, semesterFilter, sortBy via `vscodeApi.setState()` in useEffect. Restore on mount from `vscodeApi.getState()`.
    - **Breadcrumbs:** Push "Courses" breadcrumb on mount using `useNavigationStore`. On unmount or navigation, breadcrumbs updated by the target view.
    - **Loading/error:** SkeletonList (count=5) during loading. ErrorMessage with retry on error.
    - **Message handler:** Listen for `courseListInit` and `archivedCoursesLoaded` messages. Handle both typed and legacy formats.

    **2. Create CourseListView.module.css:**
    Match legacy course-list.css layout:
    - `.backLinkContainer` flex row with gap.
    - `.courseListContainer` with padding.
    - `.headerContainer` with title and search area.
    - `.searchContainer` with full-width input.
    - `.controlsGrid` CSS grid (3 columns + action) for filters.
    - `.coursesSection` and `.archivedSection` containers.
    - `.courseItem` with flex row, color indicator, content area. `.courseColorIndicator` 4px wide strip. `.courseHeader` flex row with title and semester badge. `.courseDescription` truncated with `color: var(--vscode-descriptionForeground)`. `.courseStats` flex row.
    - `.loadArchivedSection` centered button.
    - `.searchResultsInfo` styled info text.
    - `.hidden` utility class.
    - Hover state: `.courseItem:hover { background: var(--vscode-list-hoverBackground) }`.

    **3. Register in router:**
    - `views/CourseList/index.ts`: Export CourseListView and types.
    - Update `views/index.ts`: Add CourseList exports.
    - Update `App.tsx`: Add `case 'courseList': return <CourseListView vscodeApi={vscodeApi} />;`
    - Update `viewRouter.ts`: Add `['course-list', true]` to `_reactViews` map.
    - Update `artemisWebviewProvider.ts`: In ready-signal handler, when state is 'course-list', send `courseListInit` with `_appStateManager.coursesData` and `_appStateManager.archivedCoursesData`. Bridge legacy `loadArchivedCourses` response to `archivedCoursesLoaded` typed message.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify CourseList renders through React with search and filter working</manual>
  </verify>
  <done>CourseList renders through React with search, sort, type filter, semester filter, archived courses loading, breadcrumb navigation. Persists filter state across tab cycles. Registered in coexistence router.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run compile` succeeds
- CourseList view renders through React coexistence router
- Search filters courses by title/semester/description
- Sort dropdown reorders courses correctly
- Type filter toggles active/archived visibility
- Load Archived button fetches and displays archived courses
- Breadcrumbs show "Courses" on CourseList view
- Filter state persists across tab hide/show
</verification>

<success_criteria>
- CourseList React view matches legacy layout (header, filters, course items, archived section)
- Zustand store manages courses, filters, loading, error state
- Client-side search/sort/filter works identically to legacy JavaScript
- Semester comparison logic (WS/SS format) works correctly
- Breadcrumbs display "Courses" on this view
- State persistence for filters via getState/setState
- Registered in coexistence router and renders through React
</success_criteria>

<output>
After completion, create `.planning/phases/04-main-ui-views/04-02-SUMMARY.md`
</output>
