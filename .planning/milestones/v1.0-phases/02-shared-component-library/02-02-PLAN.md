---
phase: 02-shared-component-library
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - iris-thaumantias/src/views/webview/react/components/TextInput/TextInput.tsx
  - iris-thaumantias/src/views/webview/react/components/TextInput/TextInput.module.css
  - iris-thaumantias/src/views/webview/react/components/TextInput/index.ts
  - iris-thaumantias/src/views/webview/react/components/Dropdown/Dropdown.tsx
  - iris-thaumantias/src/views/webview/react/components/Dropdown/Dropdown.module.css
  - iris-thaumantias/src/views/webview/react/components/Dropdown/index.ts
  - iris-thaumantias/src/views/webview/react/components/Container/Container.tsx
  - iris-thaumantias/src/views/webview/react/components/Container/Container.module.css
  - iris-thaumantias/src/views/webview/react/components/Container/index.ts
  - iris-thaumantias/src/views/webview/react/components/ListItem/ListItem.tsx
  - iris-thaumantias/src/views/webview/react/components/ListItem/ListItem.module.css
  - iris-thaumantias/src/views/webview/react/components/ListItem/index.ts
  - iris-thaumantias/src/views/webview/react/components/List/List.tsx
  - iris-thaumantias/src/views/webview/react/components/List/List.module.css
  - iris-thaumantias/src/views/webview/react/components/List/index.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-02

must_haves:
  truths:
    - "TextInput is a controlled component (parent owns value, onChange fires with new value string)"
    - "Dropdown is a controlled component (parent owns selected value, onChange fires with new value)"
    - "Container accepts header, footer, toolbar as ReactNode props and children as body content"
    - "ListItem is presentational-only with no internal state"
    - "List manages keyboard navigation (ArrowUp/Down) and selection state, injecting selected prop into children"
    - "All components adapt to both light and dark VS Code themes via --theme-* variables"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/components/TextInput/TextInput.tsx"
      provides: "Controlled text input with label, placeholder, validation, multi-variant support"
      exports: ["TextInput"]
    - path: "iris-thaumantias/src/views/webview/react/components/Dropdown/Dropdown.tsx"
      provides: "Controlled dropdown/select with typed options"
      exports: ["Dropdown"]
    - path: "iris-thaumantias/src/views/webview/react/components/Container/Container.tsx"
      provides: "Container with composition slots (header, footer, toolbar, children)"
      exports: ["Container"]
    - path: "iris-thaumantias/src/views/webview/react/components/ListItem/ListItem.tsx"
      provides: "Presentational list item with icon, title, subtitle, badge, action slots"
      exports: ["ListItem"]
    - path: "iris-thaumantias/src/views/webview/react/components/List/List.tsx"
      provides: "List wrapper with keyboard navigation and selection state"
      exports: ["List"]
  key_links:
    - from: "iris-thaumantias/src/views/webview/react/components/List/List.tsx"
      to: "ListItem"
      via: "Children.map + cloneElement injecting selected prop"
      pattern: "cloneElement.*selected"
    - from: "iris-thaumantias/src/views/webview/react/components/Container/Container.tsx"
      to: "Container.module.css"
      via: "CSS Modules import for themed layout slots"
      pattern: "import styles from.*Container\\.module\\.css"
---

<objective>
Create form components (TextInput, Dropdown) and layout/list components (Container, ListItem, List) as React components with CSS Modules.

Purpose: Deliver the controlled form components and the container/list layout components that view screens compose to build their UIs.
Output: 5 component folders with .tsx + .module.css + index.ts, all controlled/presentational per user decisions.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-component-library/02-RESEARCH.md
@.planning/phases/02-shared-component-library/02-01-SUMMARY.md

# Existing source components to port:
@iris-thaumantias/src/views/components/input/textInputComponent.ts
@iris-thaumantias/src/views/components/input/input.css
@iris-thaumantias/src/views/components/dropdown/dropdownComponent.ts
@iris-thaumantias/src/views/components/dropdown/dropdown.css
@iris-thaumantias/src/views/components/container/containerComponent.ts
@iris-thaumantias/src/views/components/container/container.css
@iris-thaumantias/src/views/components/listItem/listItemComponent.ts
@iris-thaumantias/src/views/components/listItem/list-item.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TextInput and Dropdown controlled form components</name>
  <files>
    iris-thaumantias/src/views/webview/react/components/TextInput/TextInput.tsx
    iris-thaumantias/src/views/webview/react/components/TextInput/TextInput.module.css
    iris-thaumantias/src/views/webview/react/components/TextInput/index.ts
    iris-thaumantias/src/views/webview/react/components/Dropdown/Dropdown.tsx
    iris-thaumantias/src/views/webview/react/components/Dropdown/Dropdown.module.css
    iris-thaumantias/src/views/webview/react/components/Dropdown/index.ts
  </files>
  <action>
    Port the form components as **controlled components** (parent owns value, component fires onChange callback). Per user decision: form components are controlled, flat props interfaces.

    **TextInput.tsx** — Port from `textInputComponent.ts` (276 lines) + `input.css` (203 lines):
    - Props interface: `value: string`, `onChange: (value: string) => void`, `placeholder?: string`, `disabled?: boolean`, `type?: 'text' | 'password' | 'email' | 'url'`, `label?: string`, `id?: string`, `className?: string`, `error?: string`, `hint?: string`, `required?: boolean`, `autoFocus?: boolean`, `onBlur?: () => void`, `onFocus?: () => void`, `onKeyDown?: (e: KeyboardEvent<HTMLInputElement>) => void`
    - The existing textInputComponent supports multiple variants (standard input, password with show/hide toggle, search input). Study the source carefully and port all variants.
    - For password toggle: use `useState` for show/hide state, render an eye/eye-off icon button toggling the input type.
    - CRITICAL: Always initialize value as empty string (never undefined) to avoid controlled/uncontrolled React warning.
    - Add `aria-describedby` linking to error/hint elements when present.
    - `TextInput.module.css` — Port all styles from `input.css`. Use camelCase class names. Preserve all --theme-input-* variable references.

    **Dropdown.tsx** — Port from `dropdownComponent.ts` (80 lines) + `dropdown.css` (71 lines):
    - Props interface: `value: string`, `onChange: (value: string) => void`, `options: Array<{ label: string; value: string }>`, `disabled?: boolean`, `label?: string`, `id?: string`, `className?: string`, `placeholder?: string`
    - Use native `<select>` element (per research: don't hand-roll dropdown accessibility).
    - If `placeholder` is provided, render a disabled `<option>` with empty value as first option.
    - Wrap in labeled container when `label` prop provided.
    - `Dropdown.module.css` — Port styles from `dropdown.css`. Preserve --theme-input-* variables.

    Both components:
    - Use `clsx` for conditional classes
    - Accept optional `className` prop for external styling override
    - Use --theme-* CSS variables only (keep abstraction layer)
    - `index.ts` re-exports the component
  </action>
  <verify>
    <automated>cd iris-thaumantias && npm run compile && npm run check-types && npm run lint</automated>
    <manual>Verify TextInput and Dropdown component files exist with CSS Modules</manual>
  </verify>
  <done>
    - TextInput is controlled (value + onChange), renders label/error/hint, supports password toggle
    - Dropdown is controlled (value + onChange), uses native select, renders options list
    - Both use CSS Modules with --theme-* variables
    - Build and type-check pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Container, ListItem, and List layout components</name>
  <files>
    iris-thaumantias/src/views/webview/react/components/Container/Container.tsx
    iris-thaumantias/src/views/webview/react/components/Container/Container.module.css
    iris-thaumantias/src/views/webview/react/components/Container/index.ts
    iris-thaumantias/src/views/webview/react/components/ListItem/ListItem.tsx
    iris-thaumantias/src/views/webview/react/components/ListItem/ListItem.module.css
    iris-thaumantias/src/views/webview/react/components/ListItem/index.ts
    iris-thaumantias/src/views/webview/react/components/List/List.tsx
    iris-thaumantias/src/views/webview/react/components/List/List.module.css
    iris-thaumantias/src/views/webview/react/components/List/index.ts
  </files>
  <action>
    Port the layout/list components. Per user decisions: Container stays as single component with typed props (not Card/CardHeader/CardBody), ListItem is presentational-only, List manages keyboard nav + selection.

    **Container.tsx** — Port from `containerComponent.ts` (319 lines) + `container.css` (406 lines):
    - Props interface: `children: ReactNode` (body content), `header?: ReactNode`, `footer?: ReactNode`, `toolbar?: ReactNode`, `variant?: 'default' | 'muted' | 'highlight' | 'warning'`, `className?: string`, `accentColor?: string`, `outline?: string`
    - Composition pattern: header, footer, toolbar as ReactNode slots, body via children (per user decision: flat props, compose externally)
    - For `accentColor` and `outline`: use React inline style prop (per user decision: dynamic styles via inline style)
    - No collapsible behavior in this pass — can be added later if needed (per research guidance). Keep Container stateless.
    - `Container.module.css` — Port from `container.css`. This is the largest CSS file (406 lines). Preserve all layout structure: `.container`, `.header`, `.toolbar`, `.body`, `.footer` sections. Port variant styles. Use camelCase class names. Keep all --theme-* variable references.

    **ListItem.tsx** — Port from `listItemComponent.ts` (230 lines) + `list-item.css` (95 lines):
    - Props interface: `children?: ReactNode`, `icon?: ReactNode`, `title?: string`, `subtitle?: string`, `badge?: ReactNode`, `action?: ReactNode`, `onClick?: () => void`, `selected?: boolean`, `disabled?: boolean`, `className?: string`, `id?: string`
    - PRESENTATIONAL ONLY (per user decision) — no internal state. `selected` prop is injected by parent List component.
    - Render with `role="option"` and `aria-selected` for accessibility.
    - When `onClick` is provided, render as clickable (cursor pointer, hover state).
    - `ListItem.module.css` — Port from `list-item.css`. Include selected state, hover state, disabled state styles.

    **List.tsx** — New component (wraps ListItem children with keyboard navigation):
    - Props interface: `children: ReactNode`, `onSelect?: (index: number) => void`, `className?: string`, `ariaLabel?: string`
    - Manages `selectedIndex` state with `useState`
    - Keyboard handler (`onKeyDown`): ArrowDown moves selectedIndex forward (wraps around), ArrowUp moves backward (wraps around), Enter/Space triggers `onSelect` callback
    - Uses `Children.map` + `cloneElement` to inject `selected` and `id` props into ListItem children
    - Renders with `role="listbox"`, `tabIndex={0}`, `aria-activedescendant` pointing to selected item
    - `List.module.css` — Minimal styles: focus outline on the list wrapper, vertical layout for children

    All components:
    - Use `clsx` for conditional class composition
    - Accept optional `className` prop
    - Use --theme-* CSS variables
    - `index.ts` re-exports the component
  </action>
  <verify>
    <automated>cd iris-thaumantias && npm run compile && npm run check-types && npm run lint</automated>
    <manual>Verify Container, ListItem, and List component files exist with CSS Modules</manual>
  </verify>
  <done>
    - Container renders with composition slots (header, footer, toolbar, children as body)
    - Container supports accentColor/outline via inline styles (no collapsible behavior — deferred)
    - ListItem is presentational with icon, title, subtitle, badge, action slots
    - List manages keyboard navigation and injects selected prop into children
    - All CSS Modules use --theme-* variables for theme compliance
    - Build and type-check pass
  </done>
</task>

</tasks>

<verification>
1. `cd iris-thaumantias && npm run compile` — all bundles produced
2. `cd iris-thaumantias && npm run check-types` — zero type errors
3. `cd iris-thaumantias && npm run lint` — zero lint errors
4. `ls iris-thaumantias/src/views/webview/react/components/` — TextInput, Dropdown, Container, ListItem, List directories exist
5. Each component folder contains .tsx, .module.css, and index.ts files
</verification>

<success_criteria>
- TextInput and Dropdown are controlled components (value + onChange)
- Container uses composition pattern with ReactNode slots
- ListItem is presentational-only (no internal state)
- List manages keyboard navigation with ArrowUp/Down/Enter/Space
- All components use CSS Modules with --theme-* variables
- Build, type-check, and lint all pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-component-library/02-02-SUMMARY.md`
</output>
