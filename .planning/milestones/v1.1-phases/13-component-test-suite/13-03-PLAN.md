---
phase: 13-component-test-suite
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - iris-thaumantias/test/react/stores/useNavigationStore.test.ts
  - iris-thaumantias/test/react/stores/useCourseListStore.test.ts
  - iris-thaumantias/test/react/stores/useCourseDetailStore.test.ts
  - iris-thaumantias/test/react/stores/useExerciseDetailStore.test.ts
  - iris-thaumantias/test/react/stores/useDashboardStore.test.ts
  - iris-thaumantias/test/react/stores/useChatStore.test.ts
  - iris-thaumantias/test/react/stores/useExamStartStore.test.ts
  - iris-thaumantias/test/react/stores/useExamConductionStore.test.ts
  - iris-thaumantias/test/react/stores/useExamExerciseDetailStore.test.ts
autonomous: true
requirements:
  - TEST-02

must_haves:
  truths:
    - "All 9 Zustand stores have passing unit tests with 90%+ coverage"
    - "Every store action, selector, and state transition is tested"
    - "Store state is explicitly reset in beforeEach to prevent test pollution"
    - "PostMessage contract verification included for stores that send messages"
    - "Tests are implementation-aware (test internal state transitions, not just DOM output)"
  artifacts:
    - path: "iris-thaumantias/test/react/stores/useNavigationStore.test.ts"
      provides: "Navigation store tests — routing, back/forward, view stack"
    - path: "iris-thaumantias/test/react/stores/useCourseListStore.test.ts"
      provides: "Course list store tests — load, filter, sort"
    - path: "iris-thaumantias/test/react/stores/useCourseDetailStore.test.ts"
      provides: "Course detail store tests — load course, exercises"
    - path: "iris-thaumantias/test/react/stores/useExerciseDetailStore.test.ts"
      provides: "Exercise detail store tests — load, participation, submission"
    - path: "iris-thaumantias/test/react/stores/useDashboardStore.test.ts"
      provides: "Extended dashboard store tests"
    - path: "iris-thaumantias/test/react/stores/useChatStore.test.ts"
      provides: "Chat store tests — messages, streaming, context"
    - path: "iris-thaumantias/test/react/stores/useExamStartStore.test.ts"
      provides: "Exam start store tests — exam data, countdown"
    - path: "iris-thaumantias/test/react/stores/useExamConductionStore.test.ts"
      provides: "Exam conduction store tests — exercise navigation, submission"
    - path: "iris-thaumantias/test/react/stores/useExamExerciseDetailStore.test.ts"
      provides: "Exam exercise detail store tests"
  key_links:
    - from: "store test files"
      to: "src/views/webview/react/stores/*"
      via: "direct import + renderHook"
      pattern: "renderHook.*use.*Store"
    - from: "store tests"
      to: "createMockVsCodeApi"
      via: "postMessage verification"
      pattern: "mockApi.postMessage.*toHaveBeenCalledWith"
---

<objective>
Write comprehensive unit tests for all 9 Zustand stores achieving 90%+ coverage per store, testing every action, selector, and state transition with implementation-aware assertions.

Purpose: Stores are the central state management layer. Testing them thoroughly prevents state-related bugs that cascade into views. The 90%+ target ensures every code path is exercised, including error handling and edge cases.
Output: 9 store test files (8 new + 1 extended) with implementation-aware assertions verifying state transitions, action execution, and postMessage contract compliance.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 10 sample store test (established pattern)
@iris-thaumantias/test/react/stores/useDashboardStore.test.ts

# Test helpers
@iris-thaumantias/test/react/__helpers__/vscodeApi.ts

# Source stores to test
@iris-thaumantias/src/views/webview/react/stores/useNavigationStore.ts
@iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts
@iris-thaumantias/src/views/webview/react/stores/useCourseDetailStore.ts
@iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts
@iris-thaumantias/src/views/webview/react/stores/useChatStore.ts
@iris-thaumantias/src/views/webview/react/stores/useExamStartStore.ts
@iris-thaumantias/src/views/webview/react/stores/useExamConductionStore.ts
@iris-thaumantias/src/views/webview/react/stores/useExamExerciseDetailStore.ts

# Message contracts (for postMessage verification)
@iris-thaumantias/src/views/webview/react/types/messageContracts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test navigation and course stores (useNavigationStore, useCourseListStore, useCourseDetailStore, useExerciseDetailStore, extend useDashboardStore)</name>
  <files>
    iris-thaumantias/test/react/stores/useNavigationStore.test.ts
    iris-thaumantias/test/react/stores/useCourseListStore.test.ts
    iris-thaumantias/test/react/stores/useCourseDetailStore.test.ts
    iris-thaumantias/test/react/stores/useExerciseDetailStore.test.ts
    iris-thaumantias/test/react/stores/useDashboardStore.test.ts
  </files>
  <action>
Read each store source file to understand its state shape, actions, and selectors. Write tests using renderHook from @testing-library/react with explicit state resets in beforeEach.

**CRITICAL PATTERN (from CONTEXT.md):** Always reset store state before each test to prevent pollution:
```typescript
beforeEach(() => {
  storeUnderTest.setState({ /* complete initial state */ });
});
```

**useNavigationStore (15-20 tests):**
Read the store source to find all state properties and actions. Test:
- Initial state (default view, empty history)
- Navigate to view — updates currentView state
- Navigate back — pops view from history stack
- Navigate forward — pushes view to forward stack (if applicable)
- History stack management (push on navigate, pop on back)
- Deep navigation chain (A -> B -> C -> back -> B)
- Navigate to same view (no-op or duplicate handling)
- View parameters passed correctly (courseId, exerciseId)
- Reset navigation state

**useCourseListStore (10-15 tests):**
- Initial state (empty courses, not loading)
- loadCourses action sends correct postMessage
- setCourses populates course list and stops loading
- setError sets error and stops loading
- Loading state transitions (idle -> loading -> loaded/error)
- Filter courses if filtering exists
- Sort courses if sorting exists
- Reset state

**useCourseDetailStore (10-15 tests):**
- Initial state (null course, empty exercises)
- loadCourse sends postMessage with courseId
- setCourseData populates course and exercises, stops loading
- setError sets error, stops loading
- Exercise list extraction from course data
- Loading state transitions

**useExerciseDetailStore (10-15 tests):**
- Initial state (null exercise, null participation)
- loadExercise sends postMessage with exerciseId
- setExerciseData populates exercise details
- Participation state management (no participation -> active -> submitted)
- Build status tracking (idle -> building -> complete/error)
- Results setting (score, test cases)
- Submission state management
- Error handling

**useDashboardStore (extend existing 9 tests with 5-10 more):**
Read the existing test file and the store source. Add tests for any untested actions, selectors, or state transitions. Target: cover all remaining code paths to reach 90%+.

For ALL stores: verify postMessage calls use exact contract shape from messageContracts.ts (not just `toHaveBeenCalled` — use `toHaveBeenCalledWith` with full payload).
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx vitest run test/react/stores/useNavigationStore test/react/stores/useCourseListStore test/react/stores/useCourseDetailStore test/react/stores/useExerciseDetailStore test/react/stores/useDashboardStore --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>5 store test files pass with 10-20 tests each. All state transitions, actions, selectors, and postMessage contracts verified. useDashboardStore extended to 90%+ coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Test chat and exam stores (useChatStore, useExamStartStore, useExamConductionStore, useExamExerciseDetailStore)</name>
  <files>
    iris-thaumantias/test/react/stores/useChatStore.test.ts
    iris-thaumantias/test/react/stores/useExamStartStore.test.ts
    iris-thaumantias/test/react/stores/useExamConductionStore.test.ts
    iris-thaumantias/test/react/stores/useExamExerciseDetailStore.test.ts
  </files>
  <action>
Read each store source file, then write comprehensive tests.

**useChatStore (15-20 tests):**
- Initial state (empty messages, not streaming)
- addMessage adds user message to messages array
- startStreaming sets isStreaming true, initializes empty streaming message
- appendStreamChunk appends text to currentStreamingMessage
- finalizeStreaming moves streaming message to messages array, sets isStreaming false
- sendMessage calls postMessage with correct contract (command: sendChatMessage or equivalent)
- Exercise context selection and storage
- Referenced files management
- Conversation history preservation
- Error state handling (set, clear)
- Clear chat resets messages and state
- Multiple sequential messages
- Streaming interrupted (error during stream)

**useExamStartStore (10-15 tests):**
- Initial state (null exam, not loading)
- loadExam sends postMessage with examId
- setExamData populates exam details (title, dates, duration)
- Countdown calculation from exam start time
- Loading and error state transitions
- Exam not yet started state
- Exam already started state
- Exam ended state

**useExamConductionStore (10-15 tests):**
- Initial state (null exercises, null current exercise)
- Load exam exercises
- Navigate between exam exercises (next, previous, jump to index)
- Current exercise tracking
- Submit individual exercise answer
- Submit entire exam
- Time remaining integration (receives time updates)
- Exercise completion tracking
- Error handling

**useExamExerciseDetailStore (10-15 tests):**
- Initial state
- Load exercise detail within exam context
- Set exercise data with exam-specific fields
- Participation state within exam
- Build and submission status
- Results display
- Error handling

Same patterns as Task 1: beforeEach state reset, renderHook, act() for actions, postMessage contract verification.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx vitest run test/react/stores/useChatStore test/react/stores/useExamStartStore test/react/stores/useExamConductionStore test/react/stores/useExamExerciseDetailStore --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>4 store test files pass with 10-20 tests each. Chat streaming flow, exam lifecycle, and exam exercise management fully tested with 90%+ coverage target met.</done>
</task>

</tasks>

<verification>
- All 9 store test files exist and pass: `cd iris-thaumantias && npx vitest run test/react/stores/ --reporter=verbose`
- Every store has beforeEach state reset
- Every action that calls postMessage verified with toHaveBeenCalledWith (full payload)
- Coverage check: `cd iris-thaumantias && npx vitest run test/react/stores/ --coverage 2>&1 | grep -A5 "stores"`
- No store test depends on another store's state (isolation)
</verification>

<success_criteria>
All 9 Zustand stores have comprehensive unit tests achieving 90%+ coverage, with every action, selector, and state transition verified. PostMessage contracts verified with exact payload shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-component-test-suite/13-03-SUMMARY.md`
</output>
