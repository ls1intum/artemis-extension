---
phase: 13-component-test-suite
plan: 08
type: execute
wave: 3
depends_on:
  - 13-03
  - 13-05
  - 13-06
files_modified:
  - iris-thaumantias/test/react/flows/errors.flow.test.tsx
  - iris-thaumantias/test/react/flows/examTimer.flow.test.tsx
  - iris-thaumantias/test/react/flows/messageContracts.test.ts
  - iris-thaumantias/vitest.config.mts
  - iris-thaumantias/package.json
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "Error suite tests error boundaries and reconnection states separately from happy-path flows"
    - "ServiceHealth and ReconnectBanner tested in error suite context"
    - "Exam timer Web Worker tested for tick accuracy, pause/resume, and expiry notification"
    - "Message contract tests verify type-safe postMessage contracts between webview and extension host"
    - "Coverage reporting configured to track and report but not fail builds on thresholds"
    - "Coverage reports generated showing 80%+ overall and 90%+ on stores/critical paths"
  artifacts:
    - path: "iris-thaumantias/test/react/flows/errors.flow.test.tsx"
      provides: "Error boundary and reconnection test suite"
    - path: "iris-thaumantias/test/react/flows/examTimer.flow.test.tsx"
      provides: "Exam timer Worker integration test"
    - path: "iris-thaumantias/test/react/flows/messageContracts.test.ts"
      provides: "PostMessage contract drift detection tests"
    - path: "iris-thaumantias/vitest.config.mts"
      provides: "Updated coverage configuration"
    - path: "iris-thaumantias/package.json"
      provides: "Updated test:react:coverage script"
  key_links:
    - from: "errors.flow.test.tsx"
      to: "ServiceHealth + ReconnectBanner components"
      via: "error state rendering"
      pattern: "ServiceHealth|ReconnectBanner"
    - from: "messageContracts.test.ts"
      to: "messageContracts.ts type definitions"
      via: "contract shape verification"
      pattern: "messageContracts"
    - from: "vitest.config.mts"
      to: "coverage thresholds"
      via: "reporter configuration"
      pattern: "coverage.*thresholds|reporter"
---

<objective>
Write the dedicated error/reconnection test suite, exam timer Worker integration test, message contract verification tests, and configure coverage reporting to track the 80% overall / 90% critical path target.

Purpose: Error handling and edge cases are tested separately from happy-path flows per CONTEXT.md decision. Message contract tests catch contract drift between webview and extension host. Coverage configuration ensures progress toward the 80% combined target is tracked and reported. This plan closes out TEST-03 by covering error boundaries, exam timer Worker behavior, and contract verification.
Output: 3 test files (error suite, exam timer flow, message contracts) + updated coverage configuration.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Test helpers
@iris-thaumantias/test/react/__helpers__/vscodeApi.ts
@iris-thaumantias/test/react/__helpers__/vitest.setup.ts

# Error-related components
@iris-thaumantias/src/views/webview/react/components/ServiceHealth/ServiceHealth.tsx
@iris-thaumantias/src/views/webview/react/components/ReconnectBanner/ReconnectBanner.tsx
@iris-thaumantias/src/views/webview/react/components/ErrorMessage/ErrorMessage.tsx

# Exam timer
@iris-thaumantias/src/views/webview/react/components/ExamTimer/ExamTimer.tsx
@iris-thaumantias/src/views/webview/react/hooks/useExamTimer.ts

# Message contracts
@iris-thaumantias/src/views/webview/react/types/messageContracts.ts

# Vitest config
@iris-thaumantias/vitest.config.mts
@iris-thaumantias/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write error suite, exam timer Worker test, and message contract tests</name>
  <files>
    iris-thaumantias/test/react/flows/errors.flow.test.tsx
    iris-thaumantias/test/react/flows/examTimer.flow.test.tsx
    iris-thaumantias/test/react/flows/messageContracts.test.ts
  </files>
  <action>
Create three specialized test files covering error handling, exam timer Worker behavior, and message contract verification.

**errors.flow.test.tsx — Dedicated error suite (CONTEXT.md: separate from happy-path flows, includes ServiceHealth and ReconnectBanner):**

Test "displays error boundary on component crash":
- Render a view that throws during render
- Verify error boundary catches and displays fallback UI
- Verify error message is user-friendly (not stack trace)

Test "shows ServiceHealth degraded state":
- Render ServiceHealth component
- Simulate health check response with degraded services
- Verify degraded status indicators
- Verify individual service status displayed

Test "shows ServiceHealth disconnected state":
- Simulate all services disconnected
- Verify disconnected UI state

Test "ReconnectBanner shows on connection loss":
- Simulate WebSocket disconnection via dispatchExtensionMessage
- Verify ReconnectBanner appears
- Verify reconnect button visible

Test "ReconnectBanner reconnect action":
- Click reconnect button
- Verify reconnect postMessage sent
- Simulate successful reconnection
- Verify banner disappears

Test "handles API error responses in views":
- Render a view (e.g., CourseListView)
- Simulate error response via dispatchExtensionMessage
- Verify error message displayed
- Verify retry button visible
- Click retry, verify re-request sent

Test "handles network timeout":
- Render view, trigger data load
- Simulate timeout (no response within expected time)
- Verify loading state transitions to error

Test "multiple concurrent errors":
- Simulate errors in multiple stores simultaneously
- Verify error states don't interfere with each other

**examTimer.flow.test.tsx — Exam timer Worker integration (CONTEXT.md: tick accuracy, pause/resume, expiry notification):**

Mock Worker globally:
```typescript
let workerInstance: { postMessage: Mock; onmessage: ((e: { data: unknown }) => void) | null; terminate: Mock };
beforeEach(() => {
  workerInstance = { postMessage: vi.fn(), onmessage: null, terminate: vi.fn() };
  global.Worker = vi.fn(() => workerInstance) as unknown as typeof Worker;
  vi.useFakeTimers();
});
afterEach(() => {
  vi.runOnlyPendingTimers();
  vi.useRealTimers();
});
```

Test "receives tick messages and updates display":
- Render ExamTimer with endTime 10 minutes from now
- Simulate Worker tick: workerInstance.onmessage({ data: { type: 'TICK', remaining: 600000, expired: false } })
- Verify display shows "10:00"
- Simulate another tick: remaining: 540000
- Verify display shows "09:00"

Test "shows expired state when time runs out":
- Simulate tick with remaining: 0, expired: true
- Verify expired message/overlay displayed

Test "shows warning state when time is low":
- Simulate tick with remaining: 240000 (4 minutes)
- Verify warning visual state (urgent text or color change)

Test "pause and resume":
- If pause/resume supported: verify postMessage to Worker, verify display freezes, verify resume updates

Test "expiry notification triggers callback":
- Pass onExpiry callback prop
- Simulate expiry tick
- Verify onExpiry called

Test "handles edge cases":
- Negative remaining time (already expired)
- Very large remaining time (hours)
- Zero initial time

**messageContracts.test.ts — Contract drift detection (CONTEXT.md: dedicated tests verifying type-safe postMessage contracts):**

Import message contract types from messageContracts.ts. Write tests that:

Test "WebviewToExtensionMessage types cover all commands":
- Import the union type
- Verify key command types exist (login, logout, loadCourseList, sendChatMessage, etc.)
- Use TypeScript type assertions to verify payload shapes compile correctly

Test "ExtensionToWebviewMessage types cover all responses":
- Verify key response types exist (showLoggedIn, courseListLoaded, chatStreamChunk, etc.)
- Type assertions for payload shapes

Test "postMessage payloads match contract shapes":
- Create mock messages matching each contract type
- Verify they satisfy the type constraint (compile-time verification)
- Verify required fields are present
- Verify optional fields are handled

Test "dispatchExtensionMessage payloads match contract shapes":
- Create mock extension messages matching each contract type
- Verify type/payload structure

Note: These are structural/type verification tests. They catch contract drift when someone modifies a message type without updating all consumers. Use satisfies/type assertions for compile-time checks and runtime shape validation for key contracts.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx vitest run test/react/flows/errors.flow test/react/flows/examTimer.flow test/react/flows/messageContracts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>3 test files pass. Error suite covers error boundaries, ServiceHealth, ReconnectBanner in error scenarios. Exam timer Worker tested for tick accuracy, warning state, and expiry. Message contracts verified for type safety and drift detection.</done>
</task>

<task type="auto">
  <name>Task 2: Configure coverage reporting and verify thresholds</name>
  <files>
    iris-thaumantias/vitest.config.mts
    iris-thaumantias/package.json
  </files>
  <action>
Update Vitest configuration to track coverage and add reporting scripts. CONTEXT.md decision: track and report only — do NOT fail builds on thresholds.

**Update vitest.config.mts:**
Read the existing config. Add/update the coverage section:

```typescript
coverage: {
  provider: 'v8',
  reporter: ['text', 'text-summary', 'html', 'lcov'],
  reportsDirectory: './coverage/react',
  include: [
    'src/views/webview/react/**/*.{ts,tsx}',
  ],
  exclude: [
    'src/views/webview/react/**/*.d.ts',
    'src/views/webview/react/**/index.ts',
    'src/views/webview/react/**/*.css.ts',
  ],
  // Track only — do NOT fail builds (CONTEXT.md decision)
  // thresholds: { /* intentionally not set */ }
},
```

Important: Do NOT add `thresholds` that would fail builds. Coverage is tracked and reported only.

**Update package.json scripts:**
Read existing scripts. Ensure these exist:
- `"test:react"` — runs React tests without coverage (already exists from Phase 10)
- `"test:react:coverage"` — runs React tests WITH coverage reporting
- `"test:react:ui"` — opens Vitest UI (already exists from Phase 10)

If `test:react:coverage` doesn't exist, add it:
```json
"test:react:coverage": "vitest run --config vitest.config.mts --coverage"
```

**Run coverage and document results:**
After configuring, run `npx vitest run --config vitest.config.mts --coverage` and verify:
1. Coverage report generates successfully (HTML + text summary)
2. Text summary shows per-file coverage percentages
3. Stores show 90%+ coverage (if not, note gaps for future improvement)
4. Overall React coverage percentage is reported

Do NOT add coverage threshold enforcement. The team tracks coverage metrics manually per CONTEXT.md decision.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx vitest run --config vitest.config.mts --coverage 2>&1 | tail -50</automated>
  </verify>
  <done>Coverage reporting configured. test:react:coverage script produces HTML and text reports. Coverage metrics visible without failing builds. Store coverage at 90%+ target verified in report output.</done>
</task>

</tasks>

<verification>
- Error suite passes: `cd iris-thaumantias && npx vitest run test/react/flows/errors.flow --reporter=verbose`
- Exam timer test passes: `cd iris-thaumantias && npx vitest run test/react/flows/examTimer.flow --reporter=verbose`
- Message contract tests pass: `cd iris-thaumantias && npx vitest run test/react/flows/messageContracts --reporter=verbose`
- Coverage report generates: `cd iris-thaumantias && npx vitest run --config vitest.config.mts --coverage`
- Coverage does NOT fail builds (no threshold enforcement)
- All React tests still pass: `cd iris-thaumantias && npm run test:react`
</verification>

<success_criteria>
Dedicated error suite covers error boundaries, reconnection, and service health in error scenarios. Exam timer Worker tested for tick accuracy, pause/resume, and expiry. Message contract tests catch type drift. Coverage reporting configured to track 80% overall / 90% critical path targets without failing builds.
</success_criteria>

<output>
After completion, create `.planning/phases/13-component-test-suite/13-08-SUMMARY.md`
</output>
