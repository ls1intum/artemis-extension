---
phase: 10-testing-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - iris-thaumantias/test/react/components/Button/Button.test.tsx
  - iris-thaumantias/test/react/stores/useDashboardStore.test.ts
  - iris-thaumantias/test/react/views/Login/LoginView.test.tsx
  - iris-thaumantias/test/react/__helpers__/README.md
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Running `npm run test:react` executes and passes all 3 sample test files (Button, useDashboardStore, LoginView)"
    - "Button test validates rendering, click handlers, variant styles, disabled state, and icon modes"
    - "useDashboardStore test validates initial state, loading action with postMessage call, and setDashboardData sorting/limiting"
    - "LoginView test validates form submission sends login command via postMessage, and incoming extension messages update UI state"
    - "test/react/__helpers__/README.md documents mock patterns with usage examples"
    - "Running `npm run test:react:coverage` shows coverage for tested components/stores"
  artifacts:
    - path: "iris-thaumantias/test/react/components/Button/Button.test.tsx"
      provides: "Button component test demonstrating RTL patterns"
      contains: "describe.*Button"
    - path: "iris-thaumantias/test/react/stores/useDashboardStore.test.ts"
      provides: "Zustand store test demonstrating store testing patterns"
      contains: "describe.*useDashboardStore"
    - path: "iris-thaumantias/test/react/views/Login/LoginView.test.tsx"
      provides: "Bridge-using component test demonstrating postMessage mock and dispatchExtensionMessage"
      contains: "describe.*LoginView"
    - path: "iris-thaumantias/test/react/__helpers__/README.md"
      provides: "Documentation of mock patterns and helper usage"
      contains: "createMockVsCodeApi"
  key_links:
    - from: "iris-thaumantias/test/react/components/Button/Button.test.tsx"
      to: "src/views/webview/react/components/Button/Button.tsx"
      via: "import { Button }"
      pattern: "import.*Button.*from"
    - from: "iris-thaumantias/test/react/stores/useDashboardStore.test.ts"
      to: "src/views/webview/react/stores/useDashboardStore.ts"
      via: "import { useDashboardStore }"
      pattern: "import.*useDashboardStore.*from"
    - from: "iris-thaumantias/test/react/views/Login/LoginView.test.tsx"
      to: "test/react/__helpers__/vscodeApi.ts"
      via: "import { createMockVsCodeApi, dispatchExtensionMessage }"
      pattern: "import.*dispatchExtensionMessage"
---

<objective>
Write sample tests for Button component, useDashboardStore, and LoginView (bridge-using component) to validate the testing infrastructure. Document mock patterns in README.

Purpose: Prove the testing infrastructure works end-to-end with real project components. These sample tests establish patterns that Phase 13 will replicate across all 22 components and 9 stores. The tests must include meaningful assertions (interactions, state changes, message payloads), not just smoke/render tests — per user decision.

Output: 3 passing test files, helper README documentation.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-testing-infrastructure/10-RESEARCH.md
@.planning/phases/10-testing-infrastructure/10-CONTEXT.md
@.planning/phases/10-testing-infrastructure/10-01-SUMMARY.md

@iris-thaumantias/src/views/webview/react/components/Button/Button.tsx
@iris-thaumantias/src/views/webview/react/stores/useDashboardStore.ts
@iris-thaumantias/src/views/webview/react/views/Login/LoginView.tsx
@iris-thaumantias/src/views/webview/react/views/Login/types.ts
@iris-thaumantias/src/shared/messageContracts.ts
@iris-thaumantias/test/react/__helpers__/vscodeApi.ts
@iris-thaumantias/test/react/__helpers__/renderWithProviders.tsx
@iris-thaumantias/test/react/__helpers__/vitest.setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Button component test and useDashboardStore test</name>
  <files>
    iris-thaumantias/test/react/components/Button/Button.test.tsx
    iris-thaumantias/test/react/stores/useDashboardStore.test.ts
  </files>
  <action>
    **Button component test** at `test/react/components/Button/Button.test.tsx`:

    Import `render`, `screen` from `@testing-library/react`, `userEvent` from `@testing-library/user-event`, `Button` from the source, and `vi`, `describe`, `it`, `expect` from `vitest`.

    Test cases (per user decision: meaningful assertions, not just smoke tests):

    1. **Renders with correct text** — render `<Button>Click me</Button>`, assert `screen.getByText('Click me')` is in the document
    2. **Calls onClick handler when clicked** — render with `onClick: vi.fn()`, `await userEvent.click(...)`, assert `handleClick` called once
    3. **Applies primary variant by default** — render `<Button>Submit</Button>`, check the button element has the primary CSS class via `toHaveClass`. Since CSS modules produce hashed class names in real builds but may be identity-proxied in tests, use a flexible approach: check the button element exists and is rendered as a `<button>` element with `screen.getByRole('button')`
    4. **Renders disabled button** — render `<Button disabled>Disabled</Button>`, assert `screen.getByRole('button')` `toBeDisabled()`
    5. **Renders icon-only button with aria-label** — render `<Button icon={<span>X</span>} />`, assert `screen.getByRole('button')` has `aria-label="button"`
    6. **Renders button with icon and label** — render with both `icon` and `children`, assert both the icon element and text content are visible
    7. **Does not call onClick when disabled** — render disabled button with `onClick: vi.fn()`, click it, assert handler NOT called
    8. **Supports fullWidth prop** — render with `fullWidth`, verify button renders correctly
    9. **Supports custom testId** — render with `testId="my-btn"`, assert `screen.getByTestId('my-btn')` exists

    Note: CSS module class names in happy-dom may not resolve to real class names. Focus assertions on behavior (click, disabled, aria attributes) and DOM structure (role, text content), not class names. This follows React Testing Library best practices.

    **useDashboardStore test** at `test/react/stores/useDashboardStore.test.ts`:

    Import `renderHook`, `act` from `@testing-library/react`, `useDashboardStore` from source, `createMockVsCodeApi` from helpers, and `describe`, `it`, `expect`, `beforeEach`, `vi` from `vitest`.

    Reset store state in `beforeEach`:
    ```typescript
    beforeEach(() => {
      useDashboardStore.setState({
        recentCourses: [],
        workspaceExercise: null,
        isLoading: false,
        error: null,
      });
    });
    ```

    Test cases:

    1. **Initializes with empty state** — renderHook, assert `recentCourses` is `[]`, `isLoading` is `false`, `error` is `null`, `workspaceExercise` is `null`
    2. **Sets loading state when loadDashboard called** — create mock vscodeApi, call `result.current.loadDashboard(vscodeApi)` in `act`, assert `isLoading` is `true` and `error` is `null`
    3. **Sends reloadDashboard command via postMessage** — after loadDashboard, assert `vscodeApi.postMessage` called with `{ type: 'command', command: 'reloadDashboard' }`
    4. **Sorts courses by date and limits to 3** — create 4 course objects with different startDate values, call `setDashboardData`, assert `recentCourses` has length 3 and first item is the newest
    5. **Sets isLoading to false after setDashboardData** — call setDashboardData, assert `isLoading` is `false`
    6. **Sets error and stops loading on setError** — call `setError('Something failed')`, assert `error` is `'Something failed'` and `isLoading` is `false`
    7. **Sets workspace exercise** — call `setWorkspaceExercise({ id: 1, title: 'Ex 1' })`, assert value matches
    8. **Clears workspace exercise with null** — call `setWorkspaceExercise(null)`, assert `workspaceExercise` is `null`

    For course test data, create helper objects matching the `RecentCourseNode` interface:
    ```typescript
    const makeCourse = (title: string, startDate: string): RecentCourseNode => ({
      courseData: { course: { title, startDate } },
      exercises: [],
    });
    ```
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx vitest run test/react/components/Button/Button.test.tsx test/react/stores/useDashboardStore.test.ts 2>&1 | tail -15</automated>
    <manual>All Button and useDashboardStore tests pass. Button tests validate rendering, clicks, disabled state, icon modes. Store tests validate state management, postMessage calls, sorting logic.</manual>
  </verify>
  <done>Button test passes with 7+ meaningful assertions covering rendering, interaction, disabled state, icon modes, and testId. useDashboardStore test passes with 8 assertions covering initial state, loading, postMessage, sorting, error handling, and workspace exercise.</done>
</task>

<task type="auto">
  <name>Task 2: Write LoginView bridge test and create helper README</name>
  <files>
    iris-thaumantias/test/react/views/Login/LoginView.test.tsx
    iris-thaumantias/test/react/__helpers__/README.md
  </files>
  <action>
    **LoginView bridge test** at `test/react/views/Login/LoginView.test.tsx`:

    This is the critical test that validates the VS Code webview bridge mocking pattern end-to-end. It must prove that:
    - Components can send messages to the extension via `postMessage`
    - Components can receive messages from the extension via `window.dispatchEvent(MessageEvent)`
    - The mock helpers work correctly for both directions

    Import `render`, `screen`, `waitFor` from `@testing-library/react`, `userEvent` from `@testing-library/user-event`, `LoginView` from source, `createMockVsCodeApi`, `dispatchExtensionMessage` from helpers.

    Read `LoginView.tsx` carefully before writing tests. The component:
    - Accepts `{ vscodeApi }` prop (VsCodeApi)
    - Has view states: 'form', 'loading', 'loggedIn'
    - Persists form state via `vscodeApi.setState()`
    - Listens for window messages (showLoading, hideLoading, loginSuccess, loginError, etc.)
    - Submits login via `vscodeApi.postMessage({ type: 'command', command: 'login', ... })`

    Test cases:

    1. **Renders login form by default** — render `<LoginView vscodeApi={mockApi} />`, assert username input, password input, and login button are visible. Use `screen.getByLabelText` or `screen.getByPlaceholderText` or `screen.getByRole` depending on how the form is structured.

    2. **Persists form state via setState** — render, type into username field, assert `vscodeApi.setState` was called with object containing the username value. Use `await userEvent.type(...)` and `waitFor` for the persistence effect to fire.

    3. **Sends login command on form submit** — render, fill in username and password, click login button, assert `vscodeApi.postMessage` was called with an object containing `type: 'command'` and `command: 'login'`.

    4. **Shows loading state when receiving showLoading message** — render, dispatch `{ type: 'showLoading', message: 'Checking...' }` via `dispatchExtensionMessage`, use `waitFor` to assert the loading UI appears (look for loading text or spinner).

    5. **Returns to form on hideLoading message** — render, dispatch showLoading then hideLoading, assert form inputs are visible again.

    6. **Restores persisted state from getState** — create mock with `getState: vi.fn(() => ({ username: 'saved-user', password: 'saved-pass', rememberMe: true }))`, render, assert the username input has value 'saved-user'.

    **Important adaptation notes:**
    - Read the actual LoginView.tsx source carefully — the form structure, input names/labels, and message handling may differ from the research examples
    - Use `screen.getByRole('textbox')`, `screen.getByRole('button')`, or data-testid attributes based on what the actual component renders
    - The LoginView uses `Container` wrapper — tests should still be able to find form elements within it
    - If the component uses `TextInput` (custom component), check what HTML it renders (likely `<input>`) and query accordingly
    - For message handling: the component uses `window.addEventListener('message', handler)` pattern — `dispatchExtensionMessage` dispatches a MessageEvent on window which should trigger the handler

    **Helper README** at `test/react/__helpers__/README.md`:

    Document the testing patterns and helpers. Structure:

    ```markdown
    # React Test Helpers

    ## Overview
    Test utilities for React component and store testing in the VS Code webview context.

    ## Helpers

    ### createMockVsCodeApi(overrides?)
    [Description, usage example, override pattern]

    ### dispatchExtensionMessage(message)
    [Description, usage example, typed vs legacy format]

    ### renderWithProviders(ui, options?)
    [Description, usage example]

    ## Patterns

    ### Testing Components
    [Standard component test pattern with example]

    ### Testing Zustand Stores
    [Store test pattern with beforeEach reset, example]

    ### Testing Bridge Communication
    [postMessage assertions, dispatchExtensionMessage for incoming messages, example]

    ### Testing with Persisted State
    [getState mock override pattern, example]

    ## Anti-Patterns
    - Don't test implementation details (internal state, class names)
    - Always await userEvent interactions
    - Always reset store state in beforeEach
    - Use screen queries, not container
    ```

    Keep the README concise (~80-120 lines). Focus on patterns that Phase 13 test authors will need.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx vitest run 2>&1 | tail -20</automated>
    <manual>All 3 test files pass. LoginView test validates both outgoing (postMessage) and incoming (dispatchExtensionMessage) message patterns. README exists with pattern documentation.</manual>
  </verify>
  <done>LoginView bridge test passes with 4+ assertions covering form rendering, state persistence, login command submission, and extension message handling. README documents all helper APIs and patterns. Running `npm run test:react` executes all 3 test files with green results.</done>
</task>

</tasks>

<verification>
1. `cd iris-thaumantias && npm run test:react` — all 3 test files pass
2. `cd iris-thaumantias && npm run test:react:coverage` — coverage report generated for Button.tsx, useDashboardStore.ts, LoginView.tsx
3. `cat test/react/__helpers__/README.md` — pattern documentation exists
4. Test count: Button (7+ tests) + useDashboardStore (8 tests) + LoginView (4+ tests) = 19+ total assertions
</verification>

<success_criteria>
- All 3 sample test files pass: Button, useDashboardStore, LoginView
- Tests include meaningful assertions (not smoke tests) — interactions, state changes, message payloads
- LoginView test demonstrates both postMessage (webview -> extension) and dispatchExtensionMessage (extension -> webview) patterns
- Coverage report shows coverage for tested files
- README documents all helper patterns
</success_criteria>

<output>
After completion, create `.planning/phases/10-testing-infrastructure/10-02-SUMMARY.md`
</output>
