---
phase: 12-typescript-strict-mode
plan: 14
type: execute
wave: 2
depends_on: [12-09]
files_modified:
  - iris-thaumantias/src/api/artemisApi.ts
  - iris-thaumantias/src/auth/auth.ts
  - iris-thaumantias/src/utils/problemStatementProcessor.ts
  - iris-thaumantias/src/utils/workspaceFileChecker.ts
  - iris-thaumantias/src/types/stomp.d.ts
autonomous: true
gap_closure: true
requirements:
  - TYPE-01
  - TYPE-03

must_haves:
  truths:
    - "npx eslint src/api/ src/auth/ src/utils/ src/types/stomp.d.ts exits with zero type safety errors"
    - "API client methods return typed responses (not untyped or any)"
    - "Auth operations use typed token and user data structures"
    - "Utility functions have explicit parameter and return types"
  artifacts:
    - path: "iris-thaumantias/src/api/artemisApi.ts"
      provides: "Type-safe API client with typed response generics"
    - path: "iris-thaumantias/src/auth/auth.ts"
      provides: "Type-safe auth module with typed token/user data"
    - path: "iris-thaumantias/src/utils/problemStatementProcessor.ts"
      provides: "Type-safe HTML/DOM processing with proper DOM types"
    - path: "iris-thaumantias/src/utils/workspaceFileChecker.ts"
      provides: "Type-safe file system operations with vscode.Uri types"
  key_links:
    - from: "artemisApi.ts"
      to: "types/apiResponses.ts"
      via: "Typed API responses"
      pattern: "import.*from.*apiResponses"
    - from: "auth.ts"
      to: "types/apiResponses.ts"
      via: "User profile response types"
      pattern: "import.*from.*apiResponses"
---

<objective>
Eliminate all remaining ESLint type safety errors in api, auth, utils, and type declaration files (56 errors across 5 files).

Purpose: These are the final non-React, non-service, non-command files with type safety violations. Fixing them completes TYPE-03 coverage for the infrastructure layer. Combined with Plans 12-10 (services), 12-11 (commands), and 12-12 (React views), this achieves zero ESLint errors across the entire codebase.

Output: `npx eslint src/api/ src/auth/ src/utils/ src/types/stomp.d.ts` exits with zero errors.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typescript-strict-mode/12-09-SUMMARY.md

<interfaces>
<!-- Error breakdown for 56 ESLint errors in api/auth/utils/types -->

artemisApi.ts (~15 errors):
- API response typing: HTTP client returns untyped data
- Request parameter typing
- Error response handling with any catch blocks
- Fix: Add generic type parameters to API calls, import types from apiResponses.ts

auth.ts (~12 errors):
- Token data typing (access_token, refresh_token, expires_in)
- User profile response from Artemis auth endpoint
- Credential storage (VS Code SecretStorage API types)
- Error handling with any catch blocks

problemStatementProcessor.ts (~12 errors):
- DOM manipulation: HTMLElement, Element, NodeListOf<Element>
- Regex match results: RegExpMatchArray | null
- HTML processing intermediate data structures
- Function parameters and return types

workspaceFileChecker.ts (~12 errors):
- File system operations: vscode.Uri, vscode.WorkspaceFolder
- Workspace scanning results
- Error handling with any catch blocks

stomp.d.ts (~5 errors):
- Type declaration file itself has ESLint no-explicit-any violations
- STOMP types may genuinely need any at library boundary
- Fix: Replace with unknown where possible, eslint-disable with justification where unavoidable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type api client and auth module (2 files, ~27 errors)</name>
  <files>
    iris-thaumantias/src/api/artemisApi.ts
    iris-thaumantias/src/auth/auth.ts
  </files>
  <action>
Eliminate all ESLint type safety errors in artemisApi.ts (~15 errors) and auth.ts (~12 errors).

**artemisApi.ts (~15 errors):**
1. Type API response generics — add generic type parameters to API calls: `get<T>(url): Promise<T>`. Import response types from types/apiResponses.ts for all endpoints.
2. Type request parameters and headers with proper interfaces (e.g., `interface RequestConfig { headers?: Record<string, string>; params?: Record<string, string | number>; }`).
3. Replace `any` in error handling with `catch (error: unknown)` pattern and narrow with `error instanceof Error`.
4. Type any data transformation or response parsing.

**auth.ts (~12 errors):**
1. Type token data: create or import `interface TokenData { access_token: string; refresh_token: string; expires_in: number; token_type: string; }`.
2. Type user profile response from Artemis auth endpoint — use ArtemisUser from types or create auth-specific profile interface.
3. Type the credential storage using VS Code `SecretStorage` API types.
4. Fix all catch blocks with `catch (error: unknown)` pattern.
5. Type any auth state management (logged in/out, token expiry).

Run `npx eslint src/api/ src/auth/` — target zero errors.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/api/ src/auth/ 2>&1 | tail -1</automated>
  </verify>
  <done>Zero ESLint errors in artemisApi.ts and auth.ts. API client uses typed response generics. Auth module uses typed token and user data.</done>
</task>

<task type="auto">
  <name>Task 2: Type utils and STOMP declarations (3 files, ~29 errors)</name>
  <files>
    iris-thaumantias/src/utils/problemStatementProcessor.ts
    iris-thaumantias/src/utils/workspaceFileChecker.ts
    iris-thaumantias/src/types/stomp.d.ts
  </files>
  <action>
Eliminate all ESLint type safety errors in utils and type declaration files (~29 errors).

**problemStatementProcessor.ts (~12 errors):**
1. Type DOM manipulation with proper DOM types: `HTMLElement`, `Element`, `NodeListOf<Element>`, `Document`.
2. Type regex match results: `RegExpMatchArray | null`.
3. Type intermediate HTML processing data structures with explicit interfaces.
4. Add explicit parameter types and return type annotations to all functions.

**workspaceFileChecker.ts (~12 errors):**
1. Type file system operations with `vscode.Uri`, `vscode.WorkspaceFolder`.
2. Type workspace scanning results with proper interfaces (e.g., file paths as string[], workspace info).
3. Fix all catch blocks with `catch (error: unknown)` pattern.
4. Add explicit parameter and return types.

**stomp.d.ts (~5 errors):**
1. Replace `any` with `unknown` where possible in STOMP type declarations.
2. For STOMP library boundary types that genuinely require `any` (e.g., message body parsing), add eslint-disable-next-line with justification comment.
3. Check if more specific types are possible for frame headers (`Record<string, string>`), message body (`string | ArrayBuffer`), etc.

After fixing all files, run the full codebase lint to verify:
```bash
npx eslint src/
```

This verifies the entire codebase is clean when combined with Plans 12-10 (services), 12-11 (commands), 12-12 (React), and 12-13 (providers/commands compilation).
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/api/ src/auth/ src/utils/ src/types/stomp.d.ts 2>&1 | tail -1 | grep -q "^$\|0 problems" && echo "PASS" || (npx eslint src/api/ src/auth/ src/utils/ src/types/stomp.d.ts 2>&1 | tail -3; echo "FAIL")</automated>
  </verify>
  <done>Zero ESLint errors in api, auth, utils, and STOMP type declarations. All infrastructure files fully type-safe.</done>
</task>

</tasks>

<verification>
```bash
# Infrastructure layer ESLint check
cd iris-thaumantias && npx eslint src/api/ src/auth/ src/utils/ src/types/stomp.d.ts
# Expected: 0 errors

# Verify no explicit any in these files
grep -rn ": any\|as any\|<any>" src/api/ src/auth/ src/utils/ --include="*.ts" | grep -v "eslint-disable" | wc -l
# Expected: 0

# TypeScript compilation still passes
npx tsc --noEmit 2>&1 | grep "src/api/\|src/auth/\|src/utils/" | wc -l
# Expected: 0

# Full codebase verification (should be clean when all Wave 1+2 plans complete)
npx eslint src/ 2>&1 | tail -3
npm run compile
```
</verification>

<success_criteria>
- `npx eslint src/api/ src/auth/ src/utils/ src/types/stomp.d.ts` exits with zero errors
- Zero explicit `any` in api, auth, utils files (except justified STOMP boundary with eslint-disable)
- `npx tsc --noEmit` reports no new errors from infrastructure changes
- API client methods return typed responses
- Auth operations use typed token and user data structures
- Utility functions have explicit parameter and return types
</success_criteria>

<output>
After completion, create `.planning/phases/12-typescript-strict-mode/12-14-SUMMARY.md`
</output>
