---
phase: 12-typescript-strict-mode
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - iris-thaumantias/eslint.config.mjs
  - iris-thaumantias/src/provider/artemisWebviewProvider.ts
  - iris-thaumantias/src/provider/chatWebviewProvider.ts
  - iris-thaumantias/src/views/app/webViewMessageHandler.ts
  - iris-thaumantias/src/views/app/commands/repositoryCommands.ts
  - iris-thaumantias/src/views/app/commands/navigationCommands.ts
  - iris-thaumantias/src/views/app/commands/utilityCommands.ts
  - iris-thaumantias/src/views/app/commands/healthCommands.ts
  - iris-thaumantias/src/views/app/commands/plantUmlCommands.ts
  - iris-thaumantias/src/views/app/appStateManager.ts
  - iris-thaumantias/src/views/app/types.ts
  - iris-thaumantias/src/services/sessionManagementService.ts
  - iris-thaumantias/src/services/websocketMessageHandler.ts
  - iris-thaumantias/src/services/chatContextManager.ts
  - iris-thaumantias/src/services/artemisWebsocketService.ts
  - iris-thaumantias/src/services/irisSessionManager.ts
  - iris-thaumantias/src/services/chatMessageService.ts
  - iris-thaumantias/src/services/chatDiagnosticsService.ts
  - iris-thaumantias/src/extension.ts
autonomous: true
requirements: [TYPE-03]

must_haves:
  truths:
    - "ESLint @typescript-eslint/no-explicit-any rule set to error"
    - "ESLint no-unsafe-assignment, no-unsafe-return, no-unsafe-member-access, no-unsafe-call, no-unsafe-argument rules set to error"
    - "Zero any types remain in extension host source code (src/provider/, src/views/app/, src/services/, src/extension.ts)"
    - "All command handler signatures use proper message types instead of any"
    - "All webview message handlers use type guards to narrow from unknown/untyped messages"
    - "npm run lint passes with zero errors (excluding warnings)"
  artifacts:
    - path: "iris-thaumantias/eslint.config.mjs"
      provides: "Strict type-checked ESLint configuration with no-explicit-any and no-unsafe-* rules"
      contains: "no-explicit-any"
    - path: "iris-thaumantias/src/views/app/types.ts"
      provides: "Properly typed WebViewActionHandler interface with specific message types"
      contains: "WebViewActionHandler"
  key_links:
    - from: "iris-thaumantias/src/views/app/commands/*.ts"
      to: "iris-thaumantias/src/shared/messageContracts.ts"
      via: "Command handlers import and use typed message contracts"
      pattern: "import.*from.*messageContracts"
    - from: "iris-thaumantias/src/provider/artemisWebviewProvider.ts"
      to: "iris-thaumantias/src/views/app/webViewMessageHandler.ts"
      via: "Provider dispatches typed messages to handler"
      pattern: "WebviewToExtensionMessage"
    - from: "iris-thaumantias/eslint.config.mjs"
      to: "all .ts/.tsx files"
      via: "ESLint enforces no-any across codebase"
      pattern: "no-explicit-any.*error"
---

<objective>
Enable ESLint strict type-checking rules and eliminate all `any` types from extension host code.

Purpose: Enforce TYPE-03 (no explicit any in codebase) across the extension host layer — the heaviest concentration of any types (providers, command handlers, services, state management). This is the largest single batch of any elimination work.

Output: ESLint strict rules active; all extension host .ts files pass lint with zero any types.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typescript-strict-mode/12-RESEARCH.md
@.planning/phases/12-typescript-strict-mode/12-01-SUMMARY.md

Key source files:
@iris-thaumantias/eslint.config.mjs
@iris-thaumantias/src/shared/messageContracts.ts
@iris-thaumantias/src/views/app/types.ts
@iris-thaumantias/src/views/app/webViewMessageHandler.ts

<interfaces>
<!-- From src/shared/messageContracts.ts (existing, use as model): -->
<!-- Already has discriminated unions for typed messages: -->
<!--   ExtensionToWebviewMessage (all init/update messages) -->
<!--   WebviewToExtensionMessage (all command messages) -->
<!--   Type guards: isWebviewMessage(), isExtensionMessage() -->

<!-- From src/views/app/types.ts (needs fixing): -->
<!-- Currently: -->
<!--   export interface WebViewActionHandler { -->
<!--     handler: (message: any) => Promise<void>; -->
<!--   } -->
<!-- Needs: specific message type replacing any -->

<!-- Key any hotspots to fix (by file, ~207 occurrences total): -->
<!-- src/provider/artemisWebviewProvider.ts: 23 -->
<!-- src/views/app/commands/repositoryCommands.ts: 22 -->
<!-- src/views/app/commands/navigationCommands.ts: 20 -->
<!-- src/views/app/commands/utilityCommands.ts: 18 -->
<!-- src/views/app/appStateManager.ts: 16 -->
<!-- src/provider/chatWebviewProvider.ts: 10 -->
<!-- src/views/app/webViewMessageHandler.ts: 6 -->
<!-- src/views/app/commands/healthCommands.ts: 5 -->
<!-- src/services/sessionManagementService.ts: 5 -->
<!-- src/services/websocketMessageHandler.ts: 4 -->
<!-- src/services/chatContextManager.ts: 4 -->
<!-- src/services/artemisWebsocketService.ts: 4 -->
<!-- Plus smaller counts across other service files -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable ESLint strict type-checking rules</name>
  <files>
    iris-thaumantias/eslint.config.mjs
  </files>
  <action>
    Update eslint.config.mjs to enable strict @typescript-eslint rules. The ESLint config currently uses flat config format with @typescript-eslint/parser.

    **Add parserOptions.project for type-aware rules:**
    The type-aware rules (no-unsafe-*) require parser access to the TypeScript project. Add `parserOptions: { project: "./tsconfig.json" }` to the languageOptions section.

    **Add strict type-checking rules to the rules section:**
    ```javascript
    "@typescript-eslint/no-explicit-any": "error",
    "@typescript-eslint/no-unsafe-assignment": "error",
    "@typescript-eslint/no-unsafe-return": "error",
    "@typescript-eslint/no-unsafe-member-access": "error",
    "@typescript-eslint/no-unsafe-call": "error",
    "@typescript-eslint/no-unsafe-argument": "error",
    ```

    Per user decision: set to error from the start (not warn-then-flip).

    **Exclude test files from type-aware rules:**
    The type-aware rules require project configuration. Test files (test/**) may not be covered by the main tsconfig project. Add a config block for test files that disables the type-aware rules (no-unsafe-*) but keeps no-explicit-any as error. This prevents ESLint failures on test files that aren't part of the main tsconfig project.

    **Also exclude config files (.js, .mjs):**
    The existing config override for .js/.mjs files should also disable the type-aware rules since those aren't TypeScript.

    **Important:** The no-restricted-imports rule for lucide-react barrel imports must be preserved (from Phase 11-03).

    Run `npx eslint --max-warnings 0 src/extension.ts 2>&1` to verify the new rules are active and producing errors (they should — any types exist). This confirms the rules are working before fixing the code.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/extension.ts 2>&1 | grep -q "no-explicit-any\|no-unsafe" && echo "PASS: strict rules active" || echo "FAIL: rules not active"</automated>
  </verify>
  <done>ESLint strict type-checking rules are active and enforcing. npx eslint reports no-explicit-any and no-unsafe-* errors on files with any types. Rules are configured as error (not warn).</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate all any types from extension host code</name>
  <files>
    iris-thaumantias/src/provider/artemisWebviewProvider.ts
    iris-thaumantias/src/provider/chatWebviewProvider.ts
    iris-thaumantias/src/views/app/webViewMessageHandler.ts
    iris-thaumantias/src/views/app/commands/repositoryCommands.ts
    iris-thaumantias/src/views/app/commands/navigationCommands.ts
    iris-thaumantias/src/views/app/commands/utilityCommands.ts
    iris-thaumantias/src/views/app/commands/healthCommands.ts
    iris-thaumantias/src/views/app/commands/plantUmlCommands.ts
    iris-thaumantias/src/views/app/appStateManager.ts
    iris-thaumantias/src/views/app/types.ts
    iris-thaumantias/src/services/sessionManagementService.ts
    iris-thaumantias/src/services/websocketMessageHandler.ts
    iris-thaumantias/src/services/chatContextManager.ts
    iris-thaumantias/src/services/artemisWebsocketService.ts
    iris-thaumantias/src/services/irisSessionManager.ts
    iris-thaumantias/src/services/chatMessageService.ts
    iris-thaumantias/src/services/chatDiagnosticsService.ts
    iris-thaumantias/src/extension.ts
  </files>
  <action>
    Systematically replace ALL `any` types in extension host code with specific interfaces and union types. Per user decision: prefer specific types over `unknown`, and refactor code where necessary.

    **Strategy — work by layer, heaviest files first:**

    **Layer 1: Types and contracts (fix the interfaces first)**
    - `src/views/app/types.ts`: Replace `handler: (message: any) => Promise<void>` with a properly typed handler signature. The message parameter should use the WebviewToExtensionMessage union from messageContracts.ts, or a more specific subset per command module.
    - Read messageContracts.ts to understand the existing discriminated union pattern — use it as the model for all replacements.

    **Layer 2: Command handlers (22+20+18+5+3 = 68 any occurrences)**
    For each command file (repositoryCommands.ts, navigationCommands.ts, utilityCommands.ts, healthCommands.ts, plantUmlCommands.ts):
    - Replace `(message: any)` handler parameters with the specific message type from messageContracts.ts
    - Replace any internal `any` casts with proper type narrowing
    - Add explicit return type annotations to all handler functions
    - If the handler accesses message.payload or message.command, ensure those are typed through the discriminated union

    **Layer 3: Providers (23+10 = 33 any occurrences)**
    - `artemisWebviewProvider.ts`: The main webview provider. Replace any in:
      - `onDidReceiveMessage` callback: Use type guard `isWebviewMessage()` from messageContracts.ts
      - Internal state variables typed as any → use specific state types
      - Data passing to views → use domain model types
    - `chatWebviewProvider.ts`: Similar treatment for chat-specific messages

    **Layer 4: Services (5+4+4+4+3+3+3 = 26 any occurrences)**
    - For each service file, replace any with:
      - Domain model types from src/models/ (ArtemisUser, ArtemisCourse, ArtemisExercise, etc.)
      - API response types from src/types/apiResponses.ts
      - WebSocket message types from src/types/stomp.d.ts
      - Specific callback signatures instead of generic any

    **Layer 5: State management and extension entry (16+remaining)**
    - `appStateManager.ts`: Replace any in state variables and method signatures with domain types
    - `extension.ts`: Replace any in command registrations and service initializations

    **Key patterns to apply:**
    1. VS Code API boundaries: `webviewPanel.webview.onDidReceiveMessage((message: unknown) => { if (isWebviewMessage(message)) { ... } })` — use unknown + type guard at VS Code API boundary per user discretion. Document with comment: "// VS Code API boundary: message typed as unknown, narrowed by type guard"
    2. Domain data: Use existing model types (ArtemisUser, ArtemisCourse, etc.) — DO NOT create new `unknown` wrappers
    3. JSON parsing: `JSON.parse()` returns `unknown` in strict mode — add type assertions with comments where type guards aren't practical
    4. Error catch blocks: Use `unknown` for catch parameters (strict mode default with useUnknownInCatchVariables), narrow with `instanceof Error`

    **Add explicit return type annotations** to all functions in modified files (user requirement: all functions, not just where inference fails).

    **If a type error reveals a runtime bug:** Fix the bug, not just the type (user requirement).

    **Final check:** Run `npx eslint src/provider/ src/views/app/ src/services/ src/extension.ts --max-warnings 0` to verify zero any-related lint errors in extension host code.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/provider/ src/views/app/ src/services/ src/extension.ts --max-warnings 0 2>&1 | grep -c "no-explicit-any\|no-unsafe" | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Zero any types in extension host code. All command handlers use typed message contracts. All service methods have explicit return types. ESLint strict rules pass on all extension host files. npx tsc --noEmit still produces zero errors.</done>
</task>

</tasks>

<verification>
1. `cd iris-thaumantias && npx eslint src/provider/ src/views/app/ src/services/ src/extension.ts --max-warnings 0` passes (zero any-related errors)
2. `cd iris-thaumantias && npx tsc --noEmit` still exits with zero errors
3. `grep -rn ": any\b\|as any\b\|<any>" iris-thaumantias/src/provider/ iris-thaumantias/src/views/app/ iris-thaumantias/src/services/ iris-thaumantias/src/extension.ts` returns zero results (or only documented VS Code API boundaries)
4. `npm run compile` succeeds (full build)
</verification>

<success_criteria>
- ESLint strict type-checking rules active (no-explicit-any, no-unsafe-* all set to error)
- Zero any types in extension host layer (providers, commands, services, extension.ts)
- All handler functions use typed message contracts from messageContracts.ts
- All functions have explicit return type annotations
- Both npx tsc --noEmit and npm run lint pass cleanly
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/12-typescript-strict-mode/12-02-SUMMARY.md`
</output>
