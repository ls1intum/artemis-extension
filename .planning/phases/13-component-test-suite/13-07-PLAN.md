---
phase: 13-component-test-suite
plan: 07
type: execute
wave: 3
depends_on:
  - 13-03
  - 13-05
  - 13-06
files_modified:
  - iris-thaumantias/test/react/flows/auth.flow.test.tsx
  - iris-thaumantias/test/react/flows/courseNavigation.flow.test.tsx
  - iris-thaumantias/test/react/flows/navigation.flow.test.tsx
  - iris-thaumantias/test/react/flows/exerciseSubmission.flow.test.tsx
  - iris-thaumantias/test/react/flows/irisChat.flow.test.tsx
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "Auth flow test covers full lifecycle: login -> session persistence -> token refresh -> logout -> re-authentication"
    - "Course navigation flow tests CourseListView and CourseDetailView independently with mocked store data"
    - "Navigation flow tests useNavigationStore routing between views and back/forward navigation"
    - "Exercise submission flow covers full lifecycle: start participation -> submit -> build progress -> results -> score"
    - "Iris chat flow covers: context selection -> type message -> send -> streaming response -> final message with code blocks"
    - "All flow tests verify full postMessage round-trip (outbound messages AND inbound responses)"
    - "Streaming tests use simulated delays with vi.useFakeTimers() and advanceTimersByTimeAsync()"
  artifacts:
    - path: "iris-thaumantias/test/react/flows/auth.flow.test.tsx"
      provides: "Auth lifecycle integration test"
    - path: "iris-thaumantias/test/react/flows/courseNavigation.flow.test.tsx"
      provides: "Course browsing integration test"
    - path: "iris-thaumantias/test/react/flows/navigation.flow.test.tsx"
      provides: "Navigation store routing integration test"
    - path: "iris-thaumantias/test/react/flows/exerciseSubmission.flow.test.tsx"
      provides: "Exercise submission lifecycle integration test"
    - path: "iris-thaumantias/test/react/flows/irisChat.flow.test.tsx"
      provides: "Iris chat with streaming integration test"
  key_links:
    - from: "flow test files"
      to: "views + stores + postMessage"
      via: "multi-step user flow exercising full pipeline"
      pattern: "dispatchExtensionMessage.*postMessage"
    - from: "irisChat.flow.test.tsx"
      to: "useChatStore + IrisChatView"
      via: "streaming simulation with fake timers"
      pattern: "advanceTimersByTimeAsync|chatStreamChunk"
    - from: "exerciseSubmission.flow.test.tsx"
      to: "useExerciseDetailStore + ExerciseDetailView"
      via: "participation lifecycle with build progress"
      pattern: "startParticipation|submitExercise|buildProgress"
---

<objective>
Write integration tests for all 5 critical user flows: authentication lifecycle, course navigation, view navigation, exercise submission lifecycle, and Iris chat with streaming.

Purpose: Flow tests verify that multi-step user journeys work correctly end-to-end within the React webview layer. They exercise the full component-store-message pipeline across multiple interactions, catching integration issues that unit tests miss. These tests directly satisfy TEST-03 (expanded UI tests for critical flows) at 90%+ coverage on critical paths.
Output: 5 flow test files with multi-step scenarios, postMessage round-trip verification, and simulated streaming.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 10 sample view test (round-trip pattern reference)
@iris-thaumantias/test/react/views/Login/LoginView.test.tsx

# Test helpers
@iris-thaumantias/test/react/__helpers__/vscodeApi.ts
@iris-thaumantias/test/react/__helpers__/renderWithProviders.tsx

# Stores (for state setup and verification)
@iris-thaumantias/src/views/webview/react/stores/useChatStore.ts
@iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts
@iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts
@iris-thaumantias/src/views/webview/react/stores/useCourseDetailStore.ts
@iris-thaumantias/src/views/webview/react/stores/useNavigationStore.ts

# Views (for rendering in flow tests)
@iris-thaumantias/src/views/webview/react/views/Login/LoginView.tsx
@iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx
@iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
@iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
@iris-thaumantias/src/views/webview/react/views/IrisChat/IrisChatView.tsx

# Message contracts
@iris-thaumantias/src/views/webview/react/types/messageContracts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test auth, course navigation, and navigation flows</name>
  <files>
    iris-thaumantias/test/react/flows/auth.flow.test.tsx
    iris-thaumantias/test/react/flows/courseNavigation.flow.test.tsx
    iris-thaumantias/test/react/flows/navigation.flow.test.tsx
  </files>
  <action>
Create the `test/react/flows/` directory and write integration tests for three critical flows. Each flow test exercises a multi-step user journey with full postMessage round-trip verification.

**auth.flow.test.tsx — Full auth lifecycle (CONTEXT.md: login + session persistence + token refresh + logout + re-authentication):**

Test "completes full auth lifecycle":
1. Render LoginView with mock VS Code API
2. Enter username and password (userEvent.type)
3. Submit login form (userEvent.click on submit button)
4. OUTBOUND: Verify login postMessage sent with correct payload (username, password, serverUrl)
5. Simulate extension response: dispatchExtensionMessage({ type: 'showLoading', payload: { message: 'Authenticating...' } })
6. Verify loading state displayed
7. Simulate successful login: dispatchExtensionMessage({ type: 'showLoggedIn', payload: { userInfo: { username, serverUrl } } })
8. INBOUND: Verify logged-in state displayed (username, server URL visible)
9. Click logout button
10. OUTBOUND: Verify logout postMessage sent
11. Simulate logout response: dispatchExtensionMessage showing login form again
12. Verify login form re-displayed (re-authentication ready)

Additional auth tests:
- Login with invalid credentials shows error
- Token refresh flow if applicable (simulate token expired -> refresh -> success)
- Session persistence check on mount (extension sends existing session data)

**courseNavigation.flow.test.tsx — Course browsing (CONTEXT.md: test CourseListView and CourseDetailView independently with mocked store data):**

Test "course browsing from list to detail":
1. Render CourseListView with mock API
2. Verify loadCourseList postMessage sent on mount
3. Simulate course data: dispatchExtensionMessage with course list
4. Verify courses displayed
5. Click on a course
6. OUTBOUND: Verify navigateToCourseDetail postMessage with courseId

Test "course detail displays exercises":
1. Set up useCourseDetailStore with course data
2. Render CourseDetailView
3. Verify course title and exercises displayed
4. Click on exercise
5. OUTBOUND: Verify navigateToExerciseDetail postMessage with exerciseId

Test "handles loading and error states":
1. Verify loading skeleton during data fetch
2. Simulate error response
3. Verify error message with retry button
4. Click retry
5. Verify loadCourseList sent again

**navigation.flow.test.tsx — Dedicated navigation store integration (CONTEXT.md):**

Test "navigates between views with history":
1. Use renderHook with useNavigationStore
2. Navigate: Dashboard -> CourseList -> CourseDetail -> ExerciseDetail
3. Verify currentView updates at each step
4. Verify history stack builds correctly
5. Navigate back: verify ExerciseDetail -> CourseDetail -> CourseList -> Dashboard
6. Navigate forward if applicable

Test "handles deep navigation chain":
1. Navigate through 5+ views
2. Go back 3 steps
3. Navigate to new view (should clear forward history)
4. Verify back works, forward is empty

Test "preserves view parameters":
1. Navigate to CourseDetail with courseId=123
2. Navigate to ExerciseDetail with exerciseId=456
3. Go back
4. Verify courseId=123 preserved on CourseDetail
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx vitest run test/react/flows/auth.flow test/react/flows/courseNavigation.flow test/react/flows/navigation.flow --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>3 flow test files pass. Auth lifecycle (login -> session -> logout -> re-auth) verified. Course browsing (list -> detail -> exercise) verified with independent view tests. Navigation store routing and back/forward verified.</done>
</task>

<task type="auto">
  <name>Task 2: Test exercise submission and Iris chat flows with streaming</name>
  <files>
    iris-thaumantias/test/react/flows/exerciseSubmission.flow.test.tsx
    iris-thaumantias/test/react/flows/irisChat.flow.test.tsx
  </files>
  <action>
Write integration tests for the two most complex critical flows, both involving multi-step interactions with simulated streaming/progress.

**exerciseSubmission.flow.test.tsx — Full participation lifecycle (CONTEXT.md: start participation -> submit -> build progress -> results -> score display):**

Test "completes full submission lifecycle":
1. Render ExerciseDetailView with mock API
2. Simulate exercise data loaded (dispatchExtensionMessage with exercise details)
3. Verify exercise title and "Start Exercise" button displayed
4. Click "Start Exercise"
5. OUTBOUND: Verify startParticipation postMessage with exerciseId
6. Simulate participation started (dispatchExtensionMessage with participationId, repositoryUrl)
7. Verify "Submit Solution" button now displayed
8. Click "Submit Solution"
9. OUTBOUND: Verify submitExercise postMessage with exerciseId and participationId
10. Simulate build started: dispatchExtensionMessage({ type: 'buildStarted' })
11. Show build progress with simulated delays:
    - vi.useFakeTimers()
    - dispatchExtensionMessage({ type: 'buildProgress', payload: { stage: 'compiling', progress: 50 } })
    - await vi.advanceTimersByTimeAsync(1000)
    - dispatchExtensionMessage({ type: 'buildProgress', payload: { stage: 'testing', progress: 75 } })
    - await vi.advanceTimersByTimeAsync(1000)
12. Simulate build complete with results:
    - dispatchExtensionMessage with score, maxScore, test cases (passed and failed)
13. vi.useRealTimers()
14. INBOUND: Verify score displayed (percentage and fraction)
15. Verify test results listed with pass/fail status
16. Verify failed test shows error message

Additional submission tests:
- Build failure shows error state
- Re-submit after failure
- Exercise not started (no submit button visible)

**irisChat.flow.test.tsx — Context-aware conversation with streaming (CONTEXT.md: type message -> send -> streaming response -> final message with code blocks, PLUS exercise context selection, referenced files, conversation history):**

Test "completes full chat flow with context and streaming":
1. Render IrisChatView with mock API
2. Verify welcome state displayed (no messages)
3. Select exercise context:
   - Click context selector button
   - OUTBOUND: Verify context selection postMessage
   - Simulate extension response with exercise context
   - Verify exercise context displayed
4. Type message: userEvent.type(input, 'How do I implement binary search?')
5. Click send (or press Enter)
6. OUTBOUND: Verify sendChatMessage postMessage with message text + exercise context
7. Verify user message appears in chat list
8. Simulate streaming start: dispatchExtensionMessage({ type: 'chatStreamStart' })
9. Verify streaming indicator visible
10. vi.useFakeTimers()
11. Simulate streaming chunks with delays:
    - dispatchExtensionMessage({ type: 'chatStreamChunk', payload: { chunk: 'Binary search works by ' } })
    - await vi.advanceTimersByTimeAsync(50)
    - dispatchExtensionMessage({ type: 'chatStreamChunk', payload: { chunk: 'dividing the search space in half.\n\n```java\nint mid = (lo + hi) / 2;\n```' } })
    - await vi.advanceTimersByTimeAsync(50)
12. Simulate stream end: dispatchExtensionMessage({ type: 'chatStreamEnd' })
13. vi.useRealTimers()
14. Verify final message displayed (no streaming indicator)
15. Verify code block rendered within message

Additional chat tests:
- Multiple messages in conversation (history preservation)
- Referenced files display
- Send message without exercise context
- Streaming interrupted (error during stream shows error state)
- Empty message not sent (send button disabled)

IMPORTANT: Use advanceTimersByTimeAsync() (async version) to avoid deadlocks with waitFor. Switch to real timers before waitFor assertions that don't involve time-dependent behavior.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx vitest run test/react/flows/exerciseSubmission.flow test/react/flows/irisChat.flow --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>2 flow test files pass. Exercise submission full lifecycle verified with build progress simulation. Iris chat verified with context selection, streaming response simulation, and code block rendering in final message.</done>
</task>

</tasks>

<verification>
- All 5 flow test files pass: `cd iris-thaumantias && npx vitest run test/react/flows/ --reporter=verbose`
- Each flow test verifies postMessage round-trip (OUTBOUND + INBOUND)
- Streaming tests use vi.useFakeTimers() + advanceTimersByTimeAsync() (no deadlocks)
- No snapshot assertions
- Flow tests exercise real stores (not mocked) with state resets in beforeEach
- Exercise submission covers: start -> submit -> build progress -> results -> score
- Iris chat covers: context selection -> send -> stream -> finalize with code blocks
</verification>

<success_criteria>
All 5 critical user flows have integration tests with multi-step scenarios and full postMessage round-trip verification. Streaming behavior tested with simulated delays. Auth lifecycle, course browsing, navigation routing, exercise submission, and Iris chat flows all verified end-to-end within the React webview layer.
</success_criteria>

<output>
After completion, create `.planning/phases/13-component-test-suite/13-07-SUMMARY.md`
</output>
