---
phase: 13-component-test-suite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - iris-thaumantias/test/react/components/Button/IconButton.test.tsx
  - iris-thaumantias/test/react/components/TextInput/TextInput.test.tsx
  - iris-thaumantias/test/react/components/Dropdown/Dropdown.test.tsx
  - iris-thaumantias/test/react/components/SideMenu/SideMenu.test.tsx
  - iris-thaumantias/test/react/components/AskIris/AskIris.test.tsx
  - iris-thaumantias/test/react/components/exercise/ParticipationActions.test.tsx
  - iris-thaumantias/test/react/components/exercise/BuildProgress.test.tsx
  - iris-thaumantias/test/react/components/ExamTimer/ExamTimer.test.tsx
  - iris-thaumantias/test/react/components/ServiceHealth/ServiceHealth.test.tsx
  - iris-thaumantias/test/react/components/ReconnectBanner/ReconnectBanner.test.tsx
autonomous: true
requirements:
  - TEST-02

must_haves:
  truths:
    - "All 10 interactive/functional shared components have passing unit tests with 8-15 test cases each"
    - "Tests verify click handlers, keyboard navigation, focus management, disabled states, input validation"
    - "ExamTimer tests use Worker mock and vi.useFakeTimers() for deterministic time testing"
    - "Tests use userEvent for realistic interaction simulation, not fireEvent"
  artifacts:
    - path: "iris-thaumantias/test/react/components/Button/IconButton.test.tsx"
      provides: "IconButton interaction tests"
    - path: "iris-thaumantias/test/react/components/TextInput/TextInput.test.tsx"
      provides: "TextInput interaction tests"
    - path: "iris-thaumantias/test/react/components/Dropdown/Dropdown.test.tsx"
      provides: "Dropdown interaction tests"
    - path: "iris-thaumantias/test/react/components/SideMenu/SideMenu.test.tsx"
      provides: "SideMenu navigation tests"
    - path: "iris-thaumantias/test/react/components/ExamTimer/ExamTimer.test.tsx"
      provides: "ExamTimer timer + Worker tests"
    - path: "iris-thaumantias/test/react/components/ServiceHealth/ServiceHealth.test.tsx"
      provides: "ServiceHealth status display tests"
    - path: "iris-thaumantias/test/react/components/ReconnectBanner/ReconnectBanner.test.tsx"
      provides: "ReconnectBanner reconnection tests"
    - path: "iris-thaumantias/test/react/components/AskIris/AskIris.test.tsx"
      provides: "AskIris toggle and panel tests"
    - path: "iris-thaumantias/test/react/components/exercise/ParticipationActions.test.tsx"
      provides: "ParticipationActions interaction tests"
    - path: "iris-thaumantias/test/react/components/exercise/BuildProgress.test.tsx"
      provides: "BuildProgress status display tests"
  key_links:
    - from: "ExamTimer.test.tsx"
      to: "useExamTimer hook"
      via: "Worker mock + fake timers"
      pattern: "vi.useFakeTimers|global.Worker"
    - from: "interactive test files"
      to: "@testing-library/user-event"
      via: "userEvent.click, userEvent.type, userEvent.tab, userEvent.keyboard"
      pattern: "userEvent\\."
---

<objective>
Write unit tests for all 10 interactive and functional shared React components, including ExamTimer with Web Worker mocking and fake timers.

Purpose: Interactive components are the primary user touch points. Testing them with full interaction coverage (click, keyboard, focus, disabled states) prevents regressions in critical user interactions. ExamTimer requires special Worker mock and timer patterns that establish patterns for later plans.
Output: 10 test files with 8-15 test cases each, using userEvent for realistic interactions and vi.useFakeTimers() for time-dependent features.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 10 sample test (interaction pattern reference)
@iris-thaumantias/test/react/components/Button/Button.test.tsx

# Test helpers
@iris-thaumantias/test/react/__helpers__/vitest.setup.ts
@iris-thaumantias/test/react/__helpers__/vscodeApi.ts

# Source components to test
@iris-thaumantias/src/views/webview/react/components/Button/IconButton.tsx
@iris-thaumantias/src/views/webview/react/components/TextInput/TextInput.tsx
@iris-thaumantias/src/views/webview/react/components/Dropdown/Dropdown.tsx
@iris-thaumantias/src/views/webview/react/components/SideMenu/SideMenu.tsx
@iris-thaumantias/src/views/webview/react/components/ExamTimer/ExamTimer.tsx
@iris-thaumantias/src/views/webview/react/hooks/useExamTimer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test interactive input components (IconButton, TextInput, Dropdown, SideMenu)</name>
  <files>
    iris-thaumantias/test/react/components/Button/IconButton.test.tsx
    iris-thaumantias/test/react/components/TextInput/TextInput.test.tsx
    iris-thaumantias/test/react/components/Dropdown/Dropdown.test.tsx
    iris-thaumantias/test/react/components/SideMenu/SideMenu.test.tsx
  </files>
  <action>
Read each source component to understand props, event handlers, and DOM output. Write 8-15 test cases per component covering full interaction testing.

Use `@testing-library/user-event` (NOT `fireEvent`) for all interactions. Import as: `import userEvent from '@testing-library/user-event'`.

**IconButton (8-10 tests):**
- Renders icon element inside button
- Applies aria-label for accessibility (icon-only buttons need labels)
- Calls onClick handler on click
- Does not call onClick when disabled
- Supports keyboard activation (Enter, Space)
- Renders tooltip/title when provided
- Focus management (tab-focusable, shows focus ring)
- Supports different icon sizes
- Variant/preset props affect rendering

**TextInput (10-15 tests):**
- Renders with label (associated via htmlFor/id or wrapping)
- Calls onChange when typing
- Shows error message when error prop provided
- Disables input when disabled prop is true
- Supports keyboard navigation (Tab to focus)
- Handles placeholder text
- Supports password type (obscured input)
- Handles form submission on Enter key (if applicable)
- Shows character count if maxLength specified
- Clears input value programmatically
- Required field validation indicator

**Dropdown (10-15 tests):**
- Renders with placeholder text
- Opens option list on click
- Calls onChange when option selected
- Closes after selection
- Supports keyboard navigation (ArrowDown, ArrowUp, Enter, Escape)
- Closes on Escape key
- Disables dropdown when disabled prop
- Displays selected option text
- Handles empty options gracefully
- Supports custom option rendering if applicable

**SideMenu (8-10 tests):**
- Renders menu items with correct labels
- Highlights active/selected item
- Calls onSelect handler when item clicked
- Supports keyboard navigation between items
- Renders nested menu items if applicable
- Shows/hides menu sections
- Accessible navigation semantics (nav, role="menu" or role="navigation")
- Handles empty menu items array

Testing rules: getByRole/getByText/getByLabelText queries only, explicit assertions, no snapshots, no CSS class testing.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx vitest run test/react/components/Button/IconButton test/react/components/TextInput test/react/components/Dropdown test/react/components/SideMenu --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>4 interactive component test files pass with 8-15 tests each. All interactions tested via userEvent with keyboard navigation, focus management, and disabled states verified.</done>
</task>

<task type="auto">
  <name>Task 2: Test functional components (AskIris, ParticipationActions, BuildProgress, ExamTimer, ServiceHealth, ReconnectBanner)</name>
  <files>
    iris-thaumantias/test/react/components/AskIris/AskIris.test.tsx
    iris-thaumantias/test/react/components/exercise/ParticipationActions.test.tsx
    iris-thaumantias/test/react/components/exercise/BuildProgress.test.tsx
    iris-thaumantias/test/react/components/ExamTimer/ExamTimer.test.tsx
    iris-thaumantias/test/react/components/ServiceHealth/ServiceHealth.test.tsx
    iris-thaumantias/test/react/components/ReconnectBanner/ReconnectBanner.test.tsx
  </files>
  <action>
Read each source component, then create test files. These components have richer behavior involving state display, progress indicators, and time-dependent features.

**AskIris (8-10 tests):**
- Renders button/link to open Iris chat
- Calls handler when clicked (sends postMessage or triggers navigation)
- Displays exercise context when provided
- Disabled state when no exercise selected
- Keyboard accessible (Enter/Space activation)

**ParticipationActions (8-10 tests):**
- Renders "Start Exercise" button when no participation
- Renders "Submit Solution" button when participation exists
- Disables submit during build/submission
- Shows loading state during action
- Calls correct postMessage handler for each action
- Handles different exercise types (programming, quiz, etc.)

**BuildProgress (8-10 tests):**
- Displays build stage text (compiling, testing, etc.)
- Shows progress indicator/bar with percentage
- Updates display when progress changes
- Shows completion state when build finishes
- Shows error state on build failure

**ExamTimer (10-15 tests) — CRITICAL: Uses Worker mock and fake timers:**
- Mock Web Worker globally before tests:
  ```typescript
  const mockWorker = { postMessage: vi.fn(), onmessage: null as Function | null, terminate: vi.fn() };
  global.Worker = vi.fn().mockImplementation(() => mockWorker) as unknown as typeof Worker;
  ```
- Use vi.useFakeTimers() in beforeEach, vi.useRealTimers() in afterEach
- Run vi.runOnlyPendingTimers() before switching back to real timers
- Displays remaining time in MM:SS format
- Updates on Worker tick messages (simulate by calling mockWorker.onmessage)
- Shows expired state when time runs out
- Shows warning state when time is low (< 5 minutes)
- Pause/resume behavior if applicable
- Expiry notification triggers callback
- Handles edge cases (negative time, zero time, very large time)
- Test useExamTimer hook behavior THROUGH this component (no separate hook test file per CONTEXT.md decision)

**ServiceHealth (5-8 tests):**
- Displays health status indicator (connected, degraded, disconnected)
- Shows status text matching connection state
- Updates display when status changes
- Renders with appropriate ARIA attributes for status

**ReconnectBanner (5-8 tests):**
- Shows banner when connection lost
- Displays reconnect button
- Calls reconnect handler on button click
- Hides banner when connection restored
- Shows retry count or countdown if applicable

Testing rules: Same as Task 1 plus vi.useFakeTimers()/advanceTimersByTimeAsync() for ExamTimer.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx vitest run test/react/components/AskIris test/react/components/exercise test/react/components/ExamTimer test/react/components/ServiceHealth test/react/components/ReconnectBanner --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>6 functional component test files pass. ExamTimer tested with Worker mock and fake timers. All components verified for state display, user interactions, and edge cases.</done>
</task>

</tasks>

<verification>
- All 10 test files exist and pass: `cd iris-thaumantias && npx vitest run test/react/components/Button/IconButton test/react/components/TextInput test/react/components/Dropdown test/react/components/SideMenu test/react/components/AskIris test/react/components/exercise test/react/components/ExamTimer test/react/components/ServiceHealth test/react/components/ReconnectBanner --reporter=verbose`
- Each file has 8-15 tests with explicit assertions
- All interactions use userEvent, not fireEvent
- ExamTimer uses Worker mock and vi.useFakeTimers()
- No snapshots, no CSS class assertions, no data-testid additions
</verification>

<success_criteria>
10 interactive/functional shared components have comprehensive unit tests covering click handlers, keyboard navigation, focus management, disabled states, timer behavior, and Worker mocking patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/13-component-test-suite/13-02-SUMMARY.md`
</output>
