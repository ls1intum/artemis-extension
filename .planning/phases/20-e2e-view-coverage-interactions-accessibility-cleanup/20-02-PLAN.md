---
phase: 20-e2e-view-coverage-interactions-accessibility-cleanup
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - iris-thaumantias/test/e2e/ui/dashboard.ui.test.ts
  - iris-thaumantias/test/e2e/ui/course-list.ui.test.ts
  - iris-thaumantias/test/e2e/ui/course-detail.ui.test.ts
  - iris-thaumantias/test/e2e/ui/exercise-detail.ui.test.ts
autonomous: true
requirements: [E2EV-02, E2EV-03, E2EV-04, E2EV-05]

must_haves:
  truths:
    - "Dashboard smoke test opens Artemis view after login and asserts a heading element is visible"
    - "CourseList smoke test navigates from Dashboard and asserts course list content is visible"
    - "CourseDetail smoke test navigates to a course and asserts exercise list container is visible"
    - "ExerciseDetail smoke test navigates to an exercise and asserts participation section is visible"
    - "All 4 tests skip gracefully when ARTEMIS_USER/ARTEMIS_PASS env vars are missing"
  artifacts:
    - path: "iris-thaumantias/test/e2e/ui/dashboard.ui.test.ts"
      provides: "Dashboard view E2E smoke test"
      contains: "Dashboard"
    - path: "iris-thaumantias/test/e2e/ui/course-list.ui.test.ts"
      provides: "CourseList view E2E smoke test"
      contains: "CourseList"
    - path: "iris-thaumantias/test/e2e/ui/course-detail.ui.test.ts"
      provides: "CourseDetail view E2E smoke test"
      contains: "CourseDetail"
    - path: "iris-thaumantias/test/e2e/ui/exercise-detail.ui.test.ts"
      provides: "ExerciseDetail view E2E smoke test"
      contains: "ExerciseDetail"
  key_links:
    - from: "iris-thaumantias/test/e2e/ui/dashboard.ui.test.ts"
      to: "iris-thaumantias/test/e2e/ui/helpers.ts"
      via: "import { openArtemisView, switchToWebviewFrame, ... }"
      pattern: "from.*helpers"
---

<objective>
Create E2E smoke tests for Dashboard, CourseList, CourseDetail, and ExerciseDetail views. Each test logs in with credentials, navigates to the target view, and asserts the primary UI element is visible.

Purpose: These are the 4 core student-facing views. Proving they mount and render their containers in a real VS Code window verifies the extension host → webview → React pipeline is intact.
Output: 4 new .ui.test.ts files in test/e2e/ui/.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@iris-thaumantias/test/e2e/ui/helpers.ts
@iris-thaumantias/test/e2e/ui/login.ui.test.ts
@iris-thaumantias/test/e2e/ui/login-flow.ui.test.ts

<interfaces>
<!-- Helpers available for E2E tests -->
From iris-thaumantias/test/e2e/ui/helpers.ts:
```typescript
export async function openArtemisView(): Promise<SideBarView>;
export async function switchToWebviewFrame(driver: WebDriver): Promise<WebviewView>;
export async function switchBackFromWebview(driver: WebDriver): Promise<void>;
export async function waitForElement(driver: WebDriver, cssSelector: string, timeout?: number);
export function getCredentials(): { username: string; password: string };
export async function takeScreenshot(driver: WebDriver, name: string): Promise<string>;
```

<!-- Credential-gated test pattern from login-flow.ui.test.ts -->
```typescript
before(async function () {
    this.timeout(30000);
    try {
        ({ username, password } = getCredentials());
    } catch {
        this.skip();
    }
    driver = VSBrowser.instance.driver;
    await VSBrowser.instance.waitForWorkbench();
});
```

<!-- Navigation from Dashboard to sub-views uses XPath button text selectors -->
<!-- CSS module classes are hashed — use semantic selectors: id, aria-label, element type, XPath text -->

<!-- The 4 views and their primary selectors -->
View mapping:
- Dashboard (after login): assert `h1` or heading text
- CourseList: Dashboard → click button containing "Browse Courses" text → assert `h2` or list container
- CourseDetail: requires selecting a specific course (accept loading state)
- ExerciseDetail: requires selecting a specific exercise (accept loading state, assert container)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dashboard and CourseList smoke tests</name>
  <files>iris-thaumantias/test/e2e/ui/dashboard.ui.test.ts, iris-thaumantias/test/e2e/ui/course-list.ui.test.ts</files>
  <action>
**dashboard.ui.test.ts (E2EV-02):**
1. Follow the credential-gated pattern from `login-flow.ui.test.ts`:
   - In `before()`: call `getCredentials()` in try/catch, `this.skip()` on failure
   - Get driver from `VSBrowser.instance.driver`, wait for workbench
   - Open Artemis view, switch to webview frame, fill in login form (#username, #password), click submit button
   - Wait 5 seconds for auth + navigation
   - Switch back from webview after login
2. In `after()`: Execute `Logout from Artemis` command via Workbench, sleep 2s
3. In `afterEach()`: `switchBackFromWebview(driver)` in try/catch
4. Test `it('should render Dashboard with heading after login')`:
   - `this.timeout(20000)`
   - `openArtemisView()` → `switchToWebviewFrame(driver)`
   - Wait for a heading element: use `waitForElement(driver, 'h1', 10000)` or XPath `//h1` to find the Dashboard title
   - Accept any heading content — smoke test only proves the view mounted, not specific data
   - `assert.ok(heading, 'Dashboard heading should be visible after login')`
   - Take screenshot for debugging: `takeScreenshot(driver, 'dashboard-smoke')`

**course-list.ui.test.ts (E2EV-03):**
1. Same credential-gated `before()` as Dashboard test (login first)
2. Same `after()` logout and `afterEach()` frame switching
3. Test `it('should render CourseList after navigating from Dashboard')`:
   - `this.timeout(30000)`
   - `openArtemisView()` → `switchToWebviewFrame(driver)`
   - Navigate from Dashboard to CourseList by clicking the "Browse Courses" or "All Courses" button. Use XPath: `By.xpath("//button[.//span[contains(text(),'Courses')]] | //a[contains(text(),'Courses')]")` — match any button or link containing "Courses" text
   - If no "Courses" button found within 5s, accept the Dashboard itself as a pass (some server configs may not show this button) and log a warning
   - If navigation succeeds, wait for the CourseList content: `waitForElement(driver, 'h2', 10000)` or any list container
   - `assert.ok(element, 'CourseList content should be visible')`
   - `takeScreenshot(driver, 'course-list-smoke')`

**IMPORTANT: CSS module classes are hashed.** Do NOT use `.dashboard`, `.courseListContainer`, etc. Use only: element types (`h1`, `h2`, `form`), `id` attributes, `aria-label` attributes, `data-testid` attributes, or XPath text content selectors.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "error|\.ui\.test" | head -10</automated>
  </verify>
  <done>dashboard.ui.test.ts and course-list.ui.test.ts exist, compile without TypeScript errors, follow the credential-gated pattern, and use semantic selectors (not CSS module classes) for assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Create CourseDetail and ExerciseDetail smoke tests</name>
  <files>iris-thaumantias/test/e2e/ui/course-detail.ui.test.ts, iris-thaumantias/test/e2e/ui/exercise-detail.ui.test.ts</files>
  <action>
**course-detail.ui.test.ts (E2EV-04):**
1. Credential-gated pattern (same as Dashboard test)
2. Test `it('should render CourseDetail view or accept loading state')`:
   - `this.timeout(30000)`
   - After login, navigate from Dashboard. Try clicking a course card/link from the Dashboard "Recent Courses" section. Use XPath: `By.xpath("//button[contains(@class,'course')] | //div[contains(@class,'card')]//button | //a[.//span]")` — look for any clickable course element
   - If a course element is found and clicked, wait up to 10s for the CourseDetail view to load. Assert any container element exists (an `h2`, a list, a section).
   - If no course element is found (empty dashboard / no enrolled courses), skip gracefully with `this.skip()` and log "No courses available for CourseDetail smoke test"
   - Accept loading/empty states as valid — smoke test proves navigation and view mounting
   - `takeScreenshot(driver, 'course-detail-smoke')`

**exercise-detail.ui.test.ts (E2EV-05):**
1. Credential-gated pattern
2. Test `it('should render ExerciseDetail view or accept loading state')`:
   - `this.timeout(45000)` (longer timeout — 2 navigation steps)
   - After login, navigate Dashboard → Course → Exercise. This requires:
     a. Click a course from Dashboard
     b. In CourseDetail, click an exercise link
   - Use XPath text selectors to find clickable exercise items
   - If navigation to ExerciseDetail succeeds, assert `waitForElement(driver, '#participation-section', 10000)` OR any content container
   - If any navigation step fails (no courses, no exercises), skip gracefully
   - Accept loading/empty states
   - `takeScreenshot(driver, 'exercise-detail-smoke')`

**IMPORTANT:** Both tests may skip in CI environments without enrolled courses. This is expected — the tests verify the view can mount when the navigation path exists, and skip cleanly when it doesn't. The `this.skip()` pattern produces a "pending" result in Mocha, not a failure.

**IMPORTANT: CSS module classes are hashed.** Use semantic selectors only.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "error|\.ui\.test" | head -10</automated>
  </verify>
  <done>course-detail.ui.test.ts and exercise-detail.ui.test.ts exist, compile without TypeScript errors, follow the credential-gated + graceful-skip pattern, and accept loading/empty states as valid smoke test passes.</done>
</task>

</tasks>

<verification>
1. All 4 new test files exist in `iris-thaumantias/test/e2e/ui/`
2. `npx tsc --noEmit` passes with no errors in the test files
3. Each file imports from `./helpers` (openArtemisView, switchToWebviewFrame, getCredentials, etc.)
4. Each file uses the `this.skip()` pattern when credentials are missing
5. No CSS module class selectors used — only semantic selectors (id, aria-label, element type, XPath text)
6. Each file has `afterEach` with `switchBackFromWebview(driver)` in try/catch
</verification>

<success_criteria>
- 4 new E2E smoke test files created and compiling
- Dashboard test asserts heading visible after login
- CourseList test navigates from Dashboard and asserts course list content
- CourseDetail test navigates to a course (or skips if none available)
- ExerciseDetail test navigates to an exercise (or skips if none available)
- All tests use credential-gated pattern with graceful skip
</success_criteria>

<output>
After completion, create `.planning/phases/20-e2e-view-coverage-interactions-accessibility-cleanup/20-02-SUMMARY.md`
</output>
