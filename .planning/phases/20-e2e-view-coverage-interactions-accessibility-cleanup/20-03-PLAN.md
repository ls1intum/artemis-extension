---
phase: 20-e2e-view-coverage-interactions-accessibility-cleanup
plan: 03
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - iris-thaumantias/test/e2e/ui/exam-start.ui.test.ts
  - iris-thaumantias/test/e2e/ui/exam-conduction.ui.test.ts
  - iris-thaumantias/test/e2e/ui/exam-exercise-detail.ui.test.ts
  - iris-thaumantias/test/e2e/ui/iris-chat.ui.test.ts
  - iris-thaumantias/test/e2e/ui/service-status.ui.test.ts
  - iris-thaumantias/test/e2e/ui/git-credentials.ui.test.ts
  - iris-thaumantias/test/e2e/ui/recommended-extensions.ui.test.ts
autonomous: true
requirements: [E2EV-06, E2EV-07, E2EV-08, E2EV-09, E2EV-10, E2EV-11, E2EV-12]

must_haves:
  truths:
    - "ExamStart, ExamConduction, ExamExerciseDetail tests skip gracefully when no live exam is available"
    - "IrisChat smoke test opens the Iris Chat sidebar panel (separate from Artemis panel) and asserts chat input is visible"
    - "ServiceStatus smoke test navigates from Dashboard and asserts the server URL input field (#serverUrl) is visible"
    - "GitCredentials smoke test navigates from Dashboard and asserts git credentials form content is visible"
    - "RecommendedExtensions smoke test navigates from Dashboard and asserts extension list content is visible"
  artifacts:
    - path: "iris-thaumantias/test/e2e/ui/exam-start.ui.test.ts"
      provides: "ExamStart view E2E smoke test"
      contains: "ExamStart"
    - path: "iris-thaumantias/test/e2e/ui/exam-conduction.ui.test.ts"
      provides: "ExamConduction view E2E smoke test"
      contains: "ExamConduction"
    - path: "iris-thaumantias/test/e2e/ui/exam-exercise-detail.ui.test.ts"
      provides: "ExamExerciseDetail view E2E smoke test"
      contains: "ExamExerciseDetail"
    - path: "iris-thaumantias/test/e2e/ui/iris-chat.ui.test.ts"
      provides: "IrisChat view E2E smoke test"
      contains: "IrisChat"
    - path: "iris-thaumantias/test/e2e/ui/service-status.ui.test.ts"
      provides: "ServiceStatus view E2E smoke test"
      contains: "ServiceStatus"
    - path: "iris-thaumantias/test/e2e/ui/git-credentials.ui.test.ts"
      provides: "GitCredentials view E2E smoke test"
      contains: "GitCredentials"
    - path: "iris-thaumantias/test/e2e/ui/recommended-extensions.ui.test.ts"
      provides: "RecommendedExtensions view E2E smoke test"
      contains: "RecommendedExtensions"
  key_links:
    - from: "iris-thaumantias/test/e2e/ui/iris-chat.ui.test.ts"
      to: "ActivityBar.getViewControl('Chat')"
      via: "ActivityBar API to open Iris Chat panel"
      pattern: "getViewControl.*Chat"
    - from: "iris-thaumantias/test/e2e/ui/service-status.ui.test.ts"
      to: "Dashboard 'Service Status' button"
      via: "XPath text selector click"
      pattern: "Service Status"
---

<objective>
Create E2E smoke tests for the remaining 7 views: ExamStart, ExamConduction, ExamExerciseDetail, IrisChat, ServiceStatus, GitCredentials, and RecommendedExtensions.

Purpose: Complete E2E smoke test coverage for all 12 webview views. The exam views test graceful skip behavior (live exam dependency), IrisChat tests the separate sidebar panel, and the 3 utility views test Dashboard button navigation.
Output: 7 new .ui.test.ts files in test/e2e/ui/.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@iris-thaumantias/test/e2e/ui/helpers.ts
@iris-thaumantias/test/e2e/ui/login-flow.ui.test.ts

<interfaces>
From iris-thaumantias/test/e2e/ui/helpers.ts:
```typescript
export async function openArtemisView(): Promise<SideBarView>;
export async function switchToWebviewFrame(driver: WebDriver): Promise<WebviewView>;
export async function switchBackFromWebview(driver: WebDriver): Promise<void>;
export async function waitForElement(driver: WebDriver, cssSelector: string, timeout?: number);
export function getCredentials(): { username: string; password: string };
export async function takeScreenshot(driver: WebDriver, name: string): Promise<string>;
```

<!-- View mapping with navigation and selectors -->
| View | Navigation | Primary Selector |
|------|-----------|-----------------|
| ExamStart | CourseDetail → exam section (needs enrolled exam) | Any container element |
| ExamConduction | ExamStart → start (needs LIVE started exam) | Timer element |
| ExamExerciseDetail | ExamConduction → exercise (needs LIVE started exam) | Exercise content |
| IrisChat | ActivityBar → getViewControl('Chat') → openView() | `[aria-label="Chat input"]` or textarea |
| ServiceStatus | Dashboard → click "Service Status" button | `#serverUrl` input |
| GitCredentials | Dashboard → click "Git Credentials" button | Form or card element |
| RecommendedExtensions | Dashboard → click "Recommended Extensions" button | List or heading element |

<!-- IMPORTANT: Requirements E2EV-10 and E2EV-11 are remapped -->
E2EV-10 (originally "BuildFeedback") → maps to GitCredentials (10th actual view)
E2EV-11 (originally "ProblemStatement") → maps to RecommendedExtensions (11th actual view)
BuildFeedback and ProblemStatement are subcomponents of ExerciseDetail, not standalone views.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exam view smoke tests (ExamStart, ExamConduction, ExamExerciseDetail)</name>
  <files>iris-thaumantias/test/e2e/ui/exam-start.ui.test.ts, iris-thaumantias/test/e2e/ui/exam-conduction.ui.test.ts, iris-thaumantias/test/e2e/ui/exam-exercise-detail.ui.test.ts</files>
  <action>
All three exam view tests follow the same pattern: credential-gated + graceful skip when exam navigation fails.

**exam-start.ui.test.ts (E2EV-06):**
1. Credential-gated `before()` with login flow
2. `after()` with logout, `afterEach()` with switchBack
3. Test `it('should render ExamStart view or skip if no exam available')`:
   - `this.timeout(45000)`
   - After login, navigate Dashboard → Course → look for exam section
   - Try clicking a course from Dashboard, then look for an exam link/button in CourseDetail using XPath: `By.xpath("//*[contains(text(),'Exam') or contains(text(),'exam')]")`
   - If exam element found, click it and wait for ExamStart content (any container element)
   - If no exam found at any step, `this.skip()` with message "No exam available for ExamStart smoke test"
   - Accept loading state as valid
   - `takeScreenshot(driver, 'exam-start-smoke')`

**exam-conduction.ui.test.ts (E2EV-07):**
1. Same credential-gated pattern
2. Test `it('should render ExamConduction view or skip if no live exam')`:
   - `this.timeout(60000)` (needs 3+ navigation steps)
   - After login, attempt: Dashboard → Course → Exam → Start Exam
   - ExamConduction requires a LIVE started exam — this will almost always skip in automated CI
   - If somehow reached, assert a timer element or any content container exists
   - `this.skip()` at any failed navigation step
   - `takeScreenshot(driver, 'exam-conduction-smoke')`

**exam-exercise-detail.ui.test.ts (E2EV-08):**
1. Same credential-gated pattern
2. Test `it('should render ExamExerciseDetail view or skip if no live exam')`:
   - `this.timeout(60000)`
   - Same navigation as ExamConduction, plus selecting an exercise within the exam
   - Will almost always skip — requires ExamConduction to be reachable first
   - If reached, assert exercise content container exists
   - `this.skip()` at any failed step
   - `takeScreenshot(driver, 'exam-exercise-detail-smoke')`

**Pattern for all 3 files:** Each navigation step should be wrapped in try/catch. If any step throws (element not found, timeout), call `this.skip()` instead of failing. This produces Mocha "pending" status, not failure.

**IMPORTANT: CSS module classes are hashed.** Use semantic selectors only.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "error|exam" | head -10</automated>
  </verify>
  <done>3 exam smoke test files exist, compile without errors, and use the graceful skip pattern at every navigation step where exam data may not be available.</done>
</task>

<task type="auto">
  <name>Task 2: Create IrisChat, ServiceStatus, GitCredentials, RecommendedExtensions smoke tests</name>
  <files>iris-thaumantias/test/e2e/ui/iris-chat.ui.test.ts, iris-thaumantias/test/e2e/ui/service-status.ui.test.ts, iris-thaumantias/test/e2e/ui/git-credentials.ui.test.ts, iris-thaumantias/test/e2e/ui/recommended-extensions.ui.test.ts</files>
  <action>
**iris-chat.ui.test.ts (E2EV-09):**
IrisChat is a SEPARATE sidebar panel from Artemis — it has its own webview provider.
1. Credential-gated `before()` — IrisChat requires login to function
2. Test `it('should render IrisChat with chat input')`:
   - `this.timeout(30000)`
   - Open the Iris Chat panel via ActivityBar:
     ```typescript
     const activityBar = new ActivityBar();
     const control = await activityBar.getViewControl('Chat');
     ```
   - If `control` is null/undefined, try `getViewControl('Iris Chat')` as fallback (name may differ)
   - If neither found, skip: `this.skip()` with "Iris Chat panel not found in ActivityBar"
   - `await control.openView()`
   - Switch to webview frame: use `switchToWebviewFrame(driver)` — the WebviewView constructor should find the active webview
   - Assert chat input: `waitForElement(driver, '[aria-label="Chat input"], textarea', 10000)`
   - `assert.ok(chatInput, 'Chat input should be visible in IrisChat')`
   - `takeScreenshot(driver, 'iris-chat-smoke')`
3. Import `ActivityBar` from `vscode-extension-tester`

**service-status.ui.test.ts (E2EV-12):**
1. Credential-gated `before()` with login
2. Test `it('should render ServiceStatus with server URL input')`:
   - `this.timeout(30000)`
   - `openArtemisView()` → `switchToWebviewFrame(driver)`
   - Navigate from Dashboard by clicking "Service Status" button using XPath:
     `driver.wait(until.elementLocated(By.xpath("//button[.//span[contains(text(),'Service Status')]]")), 10000)`
   - If button not found (no Dashboard yet, or different layout), skip gracefully
   - Click button, wait for ServiceStatus view
   - Assert: `waitForElement(driver, '#serverUrl', 10000)` (known id attribute in ServiceStatusView)
   - `assert.ok(serverUrlInput, 'Server URL input should be visible')`
   - `takeScreenshot(driver, 'service-status-smoke')`

**git-credentials.ui.test.ts (E2EV-10, remapped from BuildFeedback):**
1. Credential-gated `before()` with login
2. Test `it('should render GitCredentials view')`:
   - `this.timeout(30000)`
   - `openArtemisView()` → `switchToWebviewFrame(driver)`
   - Navigate from Dashboard by clicking "Git Credentials" button via XPath:
     `By.xpath("//button[.//span[contains(text(),'Git')]]")`
   - If button not found, skip gracefully
   - Wait for GitCredentials content: form element or any input field
   - Accept loading/empty state
   - `takeScreenshot(driver, 'git-credentials-smoke')`

**recommended-extensions.ui.test.ts (E2EV-11, remapped from ProblemStatement):**
1. Credential-gated `before()` with login
2. Test `it('should render RecommendedExtensions view')`:
   - `this.timeout(30000)`
   - `openArtemisView()` → `switchToWebviewFrame(driver)`
   - Navigate from Dashboard by clicking "Recommended Extensions" button via XPath:
     `By.xpath("//button[.//span[contains(text(),'Extension')]]")`
   - If button not found, skip gracefully
   - Wait for any content container (heading, list element)
   - Accept loading/empty state
   - `takeScreenshot(driver, 'recommended-extensions-smoke')`

**All 4 files:** Import from `vscode-extension-tester` as needed. Use `afterEach()` with `switchBackFromWebview()` in try/catch. Use `after()` with logout command.

**IMPORTANT: CSS module classes are hashed.** Use semantic selectors only.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -E "error|\.ui\.test" | head -10</automated>
  </verify>
  <done>4 smoke test files exist, compile without errors. IrisChat uses ActivityBar to open separate panel. ServiceStatus asserts #serverUrl. GitCredentials and RecommendedExtensions navigate via Dashboard button XPath. All skip gracefully when navigation fails.</done>
</task>

</tasks>

<verification>
1. All 7 new test files exist in `iris-thaumantias/test/e2e/ui/`
2. `npx tsc --noEmit` passes with no errors
3. Each file imports from `./helpers`
4. Each file uses credential-gated `this.skip()` pattern
5. Exam tests skip at every navigation step where data may be unavailable
6. IrisChat uses ActivityBar API (not openArtemisView) to access separate panel
7. ServiceStatus asserts `#serverUrl` — a known id attribute
8. No CSS module class selectors used
9. Total view coverage: 12/12 (Login in existing test + 4 in Plan 02 + 7 in this plan)
</verification>

<success_criteria>
- 7 new E2E smoke test files created and compiling
- All 12 webview views now have E2E smoke test coverage
- Exam view tests gracefully skip when live exam is unavailable
- IrisChat test opens the separate Iris Chat sidebar panel
- Utility view tests (ServiceStatus, GitCredentials, RecommendedExtensions) navigate via Dashboard buttons
- All tests follow established credential-gated pattern
</success_criteria>

<output>
After completion, create `.planning/phases/20-e2e-view-coverage-interactions-accessibility-cleanup/20-03-SUMMARY.md`
</output>
