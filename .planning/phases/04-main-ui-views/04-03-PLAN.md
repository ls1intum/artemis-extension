---
phase: 04-main-ui-views
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - iris-thaumantias/src/views/webview/react/stores/useCourseDetailStore.ts
  - iris-thaumantias/src/shared/messageContracts.ts
  - iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.module.css
  - iris-thaumantias/src/views/webview/react/views/CourseDetail/types.ts
  - iris-thaumantias/src/views/webview/react/views/CourseDetail/index.ts
  - iris-thaumantias/src/views/webview/react/views/index.ts
  - iris-thaumantias/src/views/webview/react/App.tsx
  - iris-thaumantias/src/views/app/viewRouter.ts
  - iris-thaumantias/src/provider/artemisWebviewProvider.ts
autonomous: true
requirements: [VIEW-01]

must_haves:
  truths:
    - "CourseDetail view renders through React with course header, exams, and exercises"
    - "Exercise search filters exercises by title or type in real time"
    - "Exercise sort dropdown reorders exercises (by ID, title, due date, points)"
    - "Exercise categories always expanded (not collapsible) per user decision"
    - "Breadcrumbs show 'Dashboard / CourseName' navigation path"
    - "Clicking an exercise navigates to exercise detail via postMessage"
    - "Ask Iris section renders with course context"
    - "Skeleton loading shows during data fetch"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/stores/useCourseDetailStore.ts"
      provides: "Zustand store for course detail state"
    - path: "iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx"
      provides: "CourseDetail React view component"
  key_links:
    - from: "iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx"
      to: "iris-thaumantias/src/views/webview/react/stores/useCourseDetailStore.ts"
      via: "Zustand hook subscription"
      pattern: "useCourseDetailStore"
    - from: "iris-thaumantias/src/views/app/viewRouter.ts"
      to: "getReactWebviewHtml"
      via: "_reactViews map"
      pattern: "'course-detail', true"
---

<objective>
Migrate the CourseDetail view to React with Zustand store, exercise search/sort, exam listing, Ask Iris integration, and breadcrumb navigation.

Purpose: CourseDetail is the central hub between course list and exercise detail. Migration validates breadcrumb navigation chain (Dashboard > Courses > CourseName) and exercise list patterns.
Output: CourseDetail React view with Zustand store, registered in coexistence router.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-main-ui-views/04-CONTEXT.md
@.planning/phases/04-main-ui-views/04-RESEARCH.md
@.planning/phases/04-main-ui-views/04-01-SUMMARY.md
@.planning/phases/04-main-ui-views/04-02-SUMMARY.md

@iris-thaumantias/src/views/courseDetail/courseDetailView.ts
@iris-thaumantias/src/shared/messageContracts.ts
@iris-thaumantias/src/views/webview/react/App.tsx
@iris-thaumantias/src/views/webview/react/views/index.ts
@iris-thaumantias/src/views/webview/react/stores/useNavigationStore.ts
@iris-thaumantias/src/views/app/viewRouter.ts
@iris-thaumantias/src/views/app/appStateManager.ts
@iris-thaumantias/src/provider/artemisWebviewProvider.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CourseDetail message contracts and Zustand store</name>
  <files>
    iris-thaumantias/src/shared/messageContracts.ts
    iris-thaumantias/src/views/webview/react/stores/useCourseDetailStore.ts
    iris-thaumantias/src/views/webview/react/views/CourseDetail/types.ts
  </files>
  <action>
    **1. Add CourseDetail message contracts to `messageContracts.ts`:**
    Extension-to-webview:
    - `CourseDetailInitMessage { type: 'courseDetailInit'; payload: { courseData: CourseDetailData; workspaceExerciseId?: number | null } }` where `CourseDetailData` includes full course object with exercises, exams, description, stats.

    Webview-to-extension commands:
    - `ReloadCourseDetailCommand { type: 'command'; command: 'reloadCourseDetail'; payload: { courseId: number } }`
    - `OpenExerciseDetailsCommand { type: 'command'; command: 'openExerciseDetails'; payload: { exerciseId: number } }`
    - `OpenExamCommand { type: 'command'; command: 'openExam'; payload: { examId: number; courseId: number } }`
    - `AskIrisAboutCourseCommand { type: 'command'; command: 'askIrisAboutCourse'; payload: { courseId: number; courseTitle: string; courseShortName?: string } }`
    - `ToggleCourseFullscreenCommand { type: 'command'; command: 'toggleCourseFullscreen' }`
    - `OpenInEditorCommand { type: 'command'; command: 'openInEditor'; payload: { data: unknown } }`

    Add to union types and update type guard.

    **2. Create CourseDetail types (`views/CourseDetail/types.ts`):**
    - `CourseDetailViewProps { vscodeApi: VsCodeApi }`
    - `CourseDetailPersistedState { exerciseSearchTerm?: string; exerciseSortBy?: string }` (persist search/sort state)
    - Type definitions for exercise and exam data structures used by the view.

    **3. Create CourseDetail Zustand store (`stores/useCourseDetailStore.ts`):**
    Interface `CourseDetailState`:
    - `courseData: CourseDetailData | null`
    - `workspaceExerciseId: number | null`
    - `isLoading: boolean`
    - `error: string | null`
    - `exerciseSearchTerm: string`
    - `exerciseSortBy: string` (default 'id-desc')
    - Actions: `setCourseData(data, workspaceExerciseId?)`, `setLoading(loading)`, `setError(error)`, `setExerciseSearchTerm(term)`, `setExerciseSortBy(sort)`, `loadCourseDetail(vscodeApi, courseId?)`
    - Derived: `filteredExercises()` getter — filters by title/type (case-insensitive) then sorts (id-asc/desc, title-asc/desc, due-asc/desc, points-asc/desc). `sortedExams()` getter — sorts exams: active first, then upcoming, then finished (matching legacy sort logic).
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>CourseDetail message contracts added. Zustand store created with exercise search/sort logic and exam sorting. Types defined with persisted state.</done>
</task>

<task type="auto">
  <name>Task 2: CourseDetail React view with router wiring</name>
  <files>
    iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.module.css
    iris-thaumantias/src/views/webview/react/views/CourseDetail/index.ts
    iris-thaumantias/src/views/webview/react/views/index.ts
    iris-thaumantias/src/views/webview/react/App.tsx
    iris-thaumantias/src/views/app/viewRouter.ts
    iris-thaumantias/src/provider/artemisWebviewProvider.ts
  </files>
  <action>
    **1. Create CourseDetailView.tsx:**
    Match the legacy courseDetailView.ts layout:
    - **Back link + controls:** BackLink "Back to Course" (actually back to course list or dashboard — use `backToDashboard` command). Reload button, Fullscreen button, Settings button (Phase 2 IconButton presets).
    - **Course header container:** Course title (xlarge), semester badge, color indicator bar, description text, stats row (exercise count, student count, instructor group, course ID). Use Container component with header.
    - **Ask Iris section:** Use AskIris component from Phase 2. "Ask Iris about this course" with button that sends `askIrisAboutCourse` command with courseId, courseTitle, courseShortName.
    - **Exams section (if exams exist):** Container with "Exams" header + badge count. List of exam items using ListItem. Each shows exam title, start/end dates. Active exams highlighted (per legacy `highlighted` prop). Sort: active first, upcoming, finished. Clickable — sends `openExam` command. Collapsible container — collapsed by default unless active exam exists. (Note: collapsible is for exams section, NOT exercise categories per user decision.)
    - **Exercises section:** Container with "Exercises" header + badge count. Search input (TextInput, type search) + sort dropdown (Latest Added, Oldest Added, Title A-Z/Z-A, Due Date Earliest/Latest, Points Low-High/High-Low). Exercise list using ListItem: each shows exercise type icon, title, due date, points. Workspace exercise highlighted with "Open" label. Clickable — sends `openExerciseDetails` command. "No exercises found matching your search" message when filter matches nothing. Exercise categories ALWAYS expanded (no collapsible) per user decision.
    - **Developer tools (conditional):** Only show when `developerMode` is enabled. "Open Raw JSON" and "Copy Course Data" buttons. Read developer mode from init message or store. Use Button component.
    - **State management:** `useCourseDetailStore` for data. On mount, listen for `courseDetailInit`. Filter/sort via store selectors.
    - **State persistence:** Persist exerciseSearchTerm and exerciseSortBy. Restore on mount.
    - **Breadcrumbs:** Push breadcrumb trail: "Dashboard" (navigates to dashboard) > abbreviated course title (current page). Use `useNavigationStore`. Clear and rebuild on mount.
    - **Loading/error:** SkeletonList (count=5) during loading. ErrorMessage with retry on error. Empty state for no exercises.
    - **Hover states:** Subtle hover on exercise/exam items using `var(--vscode-list-hoverBackground)`.
    - **Legacy compatibility:** Handle both typed and legacy message formats.

    **2. Create CourseDetailView.module.css:**
    Match legacy course-detail.css layout:
    - `.backLinkContainer` flex row.
    - `.courseDetailContainer` with padding.
    - `.courseHeaderContainer` with color indicator, title area, stats.
    - `.courseColorIndicator` 4px-wide colored strip.
    - `.courseDescription` text with `color: var(--vscode-descriptionForeground)`.
    - `.courseStats` flex row wrap with stat items.
    - `.exerciseSearch` flex row with search input + sort dropdown.
    - `.exercisesList` flex column.
    - `.exerciseItem` with icon, title, info row (due date, points). Icon via emoji/text (matching legacy `IconDefinitions.getIcon`). Hover: `background: var(--vscode-list-hoverBackground)`.
    - `.examItem` with title, date range.
    - `.noExercisesFound` hidden by default, shown when filter returns empty.
    - `.actionButtons` flex row gap for developer tools.
    - `.irisSection` for Ask Iris area.

    **3. Register in router:**
    - `views/CourseDetail/index.ts`: Export CourseDetailView and types.
    - Update `views/index.ts`: Add CourseDetail exports.
    - Update `App.tsx`: Add `case 'courseDetail': return <CourseDetailView vscodeApi={vscodeApi} />;`
    - Update `viewRouter.ts`: Add `['course-detail', true]` to `_reactViews` map.
    - Update `artemisWebviewProvider.ts`: In ready-signal handler, when state is 'course-detail', send `courseDetailInit` with `_appStateManager.currentCourseData`. Include `workspaceExerciseId` if detected (can reuse workspace detection from legacy code — call `detectWorkspaceExercise` and include result). Bridge legacy messages for exams and exercises. Also handle `developerMode` config: read from `vscode.workspace.getConfiguration('artemis').get('developerMode', false)` and include `hideDeveloperTools: !developerMode` in init payload.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify CourseDetail renders through React with exercise search/sort working</manual>
  </verify>
  <done>CourseDetail renders through React with course header, exams (sorted by status), exercises (searchable/sortable), Ask Iris, developer tools. Breadcrumbs show navigation path. Registered in coexistence router.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run compile` succeeds
- CourseDetail view renders through React coexistence router
- Exercise search filters by title/type
- Exercise sort dropdown reorders correctly (8 sort options)
- Exams sorted: active > upcoming > finished
- Breadcrumbs show "Dashboard / CourseName"
- Ask Iris button sends course context
- Workspace exercise highlighted with "Open" label
- Exercise categories always expanded (no collapse UI)
</verification>

<success_criteria>
- CourseDetail React view matches legacy layout (header, exams, exercises, Ask Iris, dev tools)
- Zustand store manages course data, exercise search/sort, loading/error
- Exercise search/sort works identically to legacy JavaScript
- Exam sorting by status (active/upcoming/finished) correct
- Breadcrumb navigation trail displayed and clickable
- Exercise categories always expanded (not collapsible) per user decision
- Hover states on exercise/exam items
- Registered in coexistence router
</success_criteria>

<output>
After completion, create `.planning/phases/04-main-ui-views/04-03-SUMMARY.md`
</output>
