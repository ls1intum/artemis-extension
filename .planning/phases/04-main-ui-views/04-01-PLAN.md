---
phase: 04-main-ui-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - iris-thaumantias/package.json
  - iris-thaumantias/package-lock.json
  - iris-thaumantias/src/views/webview/react/components/Skeleton/Skeleton.tsx
  - iris-thaumantias/src/views/webview/react/components/Skeleton/Skeleton.module.css
  - iris-thaumantias/src/views/webview/react/components/Skeleton/SkeletonList.tsx
  - iris-thaumantias/src/views/webview/react/components/Skeleton/index.ts
  - iris-thaumantias/src/views/webview/react/components/Breadcrumbs/Breadcrumbs.tsx
  - iris-thaumantias/src/views/webview/react/components/Breadcrumbs/Breadcrumbs.module.css
  - iris-thaumantias/src/views/webview/react/components/Breadcrumbs/index.ts
  - iris-thaumantias/src/views/webview/react/components/ReconnectBanner/ReconnectBanner.tsx
  - iris-thaumantias/src/views/webview/react/components/ReconnectBanner/ReconnectBanner.module.css
  - iris-thaumantias/src/views/webview/react/components/ReconnectBanner/index.ts
  - iris-thaumantias/src/views/webview/react/components/ErrorMessage/ErrorMessage.tsx
  - iris-thaumantias/src/views/webview/react/components/ErrorMessage/ErrorMessage.module.css
  - iris-thaumantias/src/views/webview/react/components/ErrorMessage/index.ts
  - iris-thaumantias/src/views/webview/react/components/EmptyState/EmptyState.tsx
  - iris-thaumantias/src/views/webview/react/components/EmptyState/EmptyState.module.css
  - iris-thaumantias/src/views/webview/react/components/EmptyState/index.ts
  - iris-thaumantias/src/views/webview/react/components/index.ts
  - iris-thaumantias/src/views/webview/react/stores/useNavigationStore.ts
  - iris-thaumantias/src/views/webview/react/stores/useDashboardStore.ts
  - iris-thaumantias/src/shared/messageContracts.ts
  - iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx
  - iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.module.css
  - iris-thaumantias/src/views/webview/react/views/Dashboard/types.ts
  - iris-thaumantias/src/views/webview/react/views/Dashboard/index.ts
  - iris-thaumantias/src/views/webview/react/views/index.ts
  - iris-thaumantias/src/views/webview/react/App.tsx
  - iris-thaumantias/src/views/app/viewRouter.ts
  - iris-thaumantias/src/provider/artemisWebviewProvider.ts
autonomous: true
requirements: [VIEW-01, MSG-04]

must_haves:
  truths:
    - "Zustand is installed and importable in webview React code"
    - "Skeleton loading placeholders render with shimmer animation using VS Code theme variables"
    - "Breadcrumb navigation renders with clickable segments, sticky positioning, and horizontal scroll"
    - "Error message displays with inline Retry text link (not styled button)"
    - "Empty state displays with title, message, and optional action"
    - "Reconnecting banner displays as thin bar at top of webview"
    - "Dashboard view renders through React with recent courses tree, quick actions, and workspace exercise detection"
    - "Dashboard state managed through Zustand store with postMessage integration"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/components/Skeleton/Skeleton.tsx"
      provides: "Skeleton loading placeholder component"
    - path: "iris-thaumantias/src/views/webview/react/components/Breadcrumbs/Breadcrumbs.tsx"
      provides: "Breadcrumb navigation component"
    - path: "iris-thaumantias/src/views/webview/react/stores/useNavigationStore.ts"
      provides: "Zustand navigation store for breadcrumb trail"
    - path: "iris-thaumantias/src/views/webview/react/stores/useDashboardStore.ts"
      provides: "Zustand dashboard store for courses and loading state"
    - path: "iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx"
      provides: "Dashboard React view component"
  key_links:
    - from: "iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx"
      to: "iris-thaumantias/src/views/webview/react/stores/useDashboardStore.ts"
      via: "Zustand hook subscription"
      pattern: "useDashboardStore"
    - from: "iris-thaumantias/src/views/webview/react/App.tsx"
      to: "iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx"
      via: "switch case routing"
      pattern: "case 'dashboard'"
    - from: "iris-thaumantias/src/views/app/viewRouter.ts"
      to: "getReactWebviewHtml"
      via: "_reactViews map"
      pattern: "'dashboard', true"
---

<objective>
Install Zustand for webview state management, create shared UI primitives (Skeleton, Breadcrumbs, ReconnectBanner, ErrorMessage, EmptyState), establish navigation store, and migrate the Dashboard view to React.

Purpose: Foundation for all Phase 4 views — Zustand stores replace ad-hoc useState patterns, shared primitives provide consistent loading/error/navigation UX across all main views.
Output: Zustand installed, 5 shared components, navigation store, dashboard Zustand store, Dashboard React view rendering through coexistence router.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-main-ui-views/04-CONTEXT.md
@.planning/phases/04-main-ui-views/04-RESEARCH.md
@.planning/phases/03-simple-views-migration/03-01-SUMMARY.md

@iris-thaumantias/src/shared/messageContracts.ts
@iris-thaumantias/src/views/webview/react/App.tsx
@iris-thaumantias/src/views/webview/react/views/index.ts
@iris-thaumantias/src/views/webview/react/components/index.ts
@iris-thaumantias/src/views/app/viewRouter.ts
@iris-thaumantias/src/provider/artemisWebviewProvider.ts
@iris-thaumantias/src/views/dashboard/dashboardView.ts
@iris-thaumantias/src/views/app/appStateManager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Zustand and create shared UI primitives</name>
  <files>
    iris-thaumantias/package.json
    iris-thaumantias/package-lock.json
    iris-thaumantias/src/views/webview/react/components/Skeleton/Skeleton.tsx
    iris-thaumantias/src/views/webview/react/components/Skeleton/Skeleton.module.css
    iris-thaumantias/src/views/webview/react/components/Skeleton/SkeletonList.tsx
    iris-thaumantias/src/views/webview/react/components/Skeleton/index.ts
    iris-thaumantias/src/views/webview/react/components/Breadcrumbs/Breadcrumbs.tsx
    iris-thaumantias/src/views/webview/react/components/Breadcrumbs/Breadcrumbs.module.css
    iris-thaumantias/src/views/webview/react/components/Breadcrumbs/index.ts
    iris-thaumantias/src/views/webview/react/components/ReconnectBanner/ReconnectBanner.tsx
    iris-thaumantias/src/views/webview/react/components/ReconnectBanner/ReconnectBanner.module.css
    iris-thaumantias/src/views/webview/react/components/ReconnectBanner/index.ts
    iris-thaumantias/src/views/webview/react/components/ErrorMessage/ErrorMessage.tsx
    iris-thaumantias/src/views/webview/react/components/ErrorMessage/ErrorMessage.module.css
    iris-thaumantias/src/views/webview/react/components/ErrorMessage/index.ts
    iris-thaumantias/src/views/webview/react/components/EmptyState/EmptyState.tsx
    iris-thaumantias/src/views/webview/react/components/EmptyState/EmptyState.module.css
    iris-thaumantias/src/views/webview/react/components/EmptyState/index.ts
    iris-thaumantias/src/views/webview/react/components/index.ts
    iris-thaumantias/src/views/webview/react/stores/useNavigationStore.ts
  </files>
  <action>
    **1. Install Zustand:**
    Run `cd iris-thaumantias && npm install zustand` to add Zustand ^5.x as a dependency.

    **2. Create Skeleton component (`components/Skeleton/`):**
    - `Skeleton.tsx`: Accept props `{ width?: string, height?: string, variant?: 'text' | 'circular' | 'rectangular', className?: string }`. Render a `<div>` with `aria-busy="true"` and `aria-live="polite"`. Use CSS module classes.
    - `SkeletonList.tsx`: Accept `{ count?: number }` (default 5 per user decision: fixed count). Render `count` skeleton items, each with a circular skeleton (40x40) and two text skeletons (60%, 40% width).
    - `Skeleton.module.css`: Base `.skeleton` with `background: var(--vscode-editor-background)`, `border-radius: 4px`, `overflow: hidden`. Pseudo-element `::after` with shimmer animation: `linear-gradient(90deg, transparent, var(--vscode-widget-border), transparent)`, `animation: shimmer 1.5s infinite`. Variant classes: `.skeletonText` (default), `.skeletonCircular` (`border-radius: 50%`), `.skeletonRectangular` (`border-radius: 4px`). SkeletonList layout: `.skeletonList` flex column, `.skeletonListItem` flex row with gap, `.skeletonListContent` flex column.

    **3. Create Breadcrumbs component (`components/Breadcrumbs/`):**
    - `Breadcrumbs.tsx`: Accept `{ segments: BreadcrumbSegment[] }` where `BreadcrumbSegment = { label: string; onClick: () => void }`. Return `null` if segments empty. Render `<nav aria-label="Breadcrumb navigation">` with flex inner container. Non-last segments are `<button>` with onClick; last segment is `<span aria-current="page">`. Separator `/` between segments with `aria-hidden="true"`.
    - `Breadcrumbs.module.css`: `.breadcrumbs` sticky at top (`position: sticky; top: 0; z-index: 10`), `background: var(--vscode-editor-background)`, `border-bottom: 1px solid var(--vscode-widget-border)`, `padding: 8px 16px`. Inner container: flex, `overflow-x: auto`, `white-space: nowrap`, thin scrollbar (`scrollbar-width: thin`). Links: `color: var(--vscode-textLink-foreground)`, no background/border, `font-size: 13px`. Hover: `color: var(--vscode-textLink-activeForeground)`, `text-decoration: underline`. Current: `color: var(--vscode-foreground)`, `font-weight: 500`. Separator: `color: var(--vscode-descriptionForeground)`.

    **4. Create ReconnectBanner component (`components/ReconnectBanner/`):**
    - `ReconnectBanner.tsx`: Listen for `websocketDisconnected` and `websocketConnected` messages via `window.addEventListener('message', ...)`. Show thin banner on disconnect, dismiss 2s after reconnect. Render `null` when not visible. Text: "Reconnecting to Artemis...".
    - `ReconnectBanner.module.css`: `.reconnectBanner` sticky at top, `z-index: 100`, `background: var(--vscode-inputValidation-warningBackground)`, `color: var(--vscode-inputValidation-warningForeground)`, `border-bottom: 1px solid var(--vscode-inputValidation-warningBorder)`, `padding: 4px 16px`, `font-size: 12px`, `text-align: center`.

    **5. Create ErrorMessage component (`components/ErrorMessage/`):**
    - `ErrorMessage.tsx`: Accept `{ error: string; onRetry: () => void }`. Render error text and a "Retry" button styled as a text link (per user decision: inline retry text link, not styled button). The button element has `type="button"`.
    - `ErrorMessage.module.css`: `.errorContainer` with padding. `.errorMessage` with `color: var(--vscode-errorForeground)`. `.retryLink` styled as text link: `background: none`, `border: none`, `color: var(--vscode-textLink-foreground)`, `cursor: pointer`, `font-size: 13px`, `padding: 0`, `text-decoration: underline`. Hover: `color: var(--vscode-textLink-activeForeground)`.

    **6. Create EmptyState component (`components/EmptyState/`):**
    - `EmptyState.tsx`: Accept `{ title: string; message: string; actionLabel?: string; onAction?: () => void }`. Center-aligned text with optional action button using the existing Button component from Phase 2.
    - `EmptyState.module.css`: `.emptyState` flex column centered, padding `32px 16px`, `text-align: center`. Title: `color: var(--vscode-foreground)`, `font-size: 16px`. Message: `color: var(--vscode-descriptionForeground)`, `font-size: 13px`, max-width for readability. No emoji icons (per project guidelines: avoid emojis).

    **7. Update components barrel (`components/index.ts`):**
    Add exports for Skeleton, SkeletonList, Breadcrumbs, ReconnectBanner, ErrorMessage, EmptyState.

    **8. Create navigation store (`stores/useNavigationStore.ts`):**
    - Create `stores/` directory under `src/views/webview/react/`.
    - `useNavigationStore.ts`: Zustand store with state `{ breadcrumbs: BreadcrumbSegment[] }` and actions `pushBreadcrumb(label: string, view: string, navigateFn: () => void)`, `popToBreadcrumb(index: number)`, `clearBreadcrumbs()`. The `BreadcrumbSegment` type: `{ label: string; view: string; onClick: () => void }`. `pushBreadcrumb` appends to trail. `popToBreadcrumb` slices to `index + 1` and calls the target segment's onClick handler to navigate. Abbreviation: if label > 20 chars, truncate to 17 + '...' (per research recommendation). Export the store and the BreadcrumbSegment type.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify zustand appears in package.json dependencies, all component files created with proper CSS module imports</manual>
  </verify>
  <done>Zustand installed. Skeleton, Breadcrumbs, ReconnectBanner, ErrorMessage, EmptyState components exist with CSS modules. Navigation store exists with breadcrumb trail management. All exports in barrel file.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard React view with Zustand store and router wiring</name>
  <files>
    iris-thaumantias/src/views/webview/react/stores/useDashboardStore.ts
    iris-thaumantias/src/shared/messageContracts.ts
    iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx
    iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.module.css
    iris-thaumantias/src/views/webview/react/views/Dashboard/types.ts
    iris-thaumantias/src/views/webview/react/views/Dashboard/index.ts
    iris-thaumantias/src/views/webview/react/views/index.ts
    iris-thaumantias/src/views/webview/react/App.tsx
    iris-thaumantias/src/views/app/viewRouter.ts
    iris-thaumantias/src/provider/artemisWebviewProvider.ts
  </files>
  <action>
    **1. Create Dashboard Zustand store (`stores/useDashboardStore.ts`):**
    Interface `DashboardState`:
    - `recentCourses: RecentCourseNode[]` (sorted, limited to 3)
    - `workspaceExercise: { id: number; title: string } | null`
    - `isLoading: boolean`
    - `error: string | null`
    - Actions: `loadDashboard(vscodeApi)` (sets loading, posts `reloadDashboard` command), `setDashboardData(courses: RecentCourseNode[])`, `setWorkspaceExercise(exercise)`, `setError(error)`, `setLoading(loading)`

    Where `RecentCourseNode` matches the legacy interface: `{ courseData: { course: { id?, title, exercises? } }, exercises: Exercise[] }`.

    **2. Add Dashboard message contracts to `messageContracts.ts`:**
    Extension-to-webview messages:
    - `DashboardInitMessage { type: 'dashboardInit'; payload: { courses: RecentCourseNode[]; workspaceExercise?: { id: number; title: string } } }`
    - `WorkspaceExerciseDetectedMessage { type: 'workspaceExerciseDetected'; payload: { exerciseId: number; exerciseTitle: string } | null }`

    Webview-to-extension commands:
    - `ReloadDashboardCommand { type: 'command'; command: 'reloadDashboard' }`
    - `ShowAllCoursesCommand { type: 'command'; command: 'showAllCourses' }`
    - `ViewCourseDetailsCommand { type: 'command'; command: 'viewCourseDetails'; payload: { courseData: unknown } }`
    - `OpenExerciseCommand { type: 'command'; command: 'openExercise'; payload: { exerciseId: number; courseId?: number | null } }`
    - `DetectWorkspaceExerciseCommand { type: 'command'; command: 'detectWorkspaceExercise' }`
    - `ShowAiConfigCommand { type: 'command'; command: 'showAiConfig' }`
    - `ShowRecommendedExtensionsCommand { type: 'command'; command: 'showRecommendedExtensions' }`
    - `ShowServiceStatusCommand { type: 'command'; command: 'showServiceStatus' }`
    - `ShowGitCredentialsCommand { type: 'command'; command: 'showGitCredentials' }`
    - `ShowStruggleDetectionCommand { type: 'command'; command: 'showStruggleDetection' }`
    - `OpenBugReportCommand { type: 'command'; command: 'openBugReport' }`

    Add all to the respective union types. Update `isExtensionMessage` type guard to include new types.

    **3. Create Dashboard view types (`views/Dashboard/types.ts`):**
    - `DashboardViewProps { vscodeApi: VsCodeApi }`
    - `DashboardPersistedState` (empty or minimal — dashboard data is always re-fetched per user decision)
    - Re-export `RecentCourseNode`, `Exercise` types used by the component.

    **4. Create DashboardView.tsx (`views/Dashboard/DashboardView.tsx`):**
    Match the legacy dashboard layout from `dashboardView.ts`:
    - **Welcome header:** Artemis title + subtitle "Your programming learning companion". No logo image (React webview doesn't have access to media URIs easily — use text-only heading). Link "Artemis" text to `openWebsite` command.
    - **Workspace exercise section:** Hidden when no workspace exercise detected. Shows exercise name with arrow, clickable to navigate via `openExercise` command. Use ListItem component.
    - **Recent courses section:** Container with "Recent Courses" header, "Show All" link button, reload icon button. Tree structure: each course is expandable (first expanded by default), shows course title + exercise count. Children are recent exercises (up to 4 per course). Clicking course arrow navigates to course detail via `viewCourseDetails`. Clicking exercise navigates via `openExercise`. Use existing Container, ListItem, Button, IconButton components from Phase 2.
    - **Quick actions section:** "Tools & Settings" container with ghost-variant buttons: Browse Courses, Open Settings, AI Checker, Recommended Extensions, Open Artemis in browser, Struggle Detection, Service Status, Git Credentials, Bug Report, divider, Logout (danger style). Each button posts the corresponding command.
    - **State management:** Use `useDashboardStore` for data. On mount, send `reloadDashboard` command. Listen for `dashboardInit` message to populate store. Listen for `workspaceExerciseDetected` for workspace exercise.
    - **Loading/error:** Use Skeleton component (SkeletonList count=3) when loading. ErrorMessage with retry on error. No breadcrumbs on Dashboard (per user decision: hidden on root level).
    - **Legacy message compatibility:** Handle both typed format `{ type: 'dashboardInit', ... }` and legacy format `{ command: 'workspaceExerciseDetected', ... }` per established Phase 3 pattern.
    - **Hover states:** Subtle hover on clickable course/exercise items using `var(--vscode-list-hoverBackground)`.

    **5. Create DashboardView.module.css:**
    Match legacy dashboard.css layout. Key styles:
    - `.dashboard` container with padding.
    - `.dashboardTitle` with flex for logo area and text.
    - `.recentCoursesTree` tree structure with expand/collapse.
    - `.courseNode` with `.isExpanded` toggle class.
    - `.courseExpandIcon` rotates on expand (CSS transform).
    - `.courseArrow` floats right, hover effect.
    - `.actionMenu` flex column of ghost buttons.
    - `.actionMenuDivider` thin line.
    - `.workspaceExercise` section with highlight background.
    All colors via VS Code CSS variables.

    **6. Create barrel and register in router:**
    - `views/Dashboard/index.ts`: Export DashboardView, DashboardViewProps, DashboardPersistedState.
    - Update `views/index.ts`: Add Dashboard exports.
    - Update `App.tsx`: Add `case 'dashboard': return <DashboardView vscodeApi={vscodeApi} />;` Import DashboardView.
    - Update `viewRouter.ts`: Add `['dashboard', true]` to `_reactViews` map.
    - Update `artemisWebviewProvider.ts`: In the ready-signal handler, when current state is 'dashboard', send `dashboardInit` message with courses data from `_appStateManager.coursesData` and workspace exercise if available. Bridge the existing `workspaceExerciseDetected` message to the new typed format. Also ensure the `_postMessageSafe` queuing works for dashboard init messages (same pattern as login's `setServerUrl`).
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify dashboard renders through React in the extension</manual>
  </verify>
  <done>Dashboard renders through React with Zustand store. Recent courses tree with expandable nodes, workspace exercise detection, quick action buttons all functional. Skeleton loading on initial load. Router and provider wired for dashboard state.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run compile` succeeds (esbuild bundles webview)
- Dashboard view renders through React coexistence router
- Zustand store manages dashboard state (loading, courses, error)
- Skeleton placeholders show during loading
- Breadcrumbs component exists (not used by Dashboard, ready for CourseList+)
- All shared primitives render with VS Code theme variables
</verification>

<success_criteria>
- Zustand is installed and usable in webview code
- 5 shared UI primitives (Skeleton, Breadcrumbs, ReconnectBanner, ErrorMessage, EmptyState) exist with CSS modules
- Navigation store manages breadcrumb trail
- Dashboard Zustand store manages courses/loading/error state
- Dashboard React view matches legacy layout (welcome header, workspace exercise, recent courses tree, quick actions)
- Dashboard registered in coexistence router and renders through React
- TypeScript compiles with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-main-ui-views/04-01-SUMMARY.md`
</output>
