---
phase: 04-main-ui-views
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts
  - iris-thaumantias/src/views/webview/react/hooks/useWebSocketUpdates.ts
  - iris-thaumantias/src/shared/messageContracts.ts
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.module.css
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/types.ts
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/index.ts
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.module.css
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ScoreInfo.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ScoreInfo.module.css
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/TestResults.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/TestResults.module.css
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/index.ts
  - iris-thaumantias/src/views/webview/react/views/index.ts
  - iris-thaumantias/src/views/webview/react/App.tsx
  - iris-thaumantias/src/views/app/viewRouter.ts
  - iris-thaumantias/src/provider/artemisWebviewProvider.ts
autonomous: true
requirements: [VIEW-01, MSG-04]

must_haves:
  truths:
    - "ExerciseDetail view renders through React with exercise card, submission status, participation actions, problem statement, and Ask Iris"
    - "ExerciseStarted state (exercise with active participation) renders within ExerciseDetailView showing clone/submit/upload actions, submission status, and build progress via ParticipationActions component"
    - "Real-time WebSocket updates (build status, submission results) update in place without full page reload"
    - "ExerciseDetail components (ProblemStatement, ScoreInfo, TestResults) are extracted as composable primitives for Phase 5 reuse"
    - "Breadcrumbs show 'Dashboard / CourseName / ExerciseName' navigation path"
    - "Build progress indicator matches current behavior for in-progress builds"
    - "Skeleton loading shows during data fetch"
    - "Subtle reconnecting banner appears when WebSocket disconnects"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts"
      provides: "Zustand store for exercise detail state with WebSocket update actions"
    - path: "iris-thaumantias/src/views/webview/react/hooks/useWebSocketUpdates.ts"
      provides: "RAF-batched WebSocket update hook"
    - path: "iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx"
      provides: "ExerciseDetail React view component"
    - path: "iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.tsx"
      provides: "Extracted ProblemStatement component for Phase 5 reuse"
    - path: "iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ScoreInfo.tsx"
      provides: "Extracted ScoreInfo component for Phase 5 reuse"
    - path: "iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/TestResults.tsx"
      provides: "Extracted TestResults component for Phase 5 reuse"
  key_links:
    - from: "iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx"
      to: "iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts"
      via: "Zustand hook subscription"
      pattern: "useExerciseDetailStore"
    - from: "iris-thaumantias/src/views/webview/react/hooks/useWebSocketUpdates.ts"
      to: "iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts"
      via: "RAF batched store updates"
      pattern: "requestAnimationFrame"
    - from: "iris-thaumantias/src/views/app/viewRouter.ts"
      to: "getReactWebviewHtml"
      via: "_reactViews map"
      pattern: "'exercise-detail', true"
---

<objective>
Migrate the ExerciseDetail view to React with extracted composable components, Zustand store, RAF-batched WebSocket real-time updates, and breadcrumb navigation. This covers both the ExerciseDetail view AND the ExerciseStarted state (exercise with active participation), which is rendered within ExerciseDetailView via the ParticipationActions component -- there is no separate ExerciseStartedView in the codebase.

Purpose: ExerciseDetail is the most complex view, receiving real-time build status and submission result updates. The view handles dual states: pre-participation (start/practice buttons) and post-participation aka "ExerciseStarted" (clone/submit/upload actions, build progress, submission results). Extracted components (ProblemStatement, ScoreInfo, TestResults) enable clean reuse in Phase 5's ExamExerciseDetail. This plan validates the WebSocket integration pattern used across all future real-time views.
Output: ExerciseDetail React view (covering both ExerciseDetail and ExerciseStarted states) with 3 extracted components, Zustand store, WebSocket hook, registered in coexistence router.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-main-ui-views/04-CONTEXT.md
@.planning/phases/04-main-ui-views/04-RESEARCH.md
@.planning/phases/04-main-ui-views/04-01-SUMMARY.md
@.planning/phases/04-main-ui-views/04-03-SUMMARY.md

@iris-thaumantias/src/views/exerciseDetail/exerciseDetailView.ts
@iris-thaumantias/src/views/exerciseDetail/components/submissionStatusComponent.ts
@iris-thaumantias/src/views/exerciseDetail/components/participationActionsComponent.ts
@iris-thaumantias/src/views/exerciseDetail/components/buildProgressComponent.ts
@iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.tsx
@iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx
@iris-thaumantias/src/views/webview/react/components/exercise/BuildProgress.tsx
@iris-thaumantias/src/shared/messageContracts.ts
@iris-thaumantias/src/views/webview/react/App.tsx
@iris-thaumantias/src/views/webview/react/views/index.ts
@iris-thaumantias/src/views/webview/react/stores/useNavigationStore.ts
@iris-thaumantias/src/views/app/viewRouter.ts
@iris-thaumantias/src/views/app/appStateManager.ts
@iris-thaumantias/src/provider/artemisWebviewProvider.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExerciseDetail message contracts, Zustand store, and WebSocket hook</name>
  <files>
    iris-thaumantias/src/shared/messageContracts.ts
    iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts
    iris-thaumantias/src/views/webview/react/hooks/useWebSocketUpdates.ts
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/types.ts
  </files>
  <action>
    **1. Add ExerciseDetail message contracts to `messageContracts.ts`:**
    Extension-to-webview:
    - `ExerciseDetailInitMessage { type: 'exerciseDetailInit'; payload: { exerciseData: unknown; hideDeveloperTools: boolean } }` — the exerciseData matches the structure from `appStateManager.currentExerciseData` (includes exercise object, pendingSubmission, etc.)
    - `WebSocketUpdateMessage { type: 'websocketUpdate'; payload: { updateType: 'newResult' | 'newSubmission' | 'submissionProcessing'; data: unknown } }` — forwarded from the extension's WebSocket handler.
    - `WebSocketDisconnectedMessage { type: 'websocketDisconnected' }`
    - `WebSocketConnectedMessage { type: 'websocketConnected' }`

    Webview-to-extension commands:
    - `ReloadExerciseDetailCommand { type: 'command'; command: 'reloadExerciseDetail'; payload: { exerciseId: number } }`
    - `BackToCourseDetailsCommand { type: 'command'; command: 'backToCourseDetails' }`
    - `CloneRepositoryCommand { type: 'command'; command: 'cloneRepository'; payload: { participationId: number; repositoryUri: string; exerciseTitle: string } }`
    - `OpenRepositoryCommand { type: 'command'; command: 'openRepository'; payload: { repositoryUri?: string } }`
    - `SubmitExerciseCommand { type: 'command'; command: 'submitExercise'; payload: { participationId: number } }`
    - `TriggerBuildCommand { type: 'command'; command: 'triggerBuild'; payload: { participationId: number } }`
    - `UploadSubmissionCommand { type: 'command'; command: 'uploadSubmission'; payload: { exerciseId: number } }`
    - `StartExerciseCommand { type: 'command'; command: 'startExercise'; payload: { exerciseId: number } }`
    - `StartPracticeCommand { type: 'command'; command: 'startPractice'; payload: { exerciseId: number } }`
    - `AskIrisAboutExerciseCommand { type: 'command'; command: 'askIrisAboutExercise'; payload: { exerciseId: number; exerciseTitle: string; exerciseShortName?: string; courseId?: number; courseTitle?: string; courseShortName?: string } }`
    - `ToggleExerciseFullscreenCommand { type: 'command'; command: 'toggleExerciseFullscreen' }`
    - `DownloadFileCommand { type: 'command'; command: 'downloadFile'; payload: { url: string; filename: string } }`
    - `CheckRepositoryStatusCommand { type: 'command'; command: 'checkRepositoryStatus'; payload: { showNotification?: boolean } }`

    Add to union types and update type guard.

    **2. Create ExerciseDetail types (`views/ExerciseDetail/types.ts`):**
    - `ExerciseDetailViewProps { vscodeApi: VsCodeApi }`
    - Type definitions for transformed exercise data (exercise, participation, submission, result, build status).
    - Extracted component prop types: `ProblemStatementProps { markdown: string; downloadLinks?: { name: string; url: string }[]; onDownload?: (url: string, name: string) => void }`, `ScoreInfoProps { score: number | null; maxScore: number; bonusPoints?: number; assessmentType?: string; completionDate?: string }`, `TestResultsProps { testCases: TestCase[]; }` where `TestCase = { name: string; passed: boolean; message?: string }`.

    **3. Create ExerciseDetail Zustand store (`stores/useExerciseDetailStore.ts`):**
    Interface `ExerciseDetailState`:
    - `exerciseData: unknown | null` (full exercise response from extension)
    - `hideDeveloperTools: boolean`
    - `isLoading: boolean`
    - `error: string | null`
    - Actions: `setExerciseData(data, hideDeveloperTools)`, `setLoading(loading)`, `setError(error)`, `loadExerciseDetail(vscodeApi, exerciseId)`, `updateBuildStatus(payload)`, `updateSubmission(payload)`, `updateSubmissionProcessing(payload)`
    - The `updateBuildStatus`, `updateSubmission`, `updateSubmissionProcessing` actions merge incoming WebSocket data into the existing exerciseData structure (update participations, submissions, results in-place). Follow the same logic as the legacy `resolveParticipationForResult`, `getLatestSubmission`, `getLatestResult` functions in exerciseDetailView.ts.

    **4. Create WebSocket updates hook (`hooks/useWebSocketUpdates.ts`):**
    - Implement RAF batching pattern from research. Use `useRef` to buffer incoming `websocketUpdate` messages. `requestAnimationFrame` loop flushes buffer to store once per frame.
    - Listen for `window.addEventListener('message', ...)` filtering for `type === 'websocketUpdate'` messages.
    - Call appropriate store actions: `updateBuildStatus` for `newResult`, `updateSubmission` for `newSubmission`, `updateSubmissionProcessing` for `submissionProcessing`.
    - Cleanup: remove listener and cancel RAF on unmount.
    - Export as `useWebSocketUpdates(vscodeApi: VsCodeApi)` hook. The vscodeApi param is for future use (not needed for listening, but keeps consistent interface).
    - Create `hooks/` directory under `src/views/webview/react/`.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ExerciseDetail message contracts added. Zustand store created with WebSocket update actions. RAF-batched WebSocket hook created. Types defined for extracted components.</done>
</task>

<task type="auto">
  <name>Task 2: ExerciseDetail React view with extracted components and router wiring</name>
  <files>
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.module.css
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.module.css
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ScoreInfo.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ScoreInfo.module.css
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/TestResults.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/TestResults.module.css
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/index.ts
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/index.ts
    iris-thaumantias/src/views/webview/react/views/index.ts
    iris-thaumantias/src/views/webview/react/App.tsx
    iris-thaumantias/src/views/app/viewRouter.ts
    iris-thaumantias/src/provider/artemisWebviewProvider.ts
  </files>
  <action>
    **1. Create extracted components (`views/ExerciseDetail/components/`):**
    These are composable primitives with typed props (not domain model imports) for Phase 5 reuse.

    **ProblemStatement.tsx:**
    - Accept `ProblemStatementProps`. Render markdown HTML via `dangerouslySetInnerHTML` (markdown already processed to HTML by extension-side `processMarkdown`). Show download links as buttons if provided. Each download button calls `onDownload(url, name)`.
    - Use Container component with "Exercise Description" header.
    - `ProblemStatement.module.css`: `.problemStatement` with max-width prose styling. Download section below content.

    **ScoreInfo.tsx:**
    - Accept `ScoreInfoProps`. Render score display: `score / maxScore` with optional bonus points. Show assessment type and completion date if available.
    - `ScoreInfo.module.css`: Score value styling with large number, slash, max. Color based on score percentage (green > 80%, yellow > 50%, red < 50%) using VS Code variables.

    **TestResults.tsx:**
    - Accept `TestResultsProps`. Render list of test cases: each shows name, pass/fail icon (checkmark/cross), optional failure message. Group by passed/failed.
    - `TestResults.module.css`: Test case list with pass (green) / fail (red) indicators.

    **components/index.ts:** Export ProblemStatement, ScoreInfo, TestResults and their prop types.

    **2. Create ExerciseDetailView.tsx:**
    Match the legacy exerciseDetailView.ts layout (this is the largest view):
    - **Back link + controls:** BackLink "Back to Course" (sends `backToCourseDetails`). Reload, Fullscreen, Settings buttons.
    - **Exercise card (collapsible details):** Use `<details>` HTML element (matching legacy). Summary shows: exercise title, type icon, points badge (with bonus if applicable), time remaining badge (due soon in different color), repository status button. Expanded content shows: release date, mode, grading, course info with semester badge, file formats.
    - **Participation section (covers ExerciseStarted state):** Use the Phase 2 SubmissionStatus and ParticipationActions components from `components/exercise/`. These are already React components created in Phase 2. Pass appropriate props derived from the exerciseData in the store. The SubmissionStatus component handles displaying latest result, score, and test case summaries. ParticipationActions handles both pre-participation state (start/practice buttons) and the "ExerciseStarted" state (clone/submit/upload actions when `hasParticipation` is true). The ExerciseStarted layout is NOT a separate view -- it is ExerciseDetailView rendering with active participation data, showing repository clone/open buttons, submission controls, and build/result status.
    - **Build progress:** Use Phase 2 BuildProgress component. Show when a build is in progress (pending submission exists). Match current build progress indicator per user decision.
    - **Ask Iris section:** Use AskIris component. "Ask Iris about this exercise" with button sending `askIrisAboutExercise`.
    - **Problem statement:** Use extracted ProblemStatement component. Pass processed markdown HTML and download links from exerciseData.
    - **Developer tools (conditional):** "Open Raw JSON", "Copy Exercise Data", "Submission Details" buttons when developer mode enabled.
    - **WebSocket updates:** Call `useWebSocketUpdates(vscodeApi)` hook. Real-time updates flow through RAF buffer to store, components re-render via Zustand subscriptions. Build status changes update silently in place (no animation/notification per user decision). Submission results update relevant section in place.
    - **ReconnectBanner:** Render `<ReconnectBanner />` at top of view (above breadcrumbs if present). Shows "Reconnecting to Artemis..." when WebSocket disconnects.
    - **Breadcrumbs:** Push breadcrumb trail: "Dashboard" > abbreviated course name > exercise title (current page). Use `useNavigationStore`.
    - **Loading/error:** SkeletonList on loading. ErrorMessage with retry. Auto-retry once on error after 2s delay (use `setTimeout` in the error handler: on first error set `autoRetried` flag, retry once, if still fails show error with manual retry).
    - **Data transformation:** Reuse the `transformExerciseData` utility from `views/utils` if it's importable in browser context. If not (because it may reference Node.js APIs), replicate the needed transformation logic in the view component. The transformation extracts: exerciseTitle, maxPoints, bonusPoints, timeRemaining, releaseDate, dueDate, mode, includedInScore, hasParticipation, participationId, isProgrammingExercise, filePattern, etc.
    - **Legacy compatibility:** Handle both typed and legacy message formats.

    **3. Create ExerciseDetailView.module.css:**
    Match legacy exercise-detail.css (this is the largest CSS):
    - `.backLinkContainer` flex row.
    - `.exerciseCard` collapsible details with summary styling.
    - `.summaryContent` flex row with text, meta badges, toggle icon.
    - `.exerciseMeta` flex row with icon/badges.
    - `.expandedContent` grid for info items.
    - `.infoGrid` 2-column grid with label/value pairs.
    - `.participationSection` for actions and status.
    - `.irisAssistSection` for Ask Iris.
    - All colors via VS Code CSS variables.
    - `.repoStatusIcon` circular button with status colors.

    **4. Register in router:**
    - `views/ExerciseDetail/index.ts`: Export ExerciseDetailView, types, and extracted component exports.
    - Update `views/index.ts`: Add ExerciseDetail exports.
    - Update `App.tsx`: Add `case 'exerciseDetail': return <ExerciseDetailView vscodeApi={vscodeApi} />;`
    - Update `viewRouter.ts`: Add `['exercise-detail', true]` to `_reactViews` map.
    - Update `artemisWebviewProvider.ts`:
      - In ready-signal handler, when state is 'exercise-detail', send `exerciseDetailInit` with `_appStateManager.currentExerciseData` and `hideDeveloperTools`.
      - Forward WebSocket events as `websocketUpdate` messages: In the existing WebSocket handler methods (`_handleNewResult`, `_handleNewSubmission`, `_handleSubmissionProcessing`), add `_postMessageSafe({ type: 'websocketUpdate', payload: { updateType: 'newResult', data: result } })` (and similarly for submissions and processing). This is in addition to the existing legacy handling.
      - Forward WebSocket connection state changes: When `ArtemisWebsocketService` connects/disconnects, send `websocketConnected` and `websocketDisconnected` messages. Hook into the existing connection state monitoring.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify ExerciseDetail renders through React with real-time WebSocket updates working</manual>
  </verify>
  <done>ExerciseDetail renders through React with exercise card, participation actions, submission status, build progress, problem statement, Ask Iris, developer tools. ExerciseStarted state (active participation with clone/submit/upload actions) renders correctly within ExerciseDetailView. Real-time WebSocket updates via RAF batching update UI silently in place. Extracted ProblemStatement, ScoreInfo, TestResults components ready for Phase 5 reuse. ReconnectBanner shows on WebSocket disconnect. Breadcrumbs show full navigation path. Registered in coexistence router.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm run compile` succeeds
- ExerciseDetail view renders through React coexistence router
- Exercise card details expand/collapse
- Phase 2 SubmissionStatus, ParticipationActions, BuildProgress components render correctly
- ExerciseStarted state (exercise with active participation) renders within ExerciseDetailView showing clone/submit/upload actions, submission status, and build progress
- Real-time WebSocket updates (build status, submission results) update in-place
- ProblemStatement, ScoreInfo, TestResults are extracted as separate files with typed props
- Breadcrumbs show "Dashboard / CourseName / ExerciseName"
- ReconnectBanner appears on WebSocket disconnect, dismisses on reconnect
- Auto-retry once on error, then manual retry
- RAF batching prevents re-render storms from high-frequency WebSocket messages
</verification>

<success_criteria>
- ExerciseDetail React view matches legacy layout (exercise card, participation, submission, build, problem statement, Iris, dev tools)
- ExerciseStarted state (active participation) renders correctly within ExerciseDetailView -- clone/submit/upload actions, build progress, submission results all visible when hasParticipation is true
- Zustand store manages exercise data with WebSocket update merge logic
- RAF-batched WebSocket hook prevents re-render storms (max 60 updates/sec)
- 3 extracted components (ProblemStatement, ScoreInfo, TestResults) accept typed props for Phase 5 reuse
- Build status changes update silently in place (no animation/notification)
- Submission results update relevant section in place
- ReconnectBanner shows on WebSocket disconnect
- Breadcrumb navigation trail complete (Dashboard > Course > Exercise)
- Auto-retry once on error
- Registered in coexistence router
</success_criteria>

<output>
After completion, create `.planning/phases/04-main-ui-views/04-04-SUMMARY.md`
</output>
