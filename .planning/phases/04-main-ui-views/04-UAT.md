---
status: resolved
phase: 04-main-ui-views
source: [04-01-SUMMARY.md, 04-02-SUMMARY.md, 04-03-SUMMARY.md, 04-04-SUMMARY.md]
started: 2026-02-24T12:00:00Z
updated: 2026-02-24T13:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Dashboard View Renders
expected: Opening the Artemis sidebar shows the Dashboard with: "Artemis" header link, workspace exercise section (if an exercise is open), recent courses tree with expandable nodes (first expanded by default showing up to 4 exercises per course), and quick actions section. Skeleton placeholders appear briefly while data loads.
result: issue
reported: "alles flackert und nichts ist korrekt geladen!"
severity: blocker

### 2. CourseList View and Display
expected: Navigating to the course list (via quick action or breadcrumb) shows course cards with color indicator, title, semester badge, description, and stats. A search input, type filter dropdown (all/active/archived), semester filter dropdown, and sort dropdown are visible above the list. Breadcrumb shows "Courses".
result: issue
reported: "ich kann generell nichts anklicken weil irgendwie das styling komplett weg ist"
severity: blocker

### 3. CourseList Search and Filtering
expected: Typing in the search field filters courses by title, semester, or description (case-insensitive). Selecting a type filter shows only active or archived courses. Selecting a semester filters to that semester. Sort dropdown reorders by title, semester, or exercise count. "Clear Filters" button resets all filters when active. Filter state persists if you switch tabs and come back.
result: skipped
reason: Blocked by styling/rendering issues in Tests 1-2

### 4. Load Archived Courses
expected: A "Load Archived Courses" button appears below active courses. Clicking it loads archived courses into a separate "Archived" section below. The button disappears after loading.
result: skipped
reason: Blocked by styling/rendering issues in Tests 1-2

### 5. CourseDetail View
expected: Clicking a course navigates to CourseDetail showing: course header with title and semester, exams section (if any, sorted by status: active first, upcoming second, finished last), exercise categories (always expanded), Ask Iris button, and developer tools (if developer mode is on). Breadcrumb shows "Dashboard > [course name]".
result: skipped
reason: Blocked by styling/rendering issues in Tests 1-2

### 6. CourseDetail Exercise Search and Sort
expected: A search field filters exercises by title or type (case-insensitive). A sort dropdown offers 8 options: ID (latest/oldest), Title (A-Z/Z-A), Due Date (earliest/latest), Points (low-high/high-low). Workspace exercise (if detected) is highlighted with an "Open" badge.
result: skipped
reason: Blocked by styling/rendering issues in Tests 1-2

### 7. ExerciseDetail View
expected: Clicking an exercise navigates to ExerciseDetail. A collapsible exercise card shows title, type icon, points (with bonus if applicable), and time remaining (color-coded if due soon). Expanding the card reveals release date, mode, grading info, course with semester badge, and file formats. Breadcrumb shows "Dashboard > [course name] > [exercise name]".
result: skipped
reason: Blocked by styling/rendering issues in Tests 1-2

### 8. ExerciseDetail Participation and Actions
expected: Below the exercise card, a participation section shows: Start/Practice buttons if you haven't participated yet, OR clone/submit/upload actions with submission status and build progress if you have an active participation. Problem statement with markdown content is displayed. Score info shows if graded.
result: skipped
reason: Blocked by styling/rendering issues in Tests 1-2

### 9. Breadcrumb Navigation Flow
expected: Navigating Dashboard > CourseList > CourseDetail > ExerciseDetail builds a breadcrumb trail. Clicking any breadcrumb navigates back to that view. Breadcrumbs truncate long names (17 chars + "..."). The breadcrumb bar is sticky at the top.
result: skipped
reason: Blocked by styling/rendering issues in Tests 1-2

### 10. WebSocket Real-time Updates
expected: While on ExerciseDetail with an active submission, build status and submission results update in real-time without refreshing. If the WebSocket disconnects, a "Reconnecting to Artemis..." banner appears. It dismisses when connection is restored.
result: skipped
reason: Blocked by styling/rendering issues in Tests 1-2

## Summary

total: 10
passed: 0
issues: 2
pending: 0
skipped: 8

## Gaps

- truth: "Dashboard renders with proper styling, containers, and interactive components"
  status: resolved
  reason: "User reported: alles flackert und nichts ist korrekt geladen!"
  severity: blocker
  test: 1
  root_cause: "Two issues: (1) webview-react.css (74KB, all CSS module styles) is generated by esbuild but never loaded in the HTML template - getReactWebviewHtml() only links base.css. (2) Infinite render loop: DashboardView useEffect calls loadDashboard() on mount which sends reloadDashboard command, handler calls showDashboard() → render() → replaces HTML → React remounts → useEffect fires again → infinite loop."
  artifacts:
    - path: "iris-thaumantias/src/utils/webviewHelpers.ts"
      issue: "Only loads base.css, missing webview-react.css link tag"
    - path: "iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx"
      issue: "useEffect calls loadDashboard() on mount, triggering render loop via reloadDashboard command"
    - path: "iris-thaumantias/src/views/app/commands/navigationCommands.ts"
      issue: "handleReloadDashboard calls showDashboard() which calls render(), destroying React app"
  missing:
    - "Add webview-react.css link tag to getReactWebviewHtml() HTML template"
    - "Remove loadDashboard() from DashboardView initial useEffect (ready signal already sends data)"
    - "Change reload commands to send data via postMessage instead of calling render()"
  debug_session: ""

- truth: "CourseList navigable with styled course cards, search, filters, and dropdowns"
  status: resolved
  reason: "User reported: ich kann generell nichts anklicken weil irgendwie das styling komplett weg ist"
  severity: blocker
  test: 2
  root_cause: "Same root causes as Test 1: (1) webview-react.css not loaded means all CSS module styles missing. (2) Infinite render loop prevents stable interaction. Additionally, all Phase 4 reload commands (reloadCourses, reloadCourseDetail, reloadExerciseDetail) follow the same pattern of calling render() which would cause the same loop in their respective views."
  artifacts:
    - path: "iris-thaumantias/src/utils/webviewHelpers.ts"
      issue: "Only loads base.css, missing webview-react.css link tag"
    - path: "iris-thaumantias/src/views/app/commands/navigationCommands.ts"
      issue: "All reload handlers (reloadDashboard, reloadCourses, reloadCourseDetail, reloadExerciseDetail) call render() which destroys React app"
  missing:
    - "Fix webview-react.css loading (same as Test 1)"
    - "Change all reload command handlers to re-fetch data and send via typed messages instead of calling render()"
  debug_session: ""
