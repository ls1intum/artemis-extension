---
phase: 09-ui-polish-icons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - iris-thaumantias/src/utils/iconMap.ts
  - iris-thaumantias/src/views/webview/react/components/icons/ArtemisLogo.tsx
  - iris-thaumantias/src/views/webview/react/components/Button/IconButton.tsx
  - iris-thaumantias/src/views/webview/react/components/Button/IconButton.module.css
  - iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamConduction/components/ExerciseList.tsx
  - iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx
  - iris-thaumantias/src/views/webview/react/components/icons/index.ts
  - iris-thaumantias/src/views/webview/react/components/index.ts
  - iris-thaumantias/src/utils/iconDefinitions.ts
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Exercise type icons display correctly in CourseDetailView exercise lists"
    - "Exercise type icons display correctly in ExamConduction exercise list"
    - "Exercise detail page shows proper exercise type icon instead of emoji"
    - "IconButton presets (Close, Reload, Fullscreen, Settings, etc.) use Lucide components instead of inline SVGs"
    - "Artemis logo renders as a standalone React component with Lucide-compatible API"
    - "Only imported Lucide icons are bundled (tree-shaking verified via named imports)"
    - "Icons adapt to VS Code light/dark theme via CSS variables"
  artifacts:
    - path: "iris-thaumantias/src/utils/iconMap.ts"
      provides: "Typed const map of domain names to Lucide components + getIcon helper"
      exports: ["ICONS", "IconKey", "getIcon"]
    - path: "iris-thaumantias/src/views/webview/react/components/icons/ArtemisLogo.tsx"
      provides: "Artemis logo as React component with LucideProps API"
      exports: ["ArtemisLogo"]
  key_links:
    - from: "iris-thaumantias/src/utils/iconMap.ts"
      to: "lucide-react"
      via: "named imports"
      pattern: "import \\{ .+ \\} from 'lucide-react'"
    - from: "iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx"
      to: "iris-thaumantias/src/utils/iconMap.ts"
      via: "getIcon import"
      pattern: "import .+ from .+iconMap"
    - from: "iris-thaumantias/src/views/webview/react/views/ExamConduction/components/ExerciseList.tsx"
      to: "iris-thaumantias/src/utils/iconMap.ts"
      via: "getIcon import replaces IconDefinitions"
      pattern: "import .+ from .+iconMap"
---

<objective>
Migrate the entire icon system from custom SVG strings (IconDefinitions.ts) to Lucide React components with a typed const map, create an ArtemisLogo standalone component, update all IconButton presets to use Lucide icons, and migrate all icon consumers across the codebase.

Purpose: Enables tree-shaking (only used icons bundled), eliminates dangerouslySetInnerHTML for icon rendering, provides type-safe icon lookups, and establishes VS Code theme-aware icon styling via CSS variables.

Output: New iconMap.ts with typed ICONS const map, ArtemisLogo.tsx component, updated IconButton presets using Lucide, all consumers migrated, IconDefinitions.ts deleted.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-polish-icons/09-CONTEXT.md
@.planning/phases/09-ui-polish-icons/09-RESEARCH.md
@iris-thaumantias/src/utils/iconDefinitions.ts
@iris-thaumantias/src/views/webview/react/components/Button/IconButton.tsx
@iris-thaumantias/src/views/webview/react/components/Button/IconButton.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create icon infrastructure — iconMap.ts, ArtemisLogo.tsx, and icon theming CSS</name>
  <files>
    iris-thaumantias/src/utils/iconMap.ts
    iris-thaumantias/src/views/webview/react/components/icons/ArtemisLogo.tsx
    iris-thaumantias/src/views/webview/react/components/icons/index.ts
    iris-thaumantias/src/views/webview/react/components/index.ts
    iris-thaumantias/src/views/webview/react/components/Button/IconButton.tsx
    iris-thaumantias/src/views/webview/react/components/Button/IconButton.module.css
  </files>
  <action>
    **1. Create `iris-thaumantias/src/utils/iconMap.ts`:**

    Map ALL icon keys from IconDefinitions.ts to Lucide React named imports. The file must:
    - Import each icon individually from 'lucide-react' (named imports for tree-shaking)
    - Import `type LucideIcon` from 'lucide-react'
    - Export a typed const map `ICONS` using `as const satisfies Record<string, LucideIcon>`
    - Export type `IconKey = keyof typeof ICONS`
    - Export helper function `getIcon(type: string | undefined): LucideIcon` that normalizes input (lowercase, replace _ with -) and falls back to ICONS.default

    Icon mapping (audit IconDefinitions.ts for all 30+ keys and map to best Lucide match):
    - default → CircleDot
    - programming → Code2
    - quiz → CheckCircle
    - file-upload → Upload
    - modeling → Box
    - text → FileText
    - course → BookOpen
    - exercise → ClipboardList
    - refresh → RefreshCw
    - trash → Trash2
    - stethoscope → Stethoscope
    - star → Star
    - star-4-edges → Sparkles
    - cursor → MousePointer
    - gear → Settings
    - puzzle → Puzzle
    - web → Globe
    - logout → LogOut
    - uploadmessage → MessageSquareUp (or MessageCircle + ArrowUp composition — pick closest single Lucide icon)
    - key → KeyRound
    - shield → Shield
    - copy → Copy
    - link → Link
    - git → GitBranch
    - question-mark → HelpCircle
    - thumbs-up → ThumbsUp
    - thumbs-down → ThumbsDown
    - workspace → LayoutGrid
    - target → Target
    - eye-open → Eye
    - eye-closed → EyeOff
    - check → Check
    - plus → Plus
    - file → File
    - close → X
    - chevron-down → ChevronDown
    - chevron-right → ChevronRight
    - info-circle → XCircle (matches current icon which is an error/close circle, not info)
    - bug → Bug
    - artemis-logo → (not in ICONS map — handled by ArtemisLogo component)

    **2. Create `iris-thaumantias/src/views/webview/react/components/icons/ArtemisLogo.tsx`:**

    Standalone React component matching Lucide's prop API:
    - Accept `size` (default 24), `color` (default 'currentColor'), `strokeWidth` (default 2), `className`, and spread rest props
    - Import `type LucideProps` from 'lucide-react' for prop typing
    - Render the Artemis logo SVG (two paths from IconDefinitions: the stroked arrow and the filled triangle)
    - Use viewBox="0 0 232 204" to match original proportions
    - The first path uses `stroke={color}` with `strokeWidth={strokeWidth}`, the second uses `fill={color}`

    **3. Create `iris-thaumantias/src/views/webview/react/components/icons/index.ts`:**

    Barrel export: `export { ArtemisLogo } from './ArtemisLogo';`

    **4. Update `iris-thaumantias/src/views/webview/react/components/index.ts`:**

    Add export for icons: `export { ArtemisLogo } from './icons';`

    **5. Update `iris-thaumantias/src/views/webview/react/components/Button/IconButton.tsx`:**

    Migrate ALL preset variants (Close, Checkmark, BurgerMenu, Collapse, Fullscreen, Reload, Settings) from inline SVGs to Lucide React components:
    - Import needed icons from 'lucide-react': `X`, `Check`, `Menu`, `ChevronDown`, `Maximize2`, `RefreshCw`, `Settings`
    - Replace each inline `<svg>` in preset functions with the corresponding Lucide component: `<X size={16} />`, `<Check size={16} />`, `<Menu size={16} />`, `<ChevronDown size={16} className={styles.collapseChevron} />`, `<Maximize2 size={16} />`, `<RefreshCw size={16} />`, `<Settings size={16} />`
    - Keep the existing button wrapper structure, clsx classes, aria attributes, and event handlers unchanged
    - The Collapse variant's chevron still needs the `className={styles.collapseChevron}` for rotation animation
    - The main `IconButton` component's `icon: ReactNode` prop stays unchanged (consumers pass any ReactNode)

    **6. Update `iris-thaumantias/src/views/webview/react/components/Button/IconButton.module.css`:**

    Add icon theming CSS variables at the top of the file:
    ```css
    /* Icon theming — applies to all icon buttons */
    .iconBtn {
        color: var(--vscode-icon-foreground, #c5c5c5);
    }

    .iconBtn:hover:not(.iconBtnDisabled) {
        background-color: var(--vscode-toolbar-hoverBackground, rgba(90, 93, 94, 0.31));
        color: var(--vscode-foreground, #cccccc);
    }

    .iconBtn:active:not(.iconBtnDisabled) {
        background-color: var(--vscode-toolbar-activeBackground, rgba(99, 102, 103, 0.4));
    }
    ```
    This provides default hover/active for ALL icon buttons. Keep the existing variant-specific overrides (Close gets red hover, Checkmark gets blue hover) as they take precedence via specificity.

    Remove individual `color: var(--vscode-icon-foreground, #c5c5c5);` from each variant class since the base `.iconBtn` now handles it. Keep variant-specific hover overrides.
  </action>
  <verify>
    <automated>cd /Users/liamberger/Documents/private/artemis-extension/iris-thaumantias && npx tsc --noEmit 2>&1 | head -50</automated>
    <manual>Verify iconMap.ts exports ICONS, IconKey, and getIcon. Verify ArtemisLogo renders. Verify IconButton presets use Lucide components.</manual>
  </verify>
  <done>
    iconMap.ts created with typed const map covering all 30+ icon keys from IconDefinitions.ts.
    ArtemisLogo.tsx created as standalone component with LucideProps API.
    All IconButton presets (Close, Checkmark, BurgerMenu, Collapse, Fullscreen, Reload, Settings) use Lucide React components instead of inline SVGs.
    CSS variables provide base theme-aware coloring for all icon buttons.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate all icon consumers and delete IconDefinitions.ts</name>
  <files>
    iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamConduction/components/ExerciseList.tsx
    iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx
    iris-thaumantias/src/utils/iconDefinitions.ts
  </files>
  <action>
    **1. Migrate `CourseDetailView.tsx`:**

    - Remove the inline `getExerciseIcon()` function that returns emoji strings (lines ~197-213)
    - Import `getIcon` from `../../../../../../utils/iconMap` (or appropriate relative path from CourseDetail to utils)
    - Replace the emoji-based `{getExerciseIcon(exercise.type || '')}` with a Lucide component render:
      ```tsx
      const ExerciseIcon = getIcon(exercise.type);
      // Then in JSX:
      <ExerciseIcon size={16} />
      ```
    - The `<span className={styles.exerciseTypeIcon}>` wrapper stays but now contains a React component instead of an emoji string
    - Exercise type indicators display as icon only (no text label) — per user decision

    **2. Migrate `ExerciseDetailView.tsx`:**

    - Import `getIcon` from `../../../../../utils/iconMap`
    - Replace the emoji-based exercise type indicator in the exercise card (`{exerciseType === 'programming' ? '💻' : '📄'}` around line 336-337) with:
      ```tsx
      const ExerciseTypeIcon = getIcon(exercise.type);
      // In JSX:
      <ExerciseTypeIcon size={16} />
      ```
    - The `<div className={styles.exerciseIconBadge}>` wrapper stays

    **3. Migrate `ExamConduction/components/ExerciseList.tsx`:**

    - Remove `import { IconDefinitions } from '../../../../../../utils/iconDefinitions';`
    - Import `getIcon` from `../../../../../../utils/iconMap`
    - Replace the `dangerouslySetInnerHTML={{ __html: icon }}` pattern:
      - Before: `const icon = IconDefinitions.getIcon(exercise.type || 'default');` then `<span dangerouslySetInnerHTML={{ __html: icon }} />`
      - After: `const ExerciseIcon = getIcon(exercise.type);` then `<ExerciseIcon size={16} />`
    - Remove the `<span>` wrapper's `dangerouslySetInnerHTML` prop, render `<ExerciseIcon />` as child instead

    **4. Check `DashboardView.tsx`:**

    - Search for any icon usage (emoji or IconDefinitions). If it uses emojis for exercise/course types, migrate to Lucide icons via getIcon() or direct named imports.
    - If DashboardView uses ArtemisLogo or similar branding icon, import from the new icons component.

    **5. Delete `iris-thaumantias/src/utils/iconDefinitions.ts`:**

    - Only delete AFTER confirming no remaining imports. Run `grep -r "iconDefinitions\|IconDefinitions" iris-thaumantias/src/` to verify zero consumers remain.
    - Delete the file.

    **6. Verify no remaining dangerouslySetInnerHTML for icons:**

    Run `grep -r "dangerouslySetInnerHTML.*icon\|getIcon.*string" iris-thaumantias/src/views/` — should return zero results (ProblemStatement's dangerouslySetInnerHTML for markdown is expected and separate).
  </action>
  <verify>
    <automated>cd /Users/liamberger/Documents/private/artemis-extension/iris-thaumantias && npx tsc --noEmit 2>&1 | head -50</automated>
    <manual>Verify CourseDetailView, ExerciseDetailView, and ExamConduction ExerciseList render Lucide icon components. Verify IconDefinitions.ts is deleted. Verify no dangerouslySetInnerHTML remains for icon rendering.</manual>
  </verify>
  <done>
    All icon consumers migrated from emoji strings or IconDefinitions.getIcon() to Lucide React components via iconMap.ts getIcon() helper.
    dangerouslySetInnerHTML eliminated for all icon rendering (only ProblemStatement retains it for markdown content).
    IconDefinitions.ts deleted with zero remaining imports.
    Exercise type indicators display as Lucide icons in CourseDetailView, ExerciseDetailView, and ExamConduction.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no new errors (pre-existing errors acceptable)
2. `grep -r "IconDefinitions\|iconDefinitions" iris-thaumantias/src/` returns zero results
3. `grep -r "dangerouslySetInnerHTML" iris-thaumantias/src/views/webview/react/` returns only ProblemStatement (markdown rendering)
4. `iconMap.ts` exports ICONS with 30+ keys, all mapping to named Lucide imports
5. `ArtemisLogo.tsx` exports a component with LucideProps-compatible API
6. All IconButton presets render Lucide components (no inline SVG strings)
</verification>

<success_criteria>
- UI-01 satisfied: All custom SVG icons from IconDefinitions.ts replaced with Lucide React components using named imports
- Icon system uses consistent theming via CSS variables (--vscode-icon-foreground for default, VS Code semantic colors for variants)
- Tree-shaking enabled: Only named-imported icons bundled (no wildcard imports from lucide-react)
- IconDefinitions.ts deleted, zero consumers remaining
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-polish-icons/09-01-SUMMARY.md`
</output>
