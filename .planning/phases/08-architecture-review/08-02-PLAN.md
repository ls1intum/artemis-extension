---
phase: 08-architecture-review
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - .planning/phases/08-architecture-review/08-AUDIT.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "Comprehensive audit document exists with executive summary, health assessment, and prioritized findings"
    - "Every finding includes problem statement, impact/effort rating, file references, and recommendation"
    - "Impact/effort matrix categorizes findings into quick wins, v1.1 priorities, v1.2 deferrals, and backlog"
    - "Keep list documents intentional patterns that should not be refactored"
    - "Migration-era decisions are documented with rationale"
    - "Mermaid diagrams illustrate component tree, data flow, and message contracts"
    - "Findings cross-reference downstream phases (9-14) where applicable"
    - "PROJECT.md updated with architecture decisions section"
  artifacts:
    - path: ".planning/phases/08-architecture-review/08-AUDIT.md"
      provides: "Comprehensive architecture audit document"
      contains: "## Executive Summary"
    - path: ".planning/PROJECT.md"
      provides: "Updated project file with architecture decisions section"
      contains: "## Architecture Decisions"
  key_links:
    - from: ".planning/phases/08-architecture-review/08-01-analysis-raw.md"
      to: ".planning/phases/08-architecture-review/08-AUDIT.md"
      via: "raw findings synthesized into structured audit document"
      pattern: "Finding|Impact|Effort"
    - from: ".planning/phases/08-architecture-review/08-AUDIT.md"
      to: ".planning/PROJECT.md"
      via: "key architecture decisions extracted and documented in PROJECT.md"
      pattern: "Architecture Decisions"
---

<objective>
Trace end-to-end user flows through the codebase, synthesize all raw findings from Plan 01 into the final comprehensive audit document, create architecture diagrams, and update PROJECT.md with architecture decisions.

Purpose: Produce the two deliverables that satisfy ARCH-01 (audit completed) and ARCH-02 (improvements documented). End-to-end flow tracing catches cross-boundary issues that structural review misses. The audit document becomes the input for Phases 9-14 remediation work.

Output: 08-AUDIT.md (comprehensive audit document) and updated PROJECT.md (architecture decisions section).
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md
@.planning/phases/08-architecture-review/08-CONTEXT.md
@.planning/phases/08-architecture-review/08-RESEARCH.md
@.planning/phases/08-architecture-review/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end flow tracing and cross-boundary analysis</name>
  <files>.planning/phases/08-architecture-review/08-02-flow-findings.md</files>
  <action>
Read the raw findings from Plan 01 (08-01-analysis-raw.md) to understand structural observations.

Trace ALL identifiable user flows through the full stack per CONTEXT.md. For each flow, follow the data path from UI action through message contracts, extension host processing, API/WebSocket calls, and back to UI update. Document cross-boundary issues, error propagation gaps, and state synchronization problems.

**Flows to trace:**

1. **Login flow:** Login form submission → postMessage → AuthCommandModule → API.login() → cookie storage → WebSocket connect → dashboard load → webview update
2. **Course browsing flow:** Dashboard → course list navigation → course detail → exercise list → breadcrumb state management
3. **Exercise interaction flow:** Exercise detail load → repository actions (clone, pull, submit) → submission → WebSocket result notification → UI update → build error CodeLens
4. **Iris chat flow:** Chat panel open → session creation → message input → API stream → token buffering → message rendering → scroll management
5. **WebSocket connection flow:** Auth success → STOMP connect → subscription setup → message dispatch → reconnection on failure → error propagation to UI
6. **Error propagation flow:** API error / WebSocket error / timeout → boundary crossing → logging → user notification (or gap where error is swallowed)
7. **Exam flow:** Exam start → conduction view → timer (Web Worker) → exercise navigation → submission during exam → time management
8. **State persistence flow:** User switches tabs → webview hidden → state saved → webview shown → state restored → data freshness

For each flow, document:
- Entry point and exit point
- Every boundary crossing (extension ↔ webview, extension ↔ API, extension ↔ WebSocket)
- State transitions and where state lives at each step
- Error handling at each boundary (or lack thereof)
- Cross-cutting concerns (auth headers, logging, telemetry hooks)

Write findings to `08-02-flow-findings.md` as intermediate output.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-architecture-review/08-02-flow-findings.md && grep -c "flow" .planning/phases/08-architecture-review/08-02-flow-findings.md</automated>
    <manual>Flow findings document exists with traced paths for all 8 identified user flows</manual>
  </verify>
  <done>All 8 user flows traced end-to-end with boundary crossings, error handling gaps, and state synchronization issues documented. Cross-boundary findings ready for synthesis into audit document.</done>
</task>

<task type="auto">
  <name>Task 2: Compile final audit document and update PROJECT.md</name>
  <files>
    .planning/phases/08-architecture-review/08-AUDIT.md
    .planning/PROJECT.md
  </files>
  <action>
Read both intermediate documents:
- `08-01-analysis-raw.md` (structural findings from Plan 01)
- `08-02-flow-findings.md` (flow tracing findings from Task 1)

**Step 1: Create 08-AUDIT.md following the template from RESEARCH.md**

Structure the audit document with ALL required sections per CONTEXT.md locked decisions:

1. **Executive Summary** (2-3 paragraphs):
   - Overall health assessment
   - Top 3-5 priorities
   - Recommendation for what to tackle first

2. **Health Summary** (qualitative, no numeric score):
   - Strengths (patterns working well)
   - Concerns (patterns needing attention)
   - Posture (qualitative assessment)

3. **Findings** (each finding MUST include):
   - Problem statement
   - Why it matters
   - Recommendation with specific file/line references
   - Before/after code snippets where helpful
   - Impact rating (H/M/L)
   - Effort rating (H/M/L)
   - Phase mapping (which downstream phase addresses this, or "Tech Debt Backlog")
   - Flag: "current issue" vs "will become problem as codebase grows" (per CONTEXT.md)

4. **Impact/Effort Matrix**:
   - Quick Wins (High Impact, Low Effort)
   - Prioritize for v1.1 (High Impact, Medium Effort)
   - Defer to v1.2+ (High Impact, High Effort)
   - Tech Debt Backlog (Medium/Low Impact, not covered by Phases 9-14)

5. **Keep List** (intentional patterns that look like anti-patterns):
   - Pattern name, why it exists, files where used, why NOT to refactor
   - Apply conservative tolerance per CONTEXT.md tech debt philosophy
   - Distinguish accidental tech debt from deliberate tech debt

6. **Migration-Era Decisions**:
   - Document v1.0 React migration rationale
   - Context, chosen approach, rationale, current status
   - Enough context for a new developer to understand why

7. **Architecture Diagrams** (Mermaid.js):
   - Component tree (extension host + webview hierarchy)
   - Data flow (state updates across extension/webview boundary)
   - Message contracts (postMessage flows between extension and webview)

8. **Roadmap Implications** (per CONTEXT.md: flag without modifying roadmap):
   - Note any dependencies or ordering concerns for Phases 9-14
   - Identify findings that may affect scope of downstream phases

9. **Files Reviewed Appendix**:
   - Complete list of every reviewed file organized by layer
   - Extension Host (Node.js) files
   - Webview (Browser) files
   - Total file count and LOC

**Step 2: Update PROJECT.md**

Add an "## Architecture Decisions" section to PROJECT.md (after the "Key Decisions" table). Include:
- Rationale for current patterns discovered during audit
- Decisions that should be preserved (from Keep list)
- Decisions that should be revisited (from findings)
- Format: decision name, context, rationale, status

Do NOT modify the existing Key Decisions table — add a new section below it.

**Quality checks before writing:**
- Every finding has all required fields (problem, why, recommendation, files, impact, effort, phase mapping)
- Keep list protects patterns that would otherwise be accidentally refactored
- Migration-era decisions explain enough context for someone unfamiliar with v1.0
- Mermaid diagrams are syntactically valid
- Files reviewed appendix is comprehensive
- Cross-references to Phases 9-14 are accurate (check ROADMAP.md requirements)
- Tech debt philosophy applied: conservative tolerance, distinguish accidental vs deliberate debt
  </action>
  <verify>
    <automated>test -f .planning/phases/08-architecture-review/08-AUDIT.md && grep -q "Executive Summary" .planning/phases/08-architecture-review/08-AUDIT.md && grep -q "Architecture Decisions" .planning/PROJECT.md</automated>
    <manual>Audit document has all required sections. PROJECT.md has new Architecture Decisions section.</manual>
  </verify>
  <done>08-AUDIT.md exists with: executive summary, health summary, numbered findings with impact/effort ratings, impact/effort matrix, keep list, migration-era decisions, Mermaid diagrams, roadmap implications, and files reviewed appendix. PROJECT.md has a new Architecture Decisions section documenting current patterns with rationale.</done>
</task>

</tasks>

<verification>
- `08-AUDIT.md` exists with all 9 required sections
- Executive summary contains overall health assessment and top 3-5 priorities
- Every finding has: problem, why it matters, recommendation, file references, impact (H/M/L), effort (H/M/L), phase mapping
- Impact/effort matrix categorizes all findings
- Keep list documents at least 1 intentional pattern
- Migration-era decisions section documents v1.0 React migration choices
- Mermaid diagrams present for component tree, data flow, and message contracts
- Findings cross-reference to Phases 9-14 where applicable
- Unmapped findings go to tech debt backlog section
- Files reviewed appendix lists every source file
- PROJECT.md has new Architecture Decisions section
- No code changes made (documentation only per CONTEXT.md)
</verification>

<success_criteria>
- ARCH-01 satisfied: comprehensive architecture audit completed with documented findings across all five areas, flow tracing, and prioritized recommendations
- ARCH-02 satisfied: architecture improvements documented via audit findings + PROJECT.md architecture decisions section (per CONTEXT.md: satisfied by documentation, not code changes)
- All CONTEXT.md locked decisions honored (format, sections, methodology, philosophy)
- No deferred ideas implemented (none were deferred)
</success_criteria>

<output>
After completion, create `.planning/phases/08-architecture-review/08-02-SUMMARY.md`
</output>
