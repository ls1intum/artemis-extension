---
phase: 12-typescript-strict-mode
plan: 07
type: execute
wave: 3
depends_on: [12-04, 12-08]
files_modified:
  - iris-thaumantias/src/views/webview/react/views/IrisChat/IrisChatView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/Login/LoginView.tsx
  - iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx
  - iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamExerciseDetail/ExamExerciseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamConduction/ExamConductionView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamStart/ExamStartView.tsx
  - iris-thaumantias/src/views/webview/react/views/RecommendedExtensions/RecommendedExtensionsView.tsx
  - iris-thaumantias/src/views/webview/react/views/ServiceStatus/ServiceStatusView.tsx
  - iris-thaumantias/src/views/webview/react/views/GitCredentials/GitCredentialsView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/MessageBubble.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/ContextSelector.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/CodeBlock.tsx
  - iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts
  - iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts
  - iris-thaumantias/src/views/webview/react/stores/useChatStore.ts
  - iris-thaumantias/src/views/webview/react/components/List.tsx
  - iris-thaumantias/src/shared/messageContracts.ts
autonomous: true
requirements: [TYPE-01, TYPE-03]
gap_closure: true

must_haves:
  truths:
    - "Webview React layer is fully type-safe with no explicit any types in views, stores, or components"
    - "Shared message contract modules use only typed payloads with no any escape hatches"
    - "Full codebase lint passes with zero no-explicit-any or no-unsafe-* errors (npm run lint)"
    - "TypeScript compilation stays at zero errors (npx tsc --noEmit exits 0)"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/"
      provides: "Fully typed React views, stores, and components"
    - path: "iris-thaumantias/src/shared/messageContracts.ts"
      provides: "Type-safe message contracts with zero any types"
  key_links:
    - from: "src/views/webview/react/views/*"
      to: "src/shared/messageContracts.ts"
      via: "Typed message handling in useEffect listeners"
      pattern: "ExtensionToWebviewMessage|isExtensionMessage"
    - from: "src/views/webview/react/stores/*"
      to: "src/types/apiResponses.ts"
      via: "Domain types for store state"
      pattern: "import.*apiResponses"
---

<objective>
Eliminate all explicit `any` types from webview React code and shared modules (279 ESLint errors + 11 remaining explicit any from Plan 12-03).

Purpose: Complete the TYPE-03 requirement by making the entire webview React layer and shared modules fully type-safe. This is the final layer — after Plans 05 and 06 handle the extension host, this plan ensures zero any types across the entire codebase.

Output: Zero ESLint errors on `npm run lint`. TYPE-03 fully satisfied.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typescript-strict-mode/12-03-SUMMARY.md
@.planning/phases/12-typescript-strict-mode/12-04-SUMMARY.md
@iris-thaumantias/src/types/apiResponses.ts
@iris-thaumantias/src/shared/messageContracts.ts
@iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminate any from React views (largest files first)</name>
  <files>
    iris-thaumantias/src/views/webview/react/views/IrisChat/IrisChatView.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/Login/LoginView.tsx
    iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx
    iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamExerciseDetail/ExamExerciseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamConduction/ExamConductionView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamStart/ExamStartView.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.tsx
  </files>
  <action>
    **Eliminate ~217 ESLint errors across 10 React view files.**

    **Process files largest-first:**

    **IrisChatView.tsx (49 errors):**
    - `sendCommand` postMessage cast → type the message using WebviewToExtensionMessage
    - `loadMessages` map callback → type with IrisChatMessage
    - Message event handler → use type guard isExtensionMessage
    - Chat history data → use IrisChatSession, IrisChatMessage from apiResponses
    - Any in useEffect message listener → type the event.data with MessageEvent and narrow

    **ExerciseDetailView.tsx (43 errors):**
    - Exercise data access → use ExerciseDetailsResponse (already in store types)
    - `exerciseData.exercise` possibly undefined → add null checks (some already fixed in Plan 04)
    - Submission/result data → use SubmissionSummary, ResultSummary
    - Message handling → type with ExtensionToWebviewMessage

    **CourseDetailView.tsx (35 errors):**
    - `hideDeveloperTools` metadata access → type properly
    - Exercise data → use ExerciseDetail
    - Course data → use CourseDetailData
    - Message event handling → type guards

    **LoginView.tsx (26 errors):**
    - Health check results `data` is unknown → add type guard or assertion for health check result shape
    - Form state → already typed, fix cascading unsafe access
    - Message handling → type with ExtensionToWebviewMessage

    **DashboardView.tsx (22 errors):**
    - Course card data → use CourseDashboardCourse
    - Message handling → type guards
    - Navigation commands → typed payloads

    **CourseListView.tsx (21 errors):**
    - Course list data → use CourseDashboardCourse[]
    - Filter/sort callbacks → type parameters
    - Message handling → type guards

    **ExamExerciseDetailView.tsx (16 errors), ExamConductionView.tsx (11 errors), ExamStartView.tsx (5 errors):**
    - Exam data → use StudentExam, ExerciseDetailsResponse
    - Timer/working time → handle undefined with defaults
    - Message handling → type guards

    **ProblemStatement.tsx (11 errors):**
    - HTML content handling → type as string
    - Link/image handler callbacks → type event parameters
    - DOMPurify output → type as string

    **Common React view patterns:**
    1. **Message event listeners:** `window.addEventListener('message', (event) => { ... })` — type event as `MessageEvent` and narrow `event.data` with `isExtensionMessage(event.data)`
    2. **postMessage calls:** `vscodeApi.postMessage(msg)` — ensure msg conforms to `WebviewToExtensionMessage`
    3. **Store data access:** Stores are already typed (Plan 12-03). If ESLint still reports unsafe access, the issue is in how the component destructures or accesses store state.
    4. **Legacy command format:** Views that send `{ command: 'foo' }` must be updated to use typed message format from messageContracts.ts (or use the legacy message type added in Plan 12-04).
    5. **Catch blocks:** `catch (error: unknown)` pattern throughout.

    **Verify:** `npx eslint src/views/webview/react/views/ --max-warnings 0` shows 0 errors.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/views/webview/react/views/ 2>&1 | tail -3</automated>
  </verify>
  <done>Zero ESLint errors in all React view files. All views use typed message handling, domain types, and proper null checks.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate any from stores, components, shared, and final verification</name>
  <files>
    iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts
    iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts
    iris-thaumantias/src/views/webview/react/stores/useChatStore.ts
    iris-thaumantias/src/views/webview/react/views/RecommendedExtensions/RecommendedExtensionsView.tsx
    iris-thaumantias/src/views/webview/react/views/ServiceStatus/ServiceStatusView.tsx
    iris-thaumantias/src/views/webview/react/views/GitCredentials/GitCredentialsView.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/MessageBubble.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/ContextSelector.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/CodeBlock.tsx
    iris-thaumantias/src/views/webview/react/components/List.tsx
    iris-thaumantias/src/shared/messageContracts.ts
  </files>
  <action>
    **Eliminate remaining ~62 ESLint errors in stores, components, and shared code.**

    **Stores (27 errors):**
    - `useExerciseDetailStore.ts` (25 errors): Fix remaining unsafe access patterns. The store state types are set (Plan 12-03) but some callbacks and internal methods may still have unsafe patterns. Fix map/filter/reduce callback parameter types.
    - `useCourseListStore.ts` (2 errors): Small fix — likely callback parameter typing.
    - `useChatStore.ts` (0 errors currently but verify): Confirm clean.

    **Remaining React views (14 errors):**
    - `RecommendedExtensionsView.tsx` (7): Legacy command format handling — update to typed messages
    - `ServiceStatusView.tsx` (5): Legacy command format + health check data typing
    - `GitCredentialsView.tsx` (2): Small typing fixes

    **IrisChat components (8 errors):**
    - `MessageBubble.tsx` (3): Streamdown component prop typing
    - `ContextSelector.tsx` (3): Context data typing
    - `CodeBlock.tsx` (2): Shiki-related typing

    **Shared component (List.tsx):**
    - 1 error: React.cloneElement props spread — complex type. Use `React.ReactElement` with proper generic or a justified type assertion with comment.

    **messageContracts.ts:**
    - Verify zero any remains. If any were temporarily introduced in Plan 12-04 for legacy compatibility, ensure they use `Record<string, unknown>` not `any`.

    **Final full-codebase verification:**
    After all fixes, run the complete lint suite:
    ```bash
    npx eslint src/ --max-warnings 0
    ```
    Target: 0 errors, 0 warnings (excluding the 40 auto-fixable warnings if they are style-only).

    Also run:
    ```bash
    npx tsc --noEmit  # Must still be zero errors
    npm run check-types  # Must pass
    ```

    **If any errors remain in files NOT in this plan's scope:** Document them but do not modify files owned by Plans 05 or 06. Those plans handle their own files.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/views/webview/react/ src/shared/ 2>&1 | tail -3</automated>
  </verify>
  <done>Zero ESLint errors in webview React layer and shared modules. TYPE-03 requirement fully satisfied for webview scope. npm run lint passes on entire src/ when combined with Plans 05+06.</done>
</task>

</tasks>

<verification>
```bash
# TYPE-01: TypeScript compilation must stay at zero errors
cd iris-thaumantias && npx tsc --noEmit
echo "tsc exit code: $?"

# TYPE-03: Zero ESLint errors in webview + shared
cd iris-thaumantias && npx eslint src/views/webview/react/ src/shared/ 2>&1 | tail -5

# Full codebase lint (depends on Plans 05+06 also completing)
cd iris-thaumantias && npx eslint src/ 2>&1 | tail -5

# Zero explicit any in entire source
grep -rn "\bany\b" src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | grep -v ".d.ts" | grep -v "//.*any\|/\*.*any" | wc -l
# Target: 0 (or very close — VS Code API boundaries may have documented any)
```
</verification>

<success_criteria>
- Zero ESLint errors in src/views/webview/react/ and src/shared/
- No new @ts-ignore or @ts-expect-error comments
- TypeScript compilation passes (zero errors)
- All React views use typed message handling
- All stores use domain types
- TYPE-03 requirement satisfied for webview scope
- When combined with Plans 05+06: npm run lint passes on entire src/
</success_criteria>

<output>
After completion, create `.planning/phases/12-typescript-strict-mode/12-07-SUMMARY.md`
</output>
