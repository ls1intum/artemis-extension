---
phase: 12-typescript-strict-mode
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts
  - iris-thaumantias/src/views/webview/react/stores/useCourseDetailStore.ts
  - iris-thaumantias/src/views/webview/react/views/ServiceStatus/ServiceStatusView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamExerciseDetail/ExamExerciseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/CodeBlock.tsx
  - iris-thaumantias/src/views/webview/react/components/ProblemStatement/ProblemStatement.tsx
  - iris-thaumantias/src/shared/messageContracts.ts
autonomous: true
requirements: [TYPE-03]

must_haves:
  truths:
    - "Zero any types remain in webview React source code (src/views/webview/react/)"
    - "Zero any types remain in shared code (src/shared/)"
    - "Message contract payloads use specific types for exerciseData, studentExam, and other domain data"
    - "All React store actions and state use specific types (no any in Zustand stores)"
    - "npm run lint passes with zero any-related errors across entire src/"
    - "npx tsc --noEmit still produces zero errors"
  artifacts:
    - path: "iris-thaumantias/src/shared/messageContracts.ts"
      provides: "Fully typed message contracts with specific payload types (no unknown for domain data)"
      contains: "exerciseData"
    - path: "iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts"
      provides: "Type-safe exercise detail store with specific types"
  key_links:
    - from: "iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts"
      to: "iris-thaumantias/src/shared/messageContracts.ts"
      via: "Store uses typed message payloads"
      pattern: "import.*from.*messageContracts"
    - from: "iris-thaumantias/src/views/webview/react/stores/useCourseDetailStore.ts"
      to: "iris-thaumantias/src/shared/messageContracts.ts"
      via: "Store uses typed message payloads"
      pattern: "import.*from.*messageContracts"
---

<objective>
Eliminate all remaining `any` types from webview React code and shared modules.

Purpose: Complete TYPE-03 enforcement across the entire codebase. After Plan 02 handles extension host code, this plan handles the webview layer (React components, Zustand stores, shared contracts) to achieve zero any types project-wide.

Output: Zero any types in entire src/ directory. Full ESLint strict type checking passes.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typescript-strict-mode/12-RESEARCH.md
@.planning/phases/12-typescript-strict-mode/12-01-SUMMARY.md

Key source files:
@iris-thaumantias/src/shared/messageContracts.ts
@iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts
@iris-thaumantias/src/views/webview/react/stores/useCourseDetailStore.ts

<interfaces>
<!-- Key any hotspots in webview/shared layer: -->
<!-- src/views/webview/react/stores/useExerciseDetailStore.ts: 14 any -->
<!-- src/views/webview/react/stores/useCourseDetailStore.ts: 4 any -->
<!-- src/views/webview/react/views/ServiceStatus/ServiceStatusView.tsx: 2 any -->
<!-- src/shared/messageContracts.ts: uses unknown for exerciseData/studentExam payloads -->
<!-- Plus scattered any in other React components and views -->

<!-- From messageContracts.ts (key payload types using unknown): -->
<!-- ExerciseDetailInitMessage: payload.exerciseData: unknown -->
<!-- ExamConductionInitMessage: payload.studentExam: unknown -->
<!-- ExamExerciseDetailInitMessage: payload.exerciseData: unknown, payload.studentExam: unknown -->
<!-- These need specific domain types per user decision (prefer specific over unknown) -->

<!-- Existing domain types available: -->
<!-- src/models/core.ts: ArtemisCourse, ArtemisExercise, ArtemisExam -->
<!-- src/types/artemis.ts: Additional Artemis types -->
<!-- src/types/apiResponses.ts: API response shapes -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type message contract payloads and shared module any elimination</name>
  <files>
    iris-thaumantias/src/shared/messageContracts.ts
  </files>
  <action>
    Replace `unknown` and `any` types in messageContracts.ts payload definitions with specific domain types.

    **1. Audit current payload types:**
    Read messageContracts.ts fully. Identify all payload fields using `unknown`, `any`, or overly broad types. Based on research, the key ones are:
    - `ExerciseDetailInitMessage.payload.exerciseData: unknown`
    - `ExamConductionInitMessage.payload.studentExam: unknown`
    - `ExamExerciseDetailInitMessage.payload.exerciseData: unknown`
    - `ExamExerciseDetailInitMessage.payload.studentExam: unknown`
    - Any other `any` or `unknown` in message payloads

    **2. Create specific payload interfaces:**
    Check existing domain models in:
    - `src/models/core.ts` (ArtemisCourse, ArtemisExercise, ArtemisExam)
    - `src/types/artemis.ts`
    - `src/types/apiResponses.ts`

    If suitable interfaces already exist, import and use them. If not, create minimal interfaces in messageContracts.ts that describe the actual shape of data being passed. For example:
    ```typescript
    // Use existing Exercise interface (already defined in messageContracts.ts lines 477+)
    // or reference models from src/models/
    ```

    Per user decision: prefer specific interfaces over unknown. Do NOT use unknown as a lazy replacement.

    **3. Replace all any/unknown payload types:**
    Update each message interface to use specific types. If the data comes from an API response and the exact shape is uncertain, create a minimal interface with the fields actually accessed by consumers (stores, views). Add optional fields for properties that may or may not be present.

    **4. Verify no any/unknown remains in messageContracts.ts** (except in type guard implementations where `unknown` is the correct parameter type for narrowing from untyped sources).

    Add explicit return type annotations to all functions in the file.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/shared/messageContracts.ts --max-warnings 0 2>&1 | grep -c "no-explicit-any\|no-unsafe" | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>messageContracts.ts has zero any types. All payload types use specific domain interfaces. Type guards properly use unknown as parameter type (correct usage). File passes ESLint strict rules.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate any from React stores, components, and views</name>
  <files>
    iris-thaumantias/src/views/webview/react/stores/useExerciseDetailStore.ts
    iris-thaumantias/src/views/webview/react/stores/useCourseDetailStore.ts
    iris-thaumantias/src/views/webview/react/views/ServiceStatus/ServiceStatusView.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamExerciseDetail/ExamExerciseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/CodeBlock.tsx
    iris-thaumantias/src/views/webview/react/components/ProblemStatement/ProblemStatement.tsx
  </files>
  <action>
    Eliminate ALL remaining `any` types from the webview React layer.

    **1. Zustand stores (heaviest any concentration):**

    `useExerciseDetailStore.ts` (14 any occurrences):
    - Read the store and identify all any-typed state, actions, and parameters
    - Replace with specific types from messageContracts.ts (which Task 1 just typed)
    - State fields like exerciseData should use the specific Exercise/ExerciseDetail interface
    - Action parameters should match the message payload types
    - Add explicit return type annotations to all store functions and selectors

    `useCourseDetailStore.ts` (4 any occurrences):
    - Similar treatment — replace any with CourseData or specific course types
    - Reference messageContracts.ts CourseDetailInitMessage payload types

    **2. React views with any:**

    Scan ALL remaining files under `src/views/webview/react/` for any types:
    ```bash
    grep -rn "\bany\b" src/views/webview/react/ --include="*.ts" --include="*.tsx"
    ```

    For each file found:
    - Replace any with specific prop interfaces, event types, or domain types
    - For event handlers: use React.ChangeEvent<HTMLInputElement>, React.MouseEvent, etc.
    - For component props: create/use explicit Props interfaces
    - For callback types: use specific function signatures

    **3. Remaining shared code:**
    Scan `src/shared/` and any other src/ directories not covered by Plan 02:
    ```bash
    grep -rn "\bany\b" src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules"
    ```

    Fix ANY remaining occurrences. The goal is zero any in the entire src/ directory.

    **4. Add explicit return type annotations** to all functions in modified files.

    **5. Final comprehensive lint check:**
    ```bash
    npx eslint src/ --max-warnings 0
    ```
    This must pass with zero any-related errors across the entire source tree.

    **Important notes:**
    - Per user decision: no @ts-ignore or @ts-expect-error comments
    - Per user decision: type assertions (as SomeType) allowed minimally with a comment explaining why
    - Per user decision: refactor code where necessary for proper types
    - If a genuine runtime bug is revealed by typing, fix the bug
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/ --max-warnings 0 2>&1 | grep -c "no-explicit-any\|no-unsafe" | grep -q "^0$" && echo "PASS" || echo "FAIL" && npx tsc --noEmit 2>&1 | grep -c "error TS" | grep -q "^0$" && echo "PASS: zero TS errors" || echo "FAIL: TS errors remain"</automated>
  </verify>
  <done>Zero any types across entire src/ directory. All Zustand stores use specific types. All React components have typed props and callbacks. ESLint strict rules pass on entire src/. npx tsc --noEmit still produces zero errors. npm run compile succeeds.</done>
</task>

</tasks>

<verification>
1. `cd iris-thaumantias && grep -rn "\bany\b" src/ --include="*.ts" --include="*.tsx" | grep -v "//.*any\|@param.*any\|\*.*any" | wc -l` returns 0 (or only documented VS Code API boundary cases)
2. `cd iris-thaumantias && npx eslint src/ --max-warnings 0` passes with zero any-related errors
3. `cd iris-thaumantias && npx tsc --noEmit` exits with zero errors
4. `npm run compile` succeeds (full production build)
5. `npm run test:react` passes (React component tests still work with typed stores)
</verification>

<success_criteria>
- Zero any types in entire src/ directory (or documented minimal exceptions at VS Code API boundaries)
- All messageContracts.ts payloads use specific domain types
- All Zustand stores fully typed with specific state and action types
- All React components have typed props, events, and callbacks
- ESLint strict type-checking passes on entire src/
- TypeScript compilation produces zero errors
- Production build and React tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-typescript-strict-mode/12-03-SUMMARY.md`
</output>
