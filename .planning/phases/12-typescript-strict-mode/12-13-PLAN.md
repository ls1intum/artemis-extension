---
phase: 12-typescript-strict-mode
plan: 13
type: execute
wave: 3
depends_on: [12-09, 12-12]
files_modified:
  - iris-thaumantias/src/provider/artemisWebviewProvider.ts
  - iris-thaumantias/src/provider/chatWebviewProvider.ts
  - iris-thaumantias/src/views/app/commands/authCommands.ts
  - iris-thaumantias/src/views/app/commands/healthCommands.ts
  - iris-thaumantias/src/views/app/commands/navigationCommands.ts
  - iris-thaumantias/src/views/app/commands/repositoryCommands.ts
  - iris-thaumantias/src/views/app/commands/utilityCommands.ts
autonomous: true
gap_closure: true
requirements:
  - TYPE-01

must_haves:
  truths:
    - "npx tsc --noEmit exits with zero errors for all src/ files"
    - "Provider message sending uses correct payload types with undefined handling"
    - "All command handler message types match ExtensionToWebviewMessage union members"
  artifacts:
    - path: "iris-thaumantias/src/provider/artemisWebviewProvider.ts"
      provides: "Type-safe message sending with proper CourseDashboardEntry handling"
    - path: "iris-thaumantias/src/provider/chatWebviewProvider.ts"
      provides: "Type-safe chat message payloads"
  key_links:
    - from: "commands/*.ts"
      to: "messageContracts.ts"
      via: "getPayload<T> generic constraint"
      pattern: "getPayload<.*Command>"
    - from: "artemisWebviewProvider.ts"
      to: "messageContracts.ts"
      via: "ExtensionToWebviewMessage union type"
      pattern: "postMessage.*type:"
---

<objective>
Fix all remaining TypeScript compilation errors in provider and command handler files to achieve zero-error compilation (TYPE-01 requirement).

Purpose: Plan 12-09 fixed foundational type exports and message contracts. This plan uses those foundations to resolve the remaining ~35 compilation errors in providers (16 errors) and command handlers (19 errors). Together with Plan 12-09, this achieves zero `npx tsc --noEmit` errors.

Output: `npx tsc --noEmit` exits with zero errors.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typescript-strict-mode/12-09-SUMMARY.md
@.planning/phases/12-typescript-strict-mode/12-06-SUMMARY.md
@.planning/phases/12-typescript-strict-mode/12-08-SUMMARY.md

<interfaces>
<!-- Error patterns remaining after Plan 12-09 fixes foundations -->

Provider errors (~16 total):
- artemisWebviewProvider.ts (14 errors):
  - CourseDashboardEntry vs CourseData type mismatches in message sending
  - undefined handling: CourseDetailData | undefined sent where CourseDetailData expected
  - ExerciseDetailsResponse | ExamExerciseData | undefined sent where defined types expected
  - ArtemisUser type mismatch (should resolve after 12-09 duplicate fix)
  - RecommendedExtensionCategory isInstalled: boolean | undefined vs boolean
- chatWebviewProvider.ts (2 errors):
  - Line 412: payload fields (context, itemId) don't match expected { text?: string }
  - Line 471: sessionId type mismatch (string vs number)

Command handler errors (~19 total):
- authCommands.ts (2 errors): ArtemisUser model vs apiResponses + wrong message type literal
- healthCommands.ts (1 error): wrong message type literal "healthCheckResults"
- navigationCommands.ts (5 errors): ExerciseDetail import (should resolve after 12-09), getPayload constraint, type assignments
- repositoryCommands.ts (12 errors): getPayload constraint failures (should resolve after 12-09 adds union members), null vs undefined, message type literals
- utilityCommands.ts (11 errors): getPayload constraint failures (should resolve after 12-09)

Note: Many command handler errors should auto-resolve once Plan 12-09 adds the missing union members. This plan handles the remaining errors.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix provider compilation errors</name>
  <files>
    iris-thaumantias/src/provider/artemisWebviewProvider.ts
    iris-thaumantias/src/provider/chatWebviewProvider.ts
  </files>
  <action>
Fix all TypeScript compilation errors in the two provider files (~16 errors).

**artemisWebviewProvider.ts (14 errors):**
1. Fix CourseDashboardEntry to CourseData type mismatches: The provider stores CourseDashboardEntry[] but sends messages expecting CourseData[]. Either map the data to match CourseData, or update the message contract to accept CourseDashboardEntry.
2. Fix `| undefined` mismatches: Add null/undefined guards before sending messages with CourseDetailData, ExerciseDetailsResponse payloads. Use early returns or conditional sends.
3. Fix ArtemisUser type mismatch (line 762): After Plan 12-09 removes the duplicate, the import path should resolve to the single canonical type. Verify the import is correct.
4. Fix RecommendedExtensionCategory isInstalled `boolean | undefined` vs `boolean`: Either make the message contract field optional or add `?? false` default.

**chatWebviewProvider.ts (2 errors):**
1. Fix line 412: The message payload fields (context, itemId, etc.) don't match the expected `{ text?: string }`. Check the message type being constructed and align with messageContracts.
2. Fix line 471: sessionId type mismatch (string vs number). Check if the session ID should be a number in the payload interface.

Run `npx tsc --noEmit 2>&1 | grep "src/provider/" | wc -l` to verify these files have zero errors.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit 2>&1 | grep -c "src/provider/" | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Zero TypeScript errors in artemisWebviewProvider.ts and chatWebviewProvider.ts. Provider message sending uses correct payload types with proper undefined handling.</done>
</task>

<task type="auto">
  <name>Task 2: Fix command handler compilation errors and verify full compilation</name>
  <files>
    iris-thaumantias/src/views/app/commands/authCommands.ts
    iris-thaumantias/src/views/app/commands/healthCommands.ts
    iris-thaumantias/src/views/app/commands/navigationCommands.ts
    iris-thaumantias/src/views/app/commands/repositoryCommands.ts
    iris-thaumantias/src/views/app/commands/utilityCommands.ts
  </files>
  <action>
Fix all remaining TypeScript compilation errors in command handler files (~19 errors, many should auto-resolve from Plan 12-09).

1. **Verify Plan 12-09 resolved getPayload constraint errors**: After 12-09 added missing union members, most getPayload TS2344 errors in repositoryCommands and utilityCommands should be gone. Run `npx tsc --noEmit` and check remaining errors.

2. **Fix any remaining errors**:
   - authCommands.ts: Fix ArtemisUser type alignment (should resolve from 12-09 duplicate fix) + fix message type literal.
   - healthCommands.ts: Fix "healthCheckResults" message type literal (should exist in union after 12-09).
   - navigationCommands.ts: Fix ExerciseDetail import (should be exported after 12-09), fix remaining getPayload constraint, fix CourseDashboardCourse/ExamExerciseData type assignments.
   - repositoryCommands.ts: Fix null vs undefined handling for string properties, fix any remaining message type literals.
   - utilityCommands.ts: Fix any remaining inline type arguments.

3. **Explicit verification**: After fixing all command handlers, verify each command handler file imports and references the union member types added in Plan 12-09 — not inline type literals. Check:
   - Every `getPayload<X>` call: X must be an imported type from messageContracts.ts, not an inline `{ type: ...; payload: ... }` literal.
   - Every `postMessage({ type: '...' })` call: the type string must match an ExtensionToWebviewMessage union member's `type` field.

Run `npx tsc --noEmit` — target zero errors across entire codebase.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx tsc --noEmit 2>&1 | grep -c "error TS" | grep -q "^0$" && echo "PASS: Zero TS errors" || (npx tsc --noEmit 2>&1 | grep "error TS" | wc -l; echo "FAIL")</automated>
  </verify>
  <done>npx tsc --noEmit exits with zero errors. All 46 compilation errors resolved. All command handlers use imported union member types (not inline type literals). TYPE-01 requirement fully satisfied.</done>
</task>

</tasks>

<verification>
```bash
# Full TypeScript compilation check
cd iris-thaumantias && npx tsc --noEmit
# Expected: No output (zero errors)

# Verify command handlers use imported types, not inline literals
grep -rn "getPayload<{" src/views/app/commands/ --include="*.ts" | wc -l
# Expected: 0 (all getPayload calls use named union member types)

# Verify no new @ts-ignore or @ts-expect-error added
grep -rn "@ts-ignore\|@ts-expect-error" src/ --include="*.ts" --include="*.tsx" | grep -v "node_modules" | wc -l
# Expected: Same count as before (6 ESM suppressions only)
```
</verification>

<success_criteria>
- `npx tsc --noEmit` exits with code 0 (zero compilation errors)
- No new `@ts-ignore` or `@ts-expect-error` comments added
- All command handler getPayload calls reference named union member types from messageContracts.ts
- All postMessage type strings match ExtensionToWebviewMessage union members
- Provider undefined handling uses guards (early returns or conditionals), not type assertions
</success_criteria>

<output>
After completion, create `.planning/phases/12-typescript-strict-mode/12-13-SUMMARY.md`
</output>
