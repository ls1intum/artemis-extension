---
phase: 12-typescript-strict-mode
plan: 05
type: execute
wave: 2
depends_on: [12-04]
files_modified:
  - iris-thaumantias/src/provider/artemisWebviewProvider.ts
  - iris-thaumantias/src/provider/chatWebviewProvider.ts
  - iris-thaumantias/src/views/app/appStateManager.ts
  - iris-thaumantias/src/views/app/viewActionService.ts
  - iris-thaumantias/src/views/app/webViewMessageHandler.ts
  - iris-thaumantias/src/services/sessionManagementService.ts
  - iris-thaumantias/src/services/websocketMessageHandler.ts
  - iris-thaumantias/src/services/exerciseRegistry.ts
  - iris-thaumantias/src/services/fileMonitorService.ts
  - iris-thaumantias/src/services/chatDiagnosticsService.ts
  - iris-thaumantias/src/services/chatMessageService.ts
  - iris-thaumantias/src/services/irisSessionManager.ts
  - iris-thaumantias/src/services/contextStore.ts
  - iris-thaumantias/src/extension.ts
autonomous: true
requirements: [TYPE-03]
gap_closure: true

must_haves:
  truths:
    - "Zero explicit any types in provider files (artemisWebviewProvider.ts, chatWebviewProvider.ts)"
    - "Zero explicit any types in service files (all src/services/*.ts)"
    - "Zero explicit any types in appStateManager.ts, viewActionService.ts, extension.ts"
    - "ESLint passes on all files in this plan's scope with zero no-explicit-any or no-unsafe-* errors"
  artifacts:
    - path: "iris-thaumantias/src/provider/artemisWebviewProvider.ts"
      provides: "Fully typed webview provider with no any types"
    - path: "iris-thaumantias/src/provider/chatWebviewProvider.ts"
      provides: "Fully typed chat provider with no any types"
    - path: "iris-thaumantias/src/services/sessionManagementService.ts"
      provides: "Typed session management with domain types"
  key_links:
    - from: "src/provider/artemisWebviewProvider.ts"
      to: "src/types/apiResponses.ts"
      via: "Domain type imports for state variables"
      pattern: "import.*apiResponses"
    - from: "src/services/websocketMessageHandler.ts"
      to: "src/shared/messageContracts.ts"
      via: "WebSocket message typing"
      pattern: "import.*messageContracts"
---

<objective>
Eliminate all explicit `any` types from extension host providers, state management, and service files (390 ESLint errors).

Purpose: With compilation errors fixed (Plan 12-04), systematically replace every `any` type in the non-command extension host layer with specific domain types. This covers providers (243 errors), state management (13 errors), and services (134 errors).

Output: Zero ESLint no-explicit-any and no-unsafe-* errors in providers, services, appStateManager, viewActionService, and extension.ts.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typescript-strict-mode/12-04-SUMMARY.md
@iris-thaumantias/src/types/apiResponses.ts
@iris-thaumantias/src/shared/messageContracts.ts
@iris-thaumantias/src/provider/artemisWebviewProvider.ts
@iris-thaumantias/src/provider/chatWebviewProvider.ts
@iris-thaumantias/src/views/app/appStateManager.ts
@iris-thaumantias/src/services/sessionManagementService.ts
@iris-thaumantias/src/services/websocketMessageHandler.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminate any from providers and state management</name>
  <files>
    iris-thaumantias/src/provider/artemisWebviewProvider.ts
    iris-thaumantias/src/provider/chatWebviewProvider.ts
    iris-thaumantias/src/views/app/appStateManager.ts
    iris-thaumantias/src/views/app/viewActionService.ts
    iris-thaumantias/src/views/app/webViewMessageHandler.ts
    iris-thaumantias/src/extension.ts
  </files>
  <action>
    **Eliminate ~258 ESLint errors across 6 files.**

    **Systematic approach for each file:**
    1. Run `npx eslint {file} 2>&1 | head -80` to see all errors
    2. Identify root `any` types (explicit `: any` annotations)
    3. Replace with specific types from apiResponses.ts and messageContracts.ts
    4. Fix cascading no-unsafe-* errors (they disappear when root any is fixed)

    **artemisWebviewProvider.ts (171 errors — largest file):**
    Common patterns to fix:
    - `private _examData: any` → `private _examData: StudentExam | undefined`
    - `private _courseData: any` → use CourseDetailData from apiResponses
    - `(message: any)` in onDidReceiveMessage → use type guard `isWebviewMessage(message)`
    - `catch (error: any)` → `catch (error: unknown)` then narrow with `error instanceof Error`
    - Service method calls returning any → type the return value
    - Property access on any-typed state variables → fix the variable type (cascading fix)

    **chatWebviewProvider.ts (72 errors):**
    Same patterns as artemisWebviewProvider:
    - Type state variables with chat domain types (IrisChatSession, IrisChatMessage from apiResponses)
    - Type message handlers with WebviewToExtensionMessage type guards
    - Fix catch blocks: `catch (error: unknown)` + `error instanceof Error ? error.message : String(error)`

    **appStateManager.ts (11 errors):**
    Continuation of 12-02 work. Fix remaining method parameters:
    - `showCourseList(data: any)` → use proper type
    - `setCourseData(data: any)` → `setCourseData(data: CourseDetailData)`
    - `setExerciseData(data: any)` → `setExerciseData(data: ExerciseDetailsResponse)`
    - `setExamData(data: any)` → `setExamData(data: StudentExam)`
    - Other setter methods — match parameter types to their corresponding state property types

    **viewActionService.ts (2 errors), webViewMessageHandler.ts (0-1 error), extension.ts (2 errors):**
    Small fixes — likely catch blocks or callback parameters.

    **Key patterns:**
    - VS Code API boundaries (`onDidReceiveMessage`): Use type guard immediately after receiving message. The `any` parameter is unavoidable at the VS Code boundary but should be narrowed in the first line.
    - Catch blocks: ALWAYS use `catch (error: unknown)` per TypeScript strict mode. Narrow with `error instanceof Error`.
    - JSON.parse results: Type with `as T` + comment explaining the assertion, or use a type guard.
    - Third-party API responses: Use domain types from apiResponses.ts. If shape is unknown, use `Record<string, unknown>` with property checks.

    **Verify after task:** `npx eslint src/provider/ src/views/app/appStateManager.ts src/views/app/viewActionService.ts src/views/app/webViewMessageHandler.ts src/extension.ts --max-warnings 0` should show significantly reduced errors (target: 0).
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/provider/artemisWebviewProvider.ts src/provider/chatWebviewProvider.ts src/views/app/appStateManager.ts src/views/app/viewActionService.ts src/extension.ts 2>&1 | tail -3</automated>
  </verify>
  <done>Zero explicit any types in providers, appStateManager, viewActionService, and extension.ts. All ESLint no-explicit-any and no-unsafe-* errors eliminated in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate any from all service files</name>
  <files>
    iris-thaumantias/src/services/sessionManagementService.ts
    iris-thaumantias/src/services/websocketMessageHandler.ts
    iris-thaumantias/src/services/exerciseRegistry.ts
    iris-thaumantias/src/services/fileMonitorService.ts
    iris-thaumantias/src/services/chatDiagnosticsService.ts
    iris-thaumantias/src/services/chatMessageService.ts
    iris-thaumantias/src/services/irisSessionManager.ts
    iris-thaumantias/src/services/contextStore.ts
  </files>
  <action>
    **Eliminate ~134 ESLint errors across 8 service files.**

    **Per-file approach (process files largest-first):**

    **sessionManagementService.ts (37 errors):**
    - Type session data with proper interfaces
    - Fix API response handling — use domain types from apiResponses.ts
    - Fix catch blocks to use `unknown` instead of `any`
    - Type callback parameters explicitly

    **websocketMessageHandler.ts (36 errors):**
    - Type WebSocket message data using messageContracts.ts types
    - Use discriminated union for message.type narrowing
    - Type the WebSocket `onmessage` event data properly
    - Fix any in JSON.parse results with proper type narrowing

    **exerciseRegistry.ts (26 errors):**
    - Type exercise data with ExerciseDetail or ExerciseDetailsResponse
    - Fix map/filter/find callback parameter types
    - Type registry storage with proper collection types

    **fileMonitorService.ts (11 errors):**
    - Type file watcher callbacks
    - Fix VS Code API event handler types

    **chatDiagnosticsService.ts (9 errors), chatMessageService.ts (8 errors):**
    - Type chat-related data with IrisChatSession, IrisChatMessage from apiResponses
    - Fix service method parameters and return types

    **irisSessionManager.ts (3 errors), contextStore.ts (4 errors):**
    - Small fixes — likely catch blocks or a few untyped parameters

    **Common patterns across all services:**
    1. `catch (error: any)` → `catch (error: unknown)` + `error instanceof Error ? error.message : String(error)`
    2. API response data → Use types from apiResponses.ts
    3. JSON.parse results → Type assertion with justification comment
    4. Event handler callbacks → Use VS Code event types (e.g., `vscode.TextDocumentChangeEvent`)
    5. Map/filter callbacks → Add explicit parameter types

    **Verify after task:** `npx eslint src/services/ --max-warnings 0` should show 0 errors.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/services/ 2>&1 | tail -3</automated>
  </verify>
  <done>Zero explicit any types in all service files. All ESLint no-explicit-any and no-unsafe-* errors eliminated in src/services/.</done>
</task>

</tasks>

<verification>
```bash
# Zero ESLint errors in providers + services + state management
cd iris-thaumantias && npx eslint src/provider/ src/services/ src/views/app/appStateManager.ts src/views/app/viewActionService.ts src/views/app/webViewMessageHandler.ts src/extension.ts 2>&1 | tail -5

# TypeScript compilation still passes
cd iris-thaumantias && npx tsc --noEmit
echo "Exit code: $?"

# Count remaining any in these files
grep -rn "\bany\b" src/provider/ src/services/ src/views/app/appStateManager.ts src/views/app/viewActionService.ts src/extension.ts --include="*.ts" | grep -v "// " | wc -l
```
</verification>

<success_criteria>
- Zero ESLint errors in src/provider/, src/services/, appStateManager, viewActionService, extension.ts
- No new @ts-ignore or @ts-expect-error comments
- TypeScript compilation still passes (zero errors maintained from Plan 04)
- All catch blocks use `catch (error: unknown)` pattern
- VS Code API boundaries documented with comments where any is narrowed
</success_criteria>

<output>
After completion, create `.planning/phases/12-typescript-strict-mode/12-05-SUMMARY.md`
</output>
