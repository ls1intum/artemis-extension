---
phase: 12-typescript-strict-mode
plan: 12
type: execute
wave: 2
depends_on: [12-09]
files_modified:
  - iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx
  - iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamConduction/ExamConductionView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamExerciseDetail/ExamExerciseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamStart/ExamStartView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.tsx
  - iris-thaumantias/src/views/webview/react/views/Login/LoginView.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/CodeBlock.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/ContextSelector.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/MessageBubble.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/ThinkingIndicator.tsx
autonomous: true
gap_closure: true
requirements:
  - TYPE-02
  - TYPE-03

must_haves:
  truths:
    - "All React view payload access uses specific typed interfaces (not Record<string, unknown>)"
    - "npx eslint src/views/webview/react/views/ exits with zero type safety errors"
    - "IrisChat component errors resolved (CodeBlock, ContextSelector, MessageBubble, ThinkingIndicator)"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/views/"
      provides: "All React views with zero ESLint type errors"
  key_links:
    - from: "React views"
      to: "messageContracts.ts"
      via: "Typed payload interfaces"
      pattern: "payload as.*Interface"
---

<objective>
Eliminate all ESLint type safety errors in the 13 React view files (~25 errors).

Purpose: React views currently use `Record<string, unknown>` payload casts instead of specific interfaces, causing unsafe member access violations. Plan 12-09 improves message contracts. This plan uses those improved types (or creates local payload interfaces) to make all React views fully type-safe.

Output: `npx eslint src/views/webview/react/views/` exits with zero errors.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typescript-strict-mode/12-07-SUMMARY.md
@.planning/phases/12-typescript-strict-mode/12-09-SUMMARY.md

<interfaces>
<!-- Error breakdown for remaining ~25 ESLint errors in React views -->

React views (~25 errors across 13 files):
Deferred from Plan 12-07. Errors are all unsafe payload field access in views:
- CourseDetailView: ~6 errors (hideDeveloperTools, maxPoints access)
- CourseListView: ~2 errors (courses array typing)
- DashboardView: ~4 errors (exercise payload)
- ExamConductionView: ~6 errors (exam context fields)
- ExamExerciseDetailView: ~5 errors (exam exercise fields)
- ExamStartView, ExerciseDetailView, ProblemStatement, LoginView: ~3-4 errors each
- IrisChat components (CodeBlock, ContextSelector, MessageBubble, ThinkingIndicator): ~1 error each

Root cause: Views use `Record<string, unknown>` payload casts instead of specific interfaces.
Fix: Use improved types from Plan 12-09 messageContracts or create local payload interfaces.

Patterns to apply:
1. For React views: Replace `Record<string, unknown>` casts with specific payload interfaces
2. For IrisChat components: Check if error is callback type, prop type, or payload access and fix accordingly
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix main view payload typing (9 view files)</name>
  <files>
    iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx
    iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamConduction/ExamConductionView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamExerciseDetail/ExamExerciseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamStart/ExamStartView.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.tsx
    iris-thaumantias/src/views/webview/react/views/Login/LoginView.tsx
  </files>
  <action>
Eliminate all ESLint type safety errors in the 9 main React view files (~21 errors).

For each view, replace `Record<string, unknown>` payload casts with specific typed interfaces:

1. **Import payload types from messageContracts.ts** where Plan 12-09 added them. If the specific payload interface is not available, create a local payload interface in the view file matching the fields accessed.

2. **For each view's message handler**, change:
   ```typescript
   // Before (unsafe):
   const data = message.data as Record<string, unknown>;
   const courses = data.courses; // ESLint: no-unsafe-member-access

   // After (safe):
   interface CourseListPayload { courses: CourseDashboardEntry[]; }
   const data = message.data as CourseListPayload;
   const courses = data.courses; // Clean
   ```

3. **Specific views:**
   - CourseDetailView (~6 errors): Type hideDeveloperTools, maxPoints, and other course detail fields
   - CourseListView (~2 errors): Type courses array with CourseDashboardEntry[]
   - DashboardView (~4 errors): Type exercise payload fields
   - ExamConductionView (~6 errors): Type exam context fields
   - ExamExerciseDetailView (~5 errors): Type exam exercise fields
   - ExamStartView, ExerciseDetailView, ProblemStatement, LoginView (~3-4 errors each): Type respective payloads

Run `npx eslint src/views/webview/react/views/CourseDetail/ src/views/webview/react/views/CourseList/ src/views/webview/react/views/Dashboard/ src/views/webview/react/views/ExamConduction/ src/views/webview/react/views/ExamExerciseDetail/ src/views/webview/react/views/ExamStart/ src/views/webview/react/views/ExerciseDetail/ src/views/webview/react/views/Login/` — target zero errors.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/views/webview/react/views/CourseDetail/ src/views/webview/react/views/CourseList/ src/views/webview/react/views/Dashboard/ src/views/webview/react/views/ExamConduction/ src/views/webview/react/views/ExamExerciseDetail/ src/views/webview/react/views/ExamStart/ src/views/webview/react/views/ExerciseDetail/ src/views/webview/react/views/Login/ 2>&1 | tail -1</automated>
  </verify>
  <done>Zero ESLint errors in all 9 main React view files. All payload access uses specific typed interfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Fix IrisChat component errors (4 files)</name>
  <files>
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/CodeBlock.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/ContextSelector.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/MessageBubble.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/ThinkingIndicator.tsx
  </files>
  <action>
Eliminate all ESLint type safety errors in the 4 IrisChat component files (~4 errors, ~1 each).

For each component, check what the error is (callback type, prop type, or payload access) and fix accordingly:
- CodeBlock: Likely Shiki-related typing or callback type
- ContextSelector: Likely selection handler or context data typing
- MessageBubble: Likely message content prop typing
- ThinkingIndicator: Likely animation/state prop typing

Apply React-specific event types where needed (React.MouseEvent, React.ChangeEvent, etc.).

Run `npx eslint src/views/webview/react/views/` — target zero errors across all view files.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/views/webview/react/views/ 2>&1 | tail -1 | grep -q "^$\|0 problems" && echo "PASS" || (npx eslint src/views/webview/react/views/ 2>&1 | tail -3; echo "FAIL")</automated>
  </verify>
  <done>Zero ESLint errors in all 13 React view files including IrisChat components. All view-layer type safety errors eliminated.</done>
</task>

</tasks>

<verification>
```bash
# Full React views ESLint check
cd iris-thaumantias && npx eslint src/views/webview/react/views/
# Expected: No errors

# Verify no Record<string, unknown> payload casts remain in views
grep -rn "as Record<string, unknown>" src/views/webview/react/views/ --include="*.ts" --include="*.tsx" | wc -l
# Expected: 0

# Verify no explicit any in React views
grep -rn ": any\|as any\|<any>" src/views/webview/react/views/ --include="*.ts" --include="*.tsx" | grep -v "eslint-disable" | wc -l
# Expected: 0

# TypeScript compilation still passes
npx tsc --noEmit 2>&1 | grep "src/views/webview/react/views/" | wc -l
# Expected: 0
```
</verification>

<success_criteria>
- `npx eslint src/views/webview/react/views/` exits with zero errors
- Zero `Record<string, unknown>` payload casts in React views
- Zero explicit `any` types in React views
- `npx tsc --noEmit` reports no new errors from React view changes
- All view payload access uses specific typed interfaces
</success_criteria>

<output>
After completion, create `.planning/phases/12-typescript-strict-mode/12-12-SUMMARY.md`
</output>
