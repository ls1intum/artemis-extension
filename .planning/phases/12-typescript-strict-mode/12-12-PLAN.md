---
phase: 12-typescript-strict-mode
plan: 12
type: execute
wave: 2
depends_on: [12-09]
files_modified:
  - iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx
  - iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamConduction/ExamConductionView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamExerciseDetail/ExamExerciseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamStart/ExamStartView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.tsx
  - iris-thaumantias/src/views/webview/react/views/Login/LoginView.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/CodeBlock.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/ContextSelector.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/MessageBubble.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/ThinkingIndicator.tsx
  - iris-thaumantias/src/views/webview/react/hooks/useExamTimer.ts
  - iris-thaumantias/src/views/webview/react/hooks/useStreamingMessage.ts
  - iris-thaumantias/src/views/webview/react/hooks/useWebSocketUpdates.ts
  - iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts
  - iris-thaumantias/src/views/webview/react/components/Button/Button.tsx
  - iris-thaumantias/src/views/webview/react/components/TextInput/TextInput.tsx
  - iris-thaumantias/src/views/webview/react/components/ServiceHealth/ServiceHealth.tsx
  - iris-thaumantias/src/views/webview/react/components/HelpPopup/HelpPopup.tsx
  - iris-thaumantias/src/views/webview/react/components/List/List.tsx
  - iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx
  - iris-thaumantias/src/api/artemisApi.ts
  - iris-thaumantias/src/auth/auth.ts
  - iris-thaumantias/src/utils/problemStatementProcessor.ts
  - iris-thaumantias/src/utils/workspaceFileChecker.ts
  - iris-thaumantias/src/types/stomp.d.ts
autonomous: true
gap_closure: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03

must_haves:
  truths:
    - "npx eslint src/ exits with zero errors"
    - "npx tsc --noEmit exits with zero errors"
    - "Zero explicit any types remain in entire codebase (except justified library boundaries)"
    - "All React view payload access uses specific typed interfaces"
    - "npm run lint passes with zero errors"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/views/"
      provides: "All React views with zero ESLint type errors"
    - path: "iris-thaumantias/src/api/artemisApi.ts"
      provides: "Type-safe API client"
    - path: "iris-thaumantias/src/auth/auth.ts"
      provides: "Type-safe auth module"
  key_links:
    - from: "React views"
      to: "messageContracts.ts"
      via: "Typed payload interfaces"
      pattern: "payload as.*Interface"
    - from: "artemisApi.ts"
      to: "types/apiResponses.ts"
      via: "Typed API responses"
      pattern: "import.*from.*apiResponses"
---

<objective>
Eliminate all remaining ESLint type safety errors across the codebase (93 errors: 37 React views + 56 in api/auth/utils/types).

Purpose: This is the final cleanup plan. After Plans 12-09 (compilation), 12-10 (services), and 12-11 (commands) complete their respective domains, this plan fixes the remaining scattered errors across React views, API client, auth module, utility files, and type declarations. Achieving zero ESLint errors completes TYPE-03 and the entire Phase 12 goal.

Output: `npm run lint` passes with zero errors. Phase 12 goal achieved.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typescript-strict-mode/12-07-SUMMARY.md

<interfaces>
<!-- Error breakdown for remaining 93 ESLint errors -->

React views (37 errors across 13+ files):
Deferred from Plan 12-07. Errors are all unsafe payload field access in views:
- CourseDetailView: ~6 errors (hideDeveloperTools, maxPoints access)
- CourseListView: ~2 errors (courses array typing)
- DashboardView: ~4 errors (exercise payload)
- ExamConductionView: ~6 errors (exam context fields)
- ExamExerciseDetailView: ~5 errors (exam exercise fields)
- ExamStartView, ExerciseDetailView, ProblemStatement, LoginView: ~3-4 errors each
- IrisChat components (CodeBlock, ContextSelector, MessageBubble, ThinkingIndicator): ~1 error each
- Hooks (useExamTimer, useStreamingMessage, useWebSocketUpdates): ~1-2 errors each
- Stores (useCourseListStore): ~1 error
- Components (Button, TextInput, ServiceHealth, HelpPopup, List, ParticipationActions): ~1 error each

Root cause: Views use `Record<string, unknown>` payload casts instead of specific interfaces.
Fix: Plan 12-09 should improve message contracts. Use those improved types or create local payload interfaces.

API/auth/utils/types (56 errors across 5 files):
- artemisApi.ts: ~15 errors (API response typing, request parameter typing)
- auth.ts: ~12 errors (authentication flow, token handling, user data)
- problemStatementProcessor.ts: ~12 errors (HTML/DOM manipulation, content extraction)
- workspaceFileChecker.ts: ~12 errors (file system operations, workspace scanning)
- stomp.d.ts: ~5 errors (STOMP type declarations need fixes)

Patterns to apply:
1. For React views: Replace `Record<string, unknown>` casts with specific payload interfaces
2. For artemisApi.ts: Type API response generics, type request params
3. For auth.ts: Type token data, user profile, auth state
4. For utils: Type DOM manipulation with proper DOM types, file system with vscode.Uri
5. For stomp.d.ts: Fix type declaration errors (may need interface cleanup)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix React view payload typing and component errors (37 errors)</name>
  <files>
    iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx
    iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamConduction/ExamConductionView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamExerciseDetail/ExamExerciseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamStart/ExamStartView.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.tsx
    iris-thaumantias/src/views/webview/react/views/Login/LoginView.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/CodeBlock.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/ContextSelector.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/MessageBubble.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/ThinkingIndicator.tsx
    iris-thaumantias/src/views/webview/react/hooks/useExamTimer.ts
    iris-thaumantias/src/views/webview/react/hooks/useStreamingMessage.ts
    iris-thaumantias/src/views/webview/react/hooks/useWebSocketUpdates.ts
    iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts
    iris-thaumantias/src/views/webview/react/components/Button/Button.tsx
    iris-thaumantias/src/views/webview/react/components/TextInput/TextInput.tsx
    iris-thaumantias/src/views/webview/react/components/ServiceHealth/ServiceHealth.tsx
    iris-thaumantias/src/views/webview/react/components/HelpPopup/HelpPopup.tsx
    iris-thaumantias/src/views/webview/react/components/List/List.tsx
    iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx
  </files>
  <action>
Eliminate all remaining ESLint errors in the React webview layer (37 errors).

Strategy: Work through each file systematically. The errors fall into categories:

1. **Payload field access errors (views)**: Replace `Record<string, unknown>` casts with specific payload types. If Plan 12-09 added proper payload interfaces to messageContracts.ts, import and use them. Otherwise, create local payload interfaces in each view file (e.g., `interface CourseDetailPayload { hideDeveloperTools?: boolean; course: CourseData; ... }`).

2. **Component prop typing**: For Button, TextInput, ServiceHealth, etc. — check what `any` is used for. Usually it's event handler types or children prop typing. Use React's built-in types (`React.ChangeEvent<HTMLInputElement>`, `React.MouseEvent<HTMLButtonElement>`, etc.).

3. **Hook typing**: For useExamTimer, useStreamingMessage, useWebSocketUpdates — type the WebSocket/timer data with proper interfaces. Use `MessageEvent<unknown>` pattern from Plan 12-07.

4. **Store typing**: For useCourseListStore — type the store state and actions with proper course data types.

5. **Justified any in React.cloneElement**: If List.tsx still has the justified `any` for React.cloneElement, keep the eslint-disable-next-line comment (already documented in Plan 12-07).

Run `npx eslint src/views/webview/react/` — target zero errors.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/views/webview/react/ 2>&1 | grep "error" | wc -l | xargs -I {} test {} -eq 0 && echo "PASS" || (npx eslint src/views/webview/react/ 2>&1 | tail -3; echo "FAIL")</automated>
  </verify>
  <done>Zero ESLint errors in all React view, component, hook, and store files. All payload access uses specific typed interfaces. React layer fully type-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Fix api, auth, utils, and type declaration errors (56 errors)</name>
  <files>
    iris-thaumantias/src/api/artemisApi.ts
    iris-thaumantias/src/auth/auth.ts
    iris-thaumantias/src/utils/problemStatementProcessor.ts
    iris-thaumantias/src/utils/workspaceFileChecker.ts
    iris-thaumantias/src/types/stomp.d.ts
  </files>
  <action>
Eliminate all remaining ESLint errors in the api, auth, utils, and types directories (56 errors).

**artemisApi.ts (~15 errors):**
1. Type API response generics — the HTTP client likely returns `any` by default. Add generic type parameters to API calls: `get<T>(url): Promise<T>`.
2. Type request parameters and headers with proper interfaces.
3. Type error response handling with `unknown` catch blocks.
4. Import response types from types/apiResponses.ts for all API endpoints.

**auth.ts (~12 errors):**
1. Type token data: `interface TokenData { access_token: string; refresh_token: string; expires_in: number; }`.
2. Type user profile response from Artemis auth endpoint.
3. Type the credential storage using VS Code SecretStorage API types.
4. Fix catch blocks and error handling.

**problemStatementProcessor.ts (~12 errors):**
1. Type DOM manipulation — use proper DOM types: `HTMLElement`, `Element`, `NodeListOf<Element>`.
2. Type regex match results: `RegExpMatchArray | null`.
3. Type any intermediate data structures used in HTML processing.
4. Type function parameters and return types explicitly.

**workspaceFileChecker.ts (~12 errors):**
1. Type file system operations — use `vscode.Uri`, `vscode.WorkspaceFolder`.
2. Type the workspace scanning results with proper interfaces.
3. Fix catch blocks and error handling.

**stomp.d.ts (~5 errors):**
1. Fix any ESLint errors in the type declaration file itself. These are likely `no-explicit-any` in the STOMP type definitions.
2. If STOMP types genuinely need `any` at the library boundary, add eslint-disable comments with justification.
3. Alternatively, replace `any` with `unknown` or specific frame types where possible.

After fixing all files, run the full codebase ESLint check:
```bash
npx eslint src/
```

This should show zero errors since Plans 12-09 through 12-12 cover all files:
- Providers: Already 0 errors (Plan 12-05)
- Extension entry: Already 0 errors (Plan 12-05)
- State management: Already 0 errors (Plan 12-05)
- Services: Fixed in Plan 12-10
- Commands: Fixed in Plan 12-11
- React views/components: Fixed in Task 1
- API/auth/utils/types: Fixed in this task
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/ 2>&1 | tail -3 | grep -q "0 errors\|^$" && echo "PASS" || (npx eslint src/ 2>&1 | tail -5; echo "FAIL")</automated>
  </verify>
  <done>Zero ESLint errors across entire src/ directory. TYPE-03 fully satisfied. Phase 12 goal achieved: 100% type safety with zero compilation errors and zero ESLint type violations.</done>
</task>

</tasks>

<verification>
```bash
# Full codebase ESLint check
cd iris-thaumantias && npx eslint src/
# Expected: 0 errors (warnings acceptable)

# Full TypeScript compilation check
npx tsc --noEmit
# Expected: Zero errors

# Explicit any count across entire codebase
grep -rn ": any\|as any\|<any>" src/ --include="*.ts" --include="*.tsx" | grep -v "eslint-disable\|node_modules" | wc -l
# Expected: 0 (or minimal justified boundary cases)

# Full build
npm run compile
# Expected: Success

# Full test suite
npm run test
# Expected: Passes
```
</verification>

<success_criteria>
- `npx eslint src/` exits with zero errors
- `npx tsc --noEmit` exits with zero errors
- `npm run compile` succeeds
- Zero explicit `any` types in codebase (except justified eslint-disable boundary cases)
- All React view payload access uses specific typed interfaces
- All API calls use typed response generics
- All auth operations use typed token/user data
- Phase 12 requirements fully satisfied: TYPE-01 (zero TS errors), TYPE-02 (strict mode), TYPE-03 (no any)
</success_criteria>

<output>
After completion, create `.planning/phases/12-typescript-strict-mode/12-12-SUMMARY.md`
</output>
