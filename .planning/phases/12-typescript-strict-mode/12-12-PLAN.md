---
phase: 12-typescript-strict-mode
plan: 12
type: execute
wave: 2
depends_on: [12-09]
files_modified:
  - iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx
  - iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamConduction/ExamConductionView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamExerciseDetail/ExamExerciseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExamStart/ExamStartView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
  - iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.tsx
  - iris-thaumantias/src/views/webview/react/views/Login/LoginView.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/CodeBlock.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/ContextSelector.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/MessageBubble.tsx
  - iris-thaumantias/src/views/webview/react/views/IrisChat/components/ThinkingIndicator.tsx
  - iris-thaumantias/src/views/webview/react/hooks/useExamTimer.ts
  - iris-thaumantias/src/views/webview/react/hooks/useStreamingMessage.ts
  - iris-thaumantias/src/views/webview/react/hooks/useWebSocketUpdates.ts
  - iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts
  - iris-thaumantias/src/views/webview/react/components/Button/Button.tsx
  - iris-thaumantias/src/views/webview/react/components/TextInput/TextInput.tsx
  - iris-thaumantias/src/views/webview/react/components/ServiceHealth/ServiceHealth.tsx
  - iris-thaumantias/src/views/webview/react/components/HelpPopup/HelpPopup.tsx
  - iris-thaumantias/src/views/webview/react/components/List/List.tsx
  - iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx
autonomous: true
gap_closure: true
requirements:
  - TYPE-02
  - TYPE-03

must_haves:
  truths:
    - "All React view payload access uses specific typed interfaces (not Record<string, unknown>)"
    - "npx eslint src/views/webview/react/ exits with zero type safety errors"
    - "React component event handlers use React-specific event types"
    - "Hook state and callback types are explicit"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/views/"
      provides: "All React views with zero ESLint type errors"
    - path: "iris-thaumantias/src/views/webview/react/hooks/"
      provides: "Typed hooks with explicit state and callback types"
    - path: "iris-thaumantias/src/views/webview/react/components/"
      provides: "Shared components with typed props and event handlers"
  key_links:
    - from: "React views"
      to: "messageContracts.ts"
      via: "Typed payload interfaces"
      pattern: "payload as.*Interface"
    - from: "React hooks"
      to: "messageContracts.ts"
      via: "Typed WebSocket/message event data"
      pattern: "import.*from.*messageContracts"
---

<objective>
Eliminate all remaining ESLint type safety errors in the React webview layer (37 errors across 23 files).

Purpose: React views currently use `Record<string, unknown>` payload casts instead of specific interfaces, causing unsafe member access violations. Plan 12-09 improves message contracts. This plan uses those improved types (or creates local payload interfaces) to make all React views, hooks, stores, and shared components fully type-safe.

Output: `npx eslint src/views/webview/react/` exits with zero errors.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typescript-strict-mode/12-07-SUMMARY.md
@.planning/phases/12-typescript-strict-mode/12-09-SUMMARY.md

<interfaces>
<!-- Error breakdown for remaining 37 ESLint errors in React layer -->

React views (37 errors across 23 files):
Deferred from Plan 12-07. Errors are all unsafe payload field access in views:
- CourseDetailView: ~6 errors (hideDeveloperTools, maxPoints access)
- CourseListView: ~2 errors (courses array typing)
- DashboardView: ~4 errors (exercise payload)
- ExamConductionView: ~6 errors (exam context fields)
- ExamExerciseDetailView: ~5 errors (exam exercise fields)
- ExamStartView, ExerciseDetailView, ProblemStatement, LoginView: ~3-4 errors each
- IrisChat components (CodeBlock, ContextSelector, MessageBubble, ThinkingIndicator): ~1 error each
- Hooks (useExamTimer, useStreamingMessage, useWebSocketUpdates): ~1-2 errors each
- Stores (useCourseListStore): ~1 error
- Components (Button, TextInput, ServiceHealth, HelpPopup, List, ParticipationActions): ~1 error each

Root cause: Views use `Record<string, unknown>` payload casts instead of specific interfaces.
Fix: Use improved types from Plan 12-09 messageContracts or create local payload interfaces.

Patterns to apply:
1. For React views: Replace `Record<string, unknown>` casts with specific payload interfaces
2. For component event handlers: Use React.ChangeEvent<HTMLInputElement>, React.MouseEvent<HTMLButtonElement>, etc.
3. For hooks: Type WebSocket message data with MessageEvent<unknown> and narrow with type guards
4. For stores: Type store state and actions with proper domain types
5. Justified any in React.cloneElement: If List.tsx has justified `any` for React.cloneElement, keep eslint-disable-next-line comment
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix React view payload typing (13 view files)</name>
  <files>
    iris-thaumantias/src/views/webview/react/views/CourseDetail/CourseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/CourseList/CourseListView.tsx
    iris-thaumantias/src/views/webview/react/views/Dashboard/DashboardView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamConduction/ExamConductionView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamExerciseDetail/ExamExerciseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/ExamStart/ExamStartView.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/ExerciseDetailView.tsx
    iris-thaumantias/src/views/webview/react/views/ExerciseDetail/components/ProblemStatement.tsx
    iris-thaumantias/src/views/webview/react/views/Login/LoginView.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/CodeBlock.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/ContextSelector.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/MessageBubble.tsx
    iris-thaumantias/src/views/webview/react/views/IrisChat/components/ThinkingIndicator.tsx
  </files>
  <action>
Eliminate all ESLint type safety errors in the 13 React view files (~25 errors).

For each view, replace `Record<string, unknown>` payload casts with specific typed interfaces:

1. **Import payload types from messageContracts.ts** where Plan 12-09 added them. If the specific payload interface is not available, create a local payload interface in the view file matching the fields accessed.

2. **For each view's message handler**, change:
   ```typescript
   // Before (unsafe):
   const data = message.data as Record<string, unknown>;
   const courses = data.courses; // ESLint: no-unsafe-member-access

   // After (safe):
   interface CourseListPayload { courses: CourseDashboardEntry[]; }
   const data = message.data as CourseListPayload;
   const courses = data.courses; // Clean
   ```

3. **IrisChat components** (CodeBlock, ContextSelector, MessageBubble, ThinkingIndicator): These likely have 1 error each — check if it's a callback type, prop type, or payload access and fix accordingly.

Run `npx eslint src/views/webview/react/views/` — target zero errors.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/views/webview/react/views/ 2>&1 | tail -1</automated>
  </verify>
  <done>Zero ESLint errors in all 13 React view files. All payload access uses specific typed interfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Fix hook, store, and shared component errors (10 files)</name>
  <files>
    iris-thaumantias/src/views/webview/react/hooks/useExamTimer.ts
    iris-thaumantias/src/views/webview/react/hooks/useStreamingMessage.ts
    iris-thaumantias/src/views/webview/react/hooks/useWebSocketUpdates.ts
    iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts
    iris-thaumantias/src/views/webview/react/components/Button/Button.tsx
    iris-thaumantias/src/views/webview/react/components/TextInput/TextInput.tsx
    iris-thaumantias/src/views/webview/react/components/ServiceHealth/ServiceHealth.tsx
    iris-thaumantias/src/views/webview/react/components/HelpPopup/HelpPopup.tsx
    iris-thaumantias/src/views/webview/react/components/List/List.tsx
    iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx
  </files>
  <action>
Eliminate all remaining ESLint errors in hooks, stores, and shared components (~12 errors).

**Hooks (3 files, ~4 errors):**
1. useExamTimer: Type timer state and callback parameters with explicit types.
2. useStreamingMessage: Type WebSocket message data with `MessageEvent<unknown>` and narrow with type guards.
3. useWebSocketUpdates: Same pattern — type the WebSocket event data.

**Store (1 file, ~1 error):**
1. useCourseListStore: Type the store state and actions with proper course data types from types/apiResponses.ts.

**Components (6 files, ~7 errors):**
1. Button: Type event handler with `React.MouseEvent<HTMLButtonElement>`.
2. TextInput: Type onChange with `React.ChangeEvent<HTMLInputElement>`.
3. ServiceHealth: Type health status data.
4. HelpPopup: Type popup state and content.
5. List: If React.cloneElement requires `any`, keep existing eslint-disable comment (justified boundary). Otherwise fix with proper child typing.
6. ParticipationActions: Type exercise participation data.

Run `npx eslint src/views/webview/react/` — target zero errors across entire React layer.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/views/webview/react/ 2>&1 | tail -1 | grep -q "^$\|0 problems" && echo "PASS" || (npx eslint src/views/webview/react/ 2>&1 | tail -3; echo "FAIL")</automated>
  </verify>
  <done>Zero ESLint errors in all React hooks, stores, and shared components. Entire React webview layer is type-safe.</done>
</task>

</tasks>

<verification>
```bash
# Full React layer ESLint check
cd iris-thaumantias && npx eslint src/views/webview/react/
# Expected: No errors

# Verify no Record<string, unknown> payload casts remain
grep -rn "as Record<string, unknown>" src/views/webview/react/ --include="*.ts" --include="*.tsx" | wc -l
# Expected: 0

# Verify no explicit any in React layer
grep -rn ": any\|as any\|<any>" src/views/webview/react/ --include="*.ts" --include="*.tsx" | grep -v "eslint-disable" | wc -l
# Expected: 0

# TypeScript compilation still passes
npx tsc --noEmit 2>&1 | grep "src/views/webview/react/" | wc -l
# Expected: 0
```
</verification>

<success_criteria>
- `npx eslint src/views/webview/react/` exits with zero errors
- Zero `Record<string, unknown>` payload casts in React views
- Zero explicit `any` types in React layer (except justified React.cloneElement with eslint-disable)
- `npx tsc --noEmit` reports no new errors from React layer changes
- All view payload access uses specific typed interfaces
- React component event handlers use React-specific event types
</success_criteria>

<output>
After completion, create `.planning/phases/12-typescript-strict-mode/12-12-SUMMARY.md`
</output>
