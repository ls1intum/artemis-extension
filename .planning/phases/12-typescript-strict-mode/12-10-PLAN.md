---
phase: 12-typescript-strict-mode
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - iris-thaumantias/src/services/exerciseRegistry.ts
  - iris-thaumantias/src/services/fileMonitorService.ts
  - iris-thaumantias/src/services/chatDiagnosticsService.ts
  - iris-thaumantias/src/services/chatMessageService.ts
  - iris-thaumantias/src/services/chatSessionService.ts
  - iris-thaumantias/src/services/chatContextManager.ts
  - iris-thaumantias/src/services/contextStore.ts
  - iris-thaumantias/src/services/irisSessionManager.ts
  - iris-thaumantias/src/services/websocketStatusBar.ts
  - iris-thaumantias/src/services/artemisWebsocketService.ts
autonomous: true
gap_closure: true
requirements:
  - TYPE-03

must_haves:
  truths:
    - "npx eslint src/services/ exits with zero errors"
    - "Zero explicit any types remain in service files"
    - "All catch blocks use catch (error: unknown) pattern"
    - "All service methods have proper parameter and return types"
  artifacts:
    - path: "iris-thaumantias/src/services/exerciseRegistry.ts"
      provides: "Type-safe exercise registry with domain types"
    - path: "iris-thaumantias/src/services/fileMonitorService.ts"
      provides: "Type-safe file monitoring with proper event types"
    - path: "iris-thaumantias/src/services/chatDiagnosticsService.ts"
      provides: "Type-safe chat diagnostics"
    - path: "iris-thaumantias/src/services/chatMessageService.ts"
      provides: "Type-safe chat message handling"
  key_links:
    - from: "services/*.ts"
      to: "models/core.ts"
      via: "Domain type imports"
      pattern: "import.*from.*models/core"
    - from: "services/*.ts"
      to: "types/apiResponses.ts"
      via: "API response type imports"
      pattern: "import.*from.*apiResponses"
---

<objective>
Eliminate all explicit `any` types from the 10 remaining service files (72 ESLint errors).

Purpose: Plan 12-05 completed 2 of 8 service files. The remaining 10 files (exerciseRegistry, fileMonitorService, chat services, context store, websocket services) still have 72 ESLint type safety errors. These block TYPE-03 requirement completion.

Output: `npx eslint src/services/` exits with zero errors.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typescript-strict-mode/12-05-SUMMARY.md

<interfaces>
<!-- Patterns established in Plan 12-05 for service typing -->

Pattern 1 - Domain type imports:
```typescript
import { ArtemisUser, ArtemisCourse, ArtemisExercise } from '../models/core';
import { ExerciseDetailsResponse, CourseDetailData } from '../types/apiResponses';
```

Pattern 2 - Unknown-first catch blocks:
```typescript
} catch (error: unknown) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error('Operation failed:', errorMessage);
}
```

Pattern 3 - API response typing:
```typescript
// Replace: const response: any = await api.get(...)
// With: const response = await api.get<ExerciseDetailsResponse>(...)
// Or: const response = await api.get(...) as unknown as ExerciseDetailsResponse;
```

ESLint error breakdown by file (72 total):
- exerciseRegistry.ts: ~15 errors (exercise data typing, registry operations)
- artemisWebsocketService.ts: ~10 errors (WebSocket connection, STOMP message handling)
- chatSessionService.ts: ~9 errors (session management, API calls)
- chatMessageService.ts: ~8 errors (message sending, response handling)
- chatDiagnosticsService.ts: ~7 errors (health check data)
- chatContextManager.ts: ~6 errors (context data typing)
- fileMonitorService.ts: ~5 errors (file system events, watcher callbacks)
- contextStore.ts: ~4 errors (stored context data)
- websocketStatusBar.ts: ~4 errors (status bar state)
- irisSessionManager.ts: ~4 errors (session lifecycle)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type the chat services and context management (4 files)</name>
  <files>
    iris-thaumantias/src/services/chatSessionService.ts
    iris-thaumantias/src/services/chatMessageService.ts
    iris-thaumantias/src/services/chatDiagnosticsService.ts
    iris-thaumantias/src/services/chatContextManager.ts
  </files>
  <action>
Eliminate all explicit `any` types from the 4 chat-related service files (~30 errors total).

For each file, apply these patterns:
1. **Replace `any` function parameters** with specific types from models/core.ts and types/apiResponses.ts. Check what callers pass and what the function body accesses.
2. **Replace `any` API response types** with typed responses. If the Artemis API response shape isn't defined, create a minimal interface locally (e.g., `interface ChatSessionResponse { id: number; ... }`).
3. **Replace `any` in catch blocks** with `catch (error: unknown)` and narrow with `error instanceof Error`.
4. **Replace `any` in callback parameters** with proper types. For VS Code API callbacks, use the documented callback types.
5. **Replace `any` in data transformations** with proper intermediate types.

Specific focus areas:
- **chatSessionService.ts**: Session creation/retrieval API calls return chat session objects. Type these with `IrisChatSession` from existing types or create local interfaces.
- **chatMessageService.ts**: Message sending/receiving. Type with `IrisChatMessage` or equivalent. WebSocket message data should use typed STOMP frames.
- **chatDiagnosticsService.ts**: Health check responses. Type the health check result structure.
- **chatContextManager.ts**: Context payloads. Type with `ChatContextType` and related types from types/context.ts.

Run `npx eslint src/services/chatSessionService.ts src/services/chatMessageService.ts src/services/chatDiagnosticsService.ts src/services/chatContextManager.ts` — target zero errors.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/services/chatSessionService.ts src/services/chatMessageService.ts src/services/chatDiagnosticsService.ts src/services/chatContextManager.ts 2>&1 | tail -1</automated>
  </verify>
  <done>Zero ESLint errors in all 4 chat service files. All any types replaced with specific domain types. All catch blocks use unknown pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Type the remaining 6 service files</name>
  <files>
    iris-thaumantias/src/services/exerciseRegistry.ts
    iris-thaumantias/src/services/fileMonitorService.ts
    iris-thaumantias/src/services/contextStore.ts
    iris-thaumantias/src/services/irisSessionManager.ts
    iris-thaumantias/src/services/websocketStatusBar.ts
    iris-thaumantias/src/services/artemisWebsocketService.ts
  </files>
  <action>
Eliminate all explicit `any` types from the remaining 6 service files (~42 errors total).

Apply the same patterns as Task 1. Specific focus areas:

- **exerciseRegistry.ts (~15 errors)**: Exercise data storage and lookup. Replace `any` exercise parameters with `ArtemisExercise` or `ExerciseDetailsResponse`. Type the registry Map with proper generics.
- **artemisWebsocketService.ts (~10 errors)**: STOMP WebSocket client. The STOMP library may require `any` at boundaries — if genuinely unavoidable, use eslint-disable-next-line with a comment explaining the STOMP library constraint. Type STOMP frame bodies and subscription callbacks. Check `src/types/stomp.d.ts` for existing STOMP type declarations.
- **fileMonitorService.ts (~5 errors)**: File system watchers. Type with `vscode.FileSystemWatcher` callbacks and `vscode.Uri` for file paths.
- **contextStore.ts (~4 errors)**: Stored context data. Type with `ContextSnapshot`, `ActiveContext` from types/context.ts.
- **irisSessionManager.ts (~4 errors)**: Session lifecycle. Type with session ID (number) and session state interfaces.
- **websocketStatusBar.ts (~4 errors)**: Status bar item state. Type with `vscode.StatusBarItem` and connection state enum/literal types.

For the STOMP library boundary in artemisWebsocketService.ts:
- Check if `src/types/stomp.d.ts` has type declarations. If so, import and use them.
- If STOMP types are incomplete, extend the declaration file with needed interfaces rather than using `any`.
- If the STOMP library genuinely returns untyped data, use `unknown` with immediate type guards (not `any`).

Run `npx eslint src/services/` — target zero errors across ALL service files.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/services/ 2>&1 | tail -1 | grep -q "^$\|0 problems" && echo "PASS" || (npx eslint src/services/ 2>&1 | tail -3; echo "FAIL")</automated>
  </verify>
  <done>Zero ESLint errors in all service files. All 72 service-layer ESLint errors eliminated. No any types remain in src/services/.</done>
</task>

</tasks>

<verification>
```bash
# Service files ESLint check
cd iris-thaumantias && npx eslint src/services/
# Expected: No errors

# Explicit any count in services
grep -rn ": any\|as any\|<any>" src/services/ --include="*.ts" | grep -v "eslint-disable" | wc -l
# Expected: 0 (or only justified STOMP library boundary cases with eslint-disable)

# TypeScript compilation still passes
npx tsc --noEmit 2>&1 | grep "src/services/" | wc -l
# Expected: 0
```
</verification>

<success_criteria>
- `npx eslint src/services/` exits with zero errors
- Zero explicit `any` types in service files (except justified STOMP boundary with eslint-disable comment)
- `npx tsc --noEmit` reports no new errors from service changes
- All catch blocks use `catch (error: unknown)` pattern
- All service methods have typed parameters and return types
</success_criteria>

<output>
After completion, create `.planning/phases/12-typescript-strict-mode/12-10-SUMMARY.md`
</output>
