---
phase: 12-typescript-strict-mode
plan: 15
type: execute
wave: 2
depends_on: [12-09]
files_modified:
  - iris-thaumantias/src/views/webview/react/hooks/useExamTimer.ts
  - iris-thaumantias/src/views/webview/react/hooks/useStreamingMessage.ts
  - iris-thaumantias/src/views/webview/react/hooks/useWebSocketUpdates.ts
  - iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts
  - iris-thaumantias/src/views/webview/react/components/Button/Button.tsx
  - iris-thaumantias/src/views/webview/react/components/TextInput/TextInput.tsx
  - iris-thaumantias/src/views/webview/react/components/ServiceHealth/ServiceHealth.tsx
  - iris-thaumantias/src/views/webview/react/components/HelpPopup/HelpPopup.tsx
  - iris-thaumantias/src/views/webview/react/components/List/List.tsx
  - iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx
autonomous: true
gap_closure: true
requirements:
  - TYPE-03

must_haves:
  truths:
    - "npx eslint on all 10 hook/store/component files exits with zero type safety errors"
    - "React component event handlers use React-specific event types"
    - "Hook state and callback types are explicit"
    - "Store state and actions use proper domain types"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/hooks/"
      provides: "Typed hooks with explicit state and callback types"
    - path: "iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts"
      provides: "Type-safe store with domain types"
    - path: "iris-thaumantias/src/views/webview/react/components/"
      provides: "Shared components with typed props and event handlers"
  key_links:
    - from: "React hooks"
      to: "messageContracts.ts"
      via: "Typed WebSocket/message event data"
      pattern: "import.*from.*messageContracts"
    - from: "React components"
      to: "React event types"
      via: "React.ChangeEvent, React.MouseEvent"
      pattern: "React\\.(Change|Mouse|Form)Event"
---

<objective>
Eliminate all remaining ESLint type safety errors in React hooks, stores, and shared components (~12 errors across 10 files).

Purpose: These files have type safety violations in event handlers, WebSocket message data, store state types, and component props. This plan runs parallel to Plan 12-12 (React views) and together they achieve zero ESLint errors across the entire React webview layer.

Output: `npx eslint` on all 10 files exits with zero errors.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-typescript-strict-mode/12-07-SUMMARY.md
@.planning/phases/12-typescript-strict-mode/12-09-SUMMARY.md

<interfaces>
<!-- Error breakdown for ~12 ESLint errors in hooks/stores/components -->

Hooks (3 files, ~4 errors):
- useExamTimer: Timer state and callback parameters need explicit types
- useStreamingMessage: WebSocket message data typed as unknown, needs narrowing
- useWebSocketUpdates: Same pattern as useStreamingMessage

Store (1 file, ~1 error):
- useCourseListStore: Store state/actions need proper course data types from types/apiResponses.ts

Components (6 files, ~7 errors):
- Button: Event handler needs React.MouseEvent<HTMLButtonElement>
- TextInput: onChange needs React.ChangeEvent<HTMLInputElement>
- ServiceHealth: Health status data typing
- HelpPopup: Popup state and content typing
- List: Potential justified any for React.cloneElement (keep eslint-disable if so)
- ParticipationActions: Exercise participation data typing

Patterns to apply:
1. For hooks: Type WebSocket message data with MessageEvent<unknown> and narrow with type guards
2. For components: Use React.ChangeEvent<HTMLInputElement>, React.MouseEvent<HTMLButtonElement>, etc.
3. For stores: Type store state and actions with proper domain types
4. Justified any in React.cloneElement: If List.tsx has justified `any` for React.cloneElement, keep eslint-disable-next-line comment
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type hooks and store (4 files)</name>
  <files>
    iris-thaumantias/src/views/webview/react/hooks/useExamTimer.ts
    iris-thaumantias/src/views/webview/react/hooks/useStreamingMessage.ts
    iris-thaumantias/src/views/webview/react/hooks/useWebSocketUpdates.ts
    iris-thaumantias/src/views/webview/react/stores/useCourseListStore.ts
  </files>
  <action>
Eliminate all ESLint type safety errors in the 3 hook files and 1 store file (~5 errors).

**Hooks (3 files, ~4 errors):**
1. useExamTimer: Type timer state and callback parameters with explicit types. Timer callbacks should use `NodeJS.Timeout` or `ReturnType<typeof setTimeout>`. Timestamp values should be `number`.
2. useStreamingMessage: Type WebSocket message data with `MessageEvent<unknown>` and narrow with type guards before accessing fields.
3. useWebSocketUpdates: Same pattern — type the WebSocket event data with `MessageEvent<unknown>` and use type guards.

**Store (1 file, ~1 error):**
1. useCourseListStore: Type the store state and actions with proper course data types from types/apiResponses.ts. Import `CourseDashboardEntry` or equivalent for the course list.

Run `npx eslint` on all 4 files — target zero errors.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/views/webview/react/hooks/useExamTimer.ts src/views/webview/react/hooks/useStreamingMessage.ts src/views/webview/react/hooks/useWebSocketUpdates.ts src/views/webview/react/stores/useCourseListStore.ts 2>&1 | tail -1</automated>
  </verify>
  <done>Zero ESLint errors in all 3 hook files and 1 store file. Hook state and callback types are explicit. Store uses domain types.</done>
</task>

<task type="auto">
  <name>Task 2: Type shared components (6 files)</name>
  <files>
    iris-thaumantias/src/views/webview/react/components/Button/Button.tsx
    iris-thaumantias/src/views/webview/react/components/TextInput/TextInput.tsx
    iris-thaumantias/src/views/webview/react/components/ServiceHealth/ServiceHealth.tsx
    iris-thaumantias/src/views/webview/react/components/HelpPopup/HelpPopup.tsx
    iris-thaumantias/src/views/webview/react/components/List/List.tsx
    iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx
  </files>
  <action>
Eliminate all remaining ESLint errors in the 6 shared component files (~7 errors).

1. **Button**: Type event handler with `React.MouseEvent<HTMLButtonElement>`. Type onClick prop accordingly.
2. **TextInput**: Type onChange with `React.ChangeEvent<HTMLInputElement>`. Type value prop as `string`.
3. **ServiceHealth**: Type health status data with proper interface (health check results, service status).
4. **HelpPopup**: Type popup state (open/closed) and content props.
5. **List**: If React.cloneElement requires `any`, keep existing eslint-disable comment with justification (justified library boundary). Otherwise fix with proper child typing using `React.ReactElement`.
6. **ParticipationActions**: Type exercise participation data with domain types from types/apiResponses.ts.

Run `npx eslint src/views/webview/react/components/ src/views/webview/react/hooks/ src/views/webview/react/stores/` — target zero errors across all hooks, stores, and components.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npx eslint src/views/webview/react/components/ src/views/webview/react/hooks/ src/views/webview/react/stores/ 2>&1 | tail -1 | grep -q "^$\|0 problems" && echo "PASS" || (npx eslint src/views/webview/react/components/ src/views/webview/react/hooks/ src/views/webview/react/stores/ 2>&1 | tail -3; echo "FAIL")</automated>
  </verify>
  <done>Zero ESLint errors in all React hooks, stores, and shared components. All component event handlers use React-specific event types. All hooks have explicit state and callback types.</done>
</task>

</tasks>

<verification>
```bash
# Hooks/stores/components ESLint check
cd iris-thaumantias && npx eslint src/views/webview/react/hooks/ src/views/webview/react/stores/ src/views/webview/react/components/
# Expected: No errors

# Verify no explicit any in these files
grep -rn ": any\|as any\|<any>" src/views/webview/react/hooks/ src/views/webview/react/stores/ src/views/webview/react/components/ --include="*.ts" --include="*.tsx" | grep -v "eslint-disable" | wc -l
# Expected: 0

# TypeScript compilation still passes
npx tsc --noEmit 2>&1 | grep "src/views/webview/react/\(hooks\|stores\|components\)/" | wc -l
# Expected: 0
```
</verification>

<success_criteria>
- `npx eslint` on all 10 files exits with zero errors
- Zero explicit `any` types (except justified React.cloneElement with eslint-disable)
- `npx tsc --noEmit` reports no new errors from these changes
- React component event handlers use React-specific event types
- Hook state and callback types are explicit
- Store state uses proper domain types
</success_criteria>

<output>
After completion, create `.planning/phases/12-typescript-strict-mode/12-15-SUMMARY.md`
</output>
