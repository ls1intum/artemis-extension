---
phase: 03-simple-views-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - iris-thaumantias/src/shared/messageContracts.ts
  - iris-thaumantias/src/views/webview/react/App.tsx
  - iris-thaumantias/src/views/app/viewRouter.ts
  - iris-thaumantias/src/views/webview/react/views/RecommendedExtensions/RecommendedExtensionsView.tsx
  - iris-thaumantias/src/views/webview/react/views/RecommendedExtensions/types.ts
  - iris-thaumantias/src/views/webview/react/views/RecommendedExtensions/index.ts
  - iris-thaumantias/src/views/webview/react/views/index.ts
autonomous: true
requirements:
  - VIEW-01
  - MSG-02

must_haves:
  truths:
    - "RecommendedExtensions view renders through React instead of HTML string generation"
    - "Category filter selection persists across tab hide/show cycles"
    - "Extension cards are composed from Phase 2 shared components (ListItem, Badge, Button)"
    - "Clicking 'View in Marketplace' opens the VS Code extension marketplace search"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/views/RecommendedExtensions/RecommendedExtensionsView.tsx"
      provides: "React RecommendedExtensions view component"
      contains: "RecommendedExtensionsView"
    - path: "iris-thaumantias/src/views/webview/react/views/RecommendedExtensions/types.ts"
      provides: "RecommendedExtensions view message types"
      contains: "RecommendedExtensionsInitMessage"
  key_links:
    - from: "iris-thaumantias/src/views/webview/react/App.tsx"
      to: "iris-thaumantias/src/views/webview/react/views/RecommendedExtensions/RecommendedExtensionsView.tsx"
      via: "data-view='recommendedExtensions' routing"
      pattern: "case.*recommendedExtensions"
    - from: "iris-thaumantias/src/views/webview/react/views/RecommendedExtensions/RecommendedExtensionsView.tsx"
      to: "extension host"
      via: "searchMarketplace command"
      pattern: "searchMarketplace"
---

<objective>
Migrate the RecommendedExtensions view to React, adding client-side category filtering with persisted filter selection. This view validates UI state persistence (MSG-02) with the category filter and demonstrates composing extension cards from Phase 2 shared components.

Purpose: RecommendedExtensions has client-side filtering logic, making it the right view to validate UI state persistence beyond form inputs. The extension cards are composed from ListItem, Badge, and Button components — testing Phase 2 component composition in a real view.

Output: Working RecommendedExtensions React view with category filtering, persisted filter state, and marketplace integration.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-simple-views-migration/03-RESEARCH.md
@.planning/phases/03-simple-views-migration/03-CONTEXT.md
@.planning/phases/03-simple-views-migration/03-01-SUMMARY.md
@.planning/phases/03-simple-views-migration/03-02-SUMMARY.md

@iris-thaumantias/src/shared/messageContracts.ts
@iris-thaumantias/src/views/webview/react/App.tsx
@iris-thaumantias/src/views/app/viewRouter.ts
@iris-thaumantias/src/views/webview/react/views/index.ts
@iris-thaumantias/src/views/webview/react/components/index.ts
@iris-thaumantias/src/views/recommendedExtensions/recommendedExtensionsView.ts
@iris-thaumantias/src/utils/recommendedExtensions.ts
@iris-thaumantias/src/views/app/commands/utilityCommands.ts
@iris-thaumantias/src/provider/artemisWebviewProvider.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecommendedExtensions React view with category filtering and state persistence</name>
  <files>
    iris-thaumantias/src/shared/messageContracts.ts
    iris-thaumantias/src/views/webview/react/views/RecommendedExtensions/RecommendedExtensionsView.tsx
    iris-thaumantias/src/views/webview/react/views/RecommendedExtensions/types.ts
    iris-thaumantias/src/views/webview/react/views/RecommendedExtensions/index.ts
  </files>
  <action>
    **1. Add RecommendedExtensions message types to messageContracts.ts:**

    - Add `RecommendedExtensionsInitMessage = { type: 'recommendedExtensionsInit'; payload: { categories: Array<{ id: string; name: string; description: string; extensions: Array<{ id: string; name: string; publisher: string; version?: string; description: string; reason: string; optional?: boolean; isInstalled: boolean }> }> } }`
    - Add to `ExtensionToWebviewMessage` union
    - Add `SearchMarketplaceCommand = { type: 'command'; command: 'searchMarketplace'; payload: { extensionId: string } }` to `WebviewToExtensionMessage` union (if not already added from Plan 01 general commands)
    - Update type guard arrays

    **2. Create types.ts:**

    - Export `RecommendedExtensionsViewProps = { vscodeApi: VsCodeApi }`
    - Export `ExtensionCategory` type matching the init message payload structure
    - Export `Extension` type for individual extension data
    - Export `RecommendedExtensionsPersistedState = { selectedCategory: string }` — only the filter selection, per user decision

    **3. Create RecommendedExtensionsView.tsx** — React port of recommendedExtensionsView.ts:

    - **State:** `categories` (ExtensionCategory[], from init message), `selectedCategory` (string, 'all' default or restored from getState), `isLoaded` (boolean)
    - **State persistence:** useEffect persists `{ selectedCategory }` via `vscodeApi.setState()` whenever selectedCategory changes. Restore on mount from `vscodeApi.getState()`.
    - **Message handling:** useEffect with message listener:
      - `recommendedExtensionsInit`: set categories from payload, set isLoaded=true
      - Also handle legacy `command: 'recommendedExtensionsInit'` format for robustness
    - **Request initial data on mount:** Send `{ type: 'command', command: 'requestRecommendedExtensions' }` — the extension host's ArtemisWebviewProvider.showRecommendedExtensions() already prepares the data, so in the ready-signal handler, when state is 'recommended-extensions', send the categories data as init message.
    - **Category filtering:** Pure client-side. `selectedCategory === 'all'` shows all categories. Otherwise filter by category.id match. Filter buttons at top toggle selectedCategory state.
    - **Install/View in Marketplace:** Send `{ type: 'command', command: 'searchMarketplace', payload: { extensionId } }` — the existing UtilityCommandModule.handleSearchMarketplace already handles this, expecting `message.extensionId`. Bridge in provider: when command is searchMarketplace from React format, pass `{ command: 'searchMarketplace', extensionId: payload.extensionId }` to message handler.
    - **Back navigation:** BackLink sends `{ type: 'command', command: 'backToDashboard' }`

    **Compose from Phase 2 shared components (no view-specific CSS modules per user decision):**
    - `BackLink` for back navigation
    - `Container` for header card (title "Recommended Extensions", subtitle about improving Artemis workflow)
    - Filter buttons using `Button` components — "All categories" primary variant when selected, category buttons secondary variant, switch to primary on selection
    - Each category section as a `Container` with title and description
    - Extension cards composed from Phase 2 components. Per user decision: "cards composed from Phase 2 components (ListItem, Badge, Button) rather than recreating exact current card layout."
      - Each extension: render within a `<div>` or use component composition:
        - Extension name as heading text
        - Publisher and version info as secondary text
        - `Badge` for installed/not-installed status (variant="success" for installed, default for not installed)
        - `Badge` for optional tag if applicable
        - Description text
        - "Why we recommend it" section
        - `Button` variant="secondary" for "View in Marketplace"
    - Empty state using a `Container` with info state when no categories available

    **Layout:** BackLink at top, then header container with filter controls, then category sections each containing extension cards.

    **4. Create index.ts** barrel export:
    - `export { RecommendedExtensionsView } from './RecommendedExtensionsView'`
    - `export type { RecommendedExtensionsViewProps, RecommendedExtensionsPersistedState } from './types'`
  </action>
  <verify>
    <automated>cd iris-thaumantias && npm run check-types && npm run compile && npm run lint</automated>
    <manual>Launch extension, navigate to Recommended Extensions view, test category filter buttons, verify filter persists across tab hide/show</manual>
  </verify>
  <done>
    - RecommendedExtensionsView renders with category filtering
    - Filter selection persists across tab hide/show
    - Extension cards composed from Phase 2 shared components
    - View in Marketplace opens VS Code marketplace search
  </done>
</task>

<task type="auto">
  <name>Task 2: Register RecommendedExtensions in router and App, bridge init data</name>
  <files>
    iris-thaumantias/src/views/webview/react/App.tsx
    iris-thaumantias/src/views/app/viewRouter.ts
    iris-thaumantias/src/views/webview/react/views/index.ts
    iris-thaumantias/src/provider/artemisWebviewProvider.ts
  </files>
  <action>
    **1. Update viewRouter.ts:**
    - Add `['recommended-extensions', true]` to the `_reactViews` Map

    **2. Update App.tsx:**
    - Import `RecommendedExtensionsView` from `./views/RecommendedExtensions`
    - Add `case 'recommendedExtensions':` to the switch, rendering `<RecommendedExtensionsView vscodeApi={vscodeApi} />`

    **3. Update views/index.ts:**
    - Add `export { RecommendedExtensionsView } from './RecommendedExtensions'`

    **4. Update ArtemisWebviewProvider.ts** ready-signal handler:
    - When ready signal received and current state is 'recommended-extensions', send init message: `{ type: 'recommendedExtensionsInit', payload: { categories: this._appStateManager.recommendedExtensions } }`
    - The recommendedExtensions data is already populated in AppStateManager by `showRecommendedExtensions()` which is called by NavigationCommandModule before rendering
  </action>
  <verify>
    <automated>cd iris-thaumantias && npm run check-types && npm run compile && npm run lint</automated>
    <manual>Verify RecommendedExtensions renders through React, other views unaffected</manual>
  </verify>
  <done>
    - ViewRouter routes 'recommended-extensions' to React HTML
    - App.tsx routes 'recommendedExtensions' to RecommendedExtensionsView component
    - Init data (categories with installed status) sent after ready signal
    - All non-migrated views still use legacy HTML
  </done>
</task>

</tasks>

<verification>
1. `npm run check-types` passes with zero errors
2. `npm run compile` produces all bundles
3. `npm run lint` passes with zero errors
4. Category filter buttons toggle correctly (client-side filtering)
5. Filter selection persists across tab hide/show
6. Extension cards display using Phase 2 shared components
7. "View in Marketplace" opens VS Code extension marketplace
</verification>

<success_criteria>
- RecommendedExtensions renders through React with client-side category filtering
- Category filter selection persists across hide/show (validates MSG-02)
- Extension cards composed from Phase 2 components per user decision
- Marketplace integration works through existing UtilityCommandModule
- No regression in other views
</success_criteria>

<output>
After completion, create `.planning/phases/03-simple-views-migration/03-03-SUMMARY.md`
</output>
