---
phase: 03-simple-views-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - iris-thaumantias/src/shared/messageContracts.ts
  - iris-thaumantias/src/views/webview/react/App.tsx
  - iris-thaumantias/src/views/app/viewRouter.ts
  - iris-thaumantias/src/views/webview/react/views/ServiceStatus/ServiceStatusView.tsx
  - iris-thaumantias/src/views/webview/react/views/ServiceStatus/types.ts
  - iris-thaumantias/src/views/webview/react/views/ServiceStatus/index.ts
  - iris-thaumantias/src/views/webview/react/views/index.ts
autonomous: true
requirements:
  - VIEW-01

must_haves:
  truths:
    - "ServiceStatus view renders through React instead of HTML string generation"
    - "Health check results from extension host display correctly in React component"
    - "Back navigation returns to dashboard"
    - "ServiceHealth component from Phase 2 is reused for health check display"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/views/ServiceStatus/ServiceStatusView.tsx"
      provides: "React ServiceStatus view component"
      contains: "ServiceStatusView"
    - path: "iris-thaumantias/src/views/webview/react/views/ServiceStatus/types.ts"
      provides: "ServiceStatus view message types"
      contains: "ServiceStatusInitMessage"
  key_links:
    - from: "iris-thaumantias/src/views/webview/react/App.tsx"
      to: "iris-thaumantias/src/views/webview/react/views/ServiceStatus/ServiceStatusView.tsx"
      via: "data-view='serviceStatus' routing"
      pattern: "case.*serviceStatus"
    - from: "iris-thaumantias/src/views/app/viewRouter.ts"
      to: "iris-thaumantias/src/utils/webviewHelpers.ts"
      via: "React view detection for service-status state"
      pattern: "service-status.*true"
---

<objective>
Migrate the ServiceStatus view to React, validating component composition with the Phase 2 ServiceHealth shared component. This is a mostly-static display view with minimal state.

Purpose: ServiceStatus is the second simplest view (displays health check results, uses the ServiceHealth component for rendering). It validates that Phase 2 composite components work correctly within a migrated view.

Output: Working ServiceStatus React view reusing the Phase 2 ServiceHealth component, with typed messaging for health check requests and results.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-simple-views-migration/03-RESEARCH.md
@.planning/phases/03-simple-views-migration/03-CONTEXT.md
@.planning/phases/03-simple-views-migration/03-01-SUMMARY.md

@iris-thaumantias/src/shared/messageContracts.ts
@iris-thaumantias/src/views/webview/react/App.tsx
@iris-thaumantias/src/views/app/viewRouter.ts
@iris-thaumantias/src/views/webview/react/views/index.ts
@iris-thaumantias/src/views/webview/react/components/ServiceHealth/ServiceHealth.tsx
@iris-thaumantias/src/views/webview/react/components/index.ts
@iris-thaumantias/src/views/serviceStatus/serviceStatusView.ts
@iris-thaumantias/src/views/app/commands/healthCommands.ts
@iris-thaumantias/src/provider/artemisWebviewProvider.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ServiceStatus React view with health check messaging</name>
  <files>
    iris-thaumantias/src/shared/messageContracts.ts
    iris-thaumantias/src/views/webview/react/views/ServiceStatus/ServiceStatusView.tsx
    iris-thaumantias/src/views/webview/react/views/ServiceStatus/types.ts
    iris-thaumantias/src/views/webview/react/views/ServiceStatus/index.ts
  </files>
  <action>
    **1. Add ServiceStatus message types to messageContracts.ts:**

    - Add `ServiceStatusInitMessage = { type: 'serviceStatusInit'; payload: { serverUrl?: string } }`
    - Add `HealthCheckResultsMessage = { type: 'healthCheckResults'; payload: { results: Record<string, { status: string; message: string; endpoint: string; httpStatus: number | null; response: string | null }> } }`
    - Add these to `ExtensionToWebviewMessage` union
    - Add `PerformHealthChecksCommand = { type: 'command'; command: 'performHealthChecks'; payload: { serverUrl: string } }` to `WebviewToExtensionMessage` union
    - Update type guard arrays to include new type values

    **2. Create types.ts** with view-specific types:

    - Export `ServiceStatusViewProps = { vscodeApi: VsCodeApi }`
    - Export `HealthCheckResult = { status: 'online' | 'offline' | 'checking' | 'unknown'; message: string; endpoint: string; httpStatus: number | null; response: string | null }`
    - Export `ServiceStatusState` (minimal — only serverUrl, since user decision says minimal persistence for mostly static display)

    **3. Create ServiceStatusView.tsx** — React port of serviceStatusView.ts:

    - **State:** `serverUrl` (string, from init message or empty), `healthResults` (Record<string, HealthCheckResult>, initially all 'checking'), `isChecking` (boolean)
    - **State persistence:** Minimal per user decision. Persist only serverUrl (restore on tab return so user sees the connected server). Do NOT persist health check results (they become stale).
    - **Message handling:** useEffect with message listener:
      - `serviceStatusInit`: set serverUrl from payload, trigger health check request
      - `healthCheckResults`: update healthResults from payload, set isChecking=false
    - **Request initial data on mount:** Send `{ type: 'command', command: 'showServiceStatus' }` — no, actually the extension host already knows we're on service-status state. The view should send `{ type: 'command', command: 'performHealthChecks', payload: { serverUrl } }` once it receives the serverUrl.
    - **Refresh:** Button to re-trigger health checks sends `performHealthChecks` command
    - **Back navigation:** BackLink sends `{ type: 'command', command: 'backToDashboard' }`

    **Compose from Phase 2 shared components (no view-specific CSS modules):**
    - `BackLink` for back navigation
    - `Container` for header card (title "Service Status", subtitle "Real-time monitoring of Artemis services")
    - `Container` for server info section displaying the serverUrl in a disabled `TextInput`
    - `ServiceHealth` component from Phase 2 for health check results display — pass the health results data as props, map the results from the extension-host format to the `ServiceStatus[]` array format expected by the ServiceHealth component
    - `Button` for refresh (triggers re-check)

    **ServiceHealth integration:** The Phase 2 `ServiceHealth` component expects `services: ServiceStatus[]` where each has `{ name, status, message, endpoint?, httpStatus?, response?, lastCheck? }`. Map the `healthResults` record to this array format. The ServiceHealth component handles expandable details, status indicators, and refresh button internally.

    **Note on health checks:** The legacy serviceStatusView.ts performs health checks from the webview (client-side fetch). However, the React approach should request checks through the extension host (which has the HealthCommandModule that performs the actual checks). On mount, send the `performHealthChecks` command. The extension host's HealthCommandModule already sends back `healthCheckResults`.

    **Layout:** BackLink at top, then header container, server info container, health checks container with ServiceHealth component.

    **4. Create index.ts** barrel export:
    - `export { ServiceStatusView } from './ServiceStatusView'`
    - `export type { ServiceStatusViewProps } from './types'`
  </action>
  <verify>
    <automated>cd iris-thaumantias && npm run check-types && npm run compile && npm run lint</automated>
    <manual>Launch extension, navigate to Service Status view, verify health checks display correctly</manual>
  </verify>
  <done>
    - ServiceStatusView renders using Phase 2 ServiceHealth component
    - Health check results received from extension host via typed messages
    - Refresh button triggers new health check cycle
    - Server URL displayed in disabled TextInput
  </done>
</task>

<task type="auto">
  <name>Task 2: Register ServiceStatus in router and App</name>
  <files>
    iris-thaumantias/src/views/webview/react/App.tsx
    iris-thaumantias/src/views/app/viewRouter.ts
    iris-thaumantias/src/views/webview/react/views/index.ts
  </files>
  <action>
    **1. Update viewRouter.ts:**
    - Add `['service-status', true]` to the `_reactViews` Map (alongside the existing `['git-credentials', true]` from Plan 01)

    **2. Update App.tsx:**
    - Import `ServiceStatusView` from `./views/ServiceStatus`
    - Add `case 'serviceStatus':` to the switch statement, rendering `<ServiceStatusView vscodeApi={vscodeApi} />`

    **3. Update views/index.ts:**
    - Add `export { ServiceStatusView } from './ServiceStatus'`

    **4. Bridge health check messaging** in ArtemisWebviewProvider if needed:
    - The existing `HealthCommandModule.handlePerformHealthChecks` sends results with `command: 'healthCheckResults'`. This is the legacy format.
    - In the ready-signal handler (added in Plan 01), when the current state is 'service-status', send `{ type: 'serviceStatusInit', payload: { serverUrl } }` as the initial data.
    - The React view will then trigger `performHealthChecks` which goes through the existing HealthCommandModule.
    - The HealthCommandModule sends back `{ command: 'healthCheckResults', results: ... }`. This needs to reach the React view. In the provider's message sender, when sending to a React view, wrap the legacy `command: 'healthCheckResults'` format as `{ type: 'healthCheckResults', payload: { results } }`. Alternatively, update the ServiceStatusView to also listen for the legacy `command` format — this is simpler and avoids modifying the provider further. **Choose:** Have ServiceStatusView handle both `type: 'healthCheckResults'` AND legacy `event.data.command === 'healthCheckResults'` patterns for robustness. This avoids modifying the existing HealthCommandModule.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npm run check-types && npm run compile && npm run lint</automated>
    <manual>Test that Service Status view renders through React, and that non-migrated views (dashboard, course list) still render through legacy HTML</manual>
  </verify>
  <done>
    - ViewRouter routes 'service-status' state to React HTML
    - App.tsx routes 'serviceStatus' view to ServiceStatusView component
    - ServiceStatusView receives health check results from existing HealthCommandModule
    - All non-migrated views continue using legacy HTML generation
  </done>
</task>

</tasks>

<verification>
1. `npm run check-types` passes with zero errors
2. `npm run compile` produces all bundles
3. `npm run lint` passes with zero errors
4. ViewRouter returns React HTML for 'service-status' state
5. ServiceHealth component from Phase 2 renders correctly within the view
6. Health check results from HealthCommandModule display in the React view
</verification>

<success_criteria>
- ServiceStatus renders through React using Phase 2 ServiceHealth component
- Health check cycle works: view requests checks, extension performs them, results display
- Minimal state persistence (serverUrl only, not stale health results)
- No regression in other views
</success_criteria>

<output>
After completion, create `.planning/phases/03-simple-views-migration/03-02-SUMMARY.md`
</output>
