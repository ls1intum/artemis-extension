---
phase: 02-shared-component-library
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - iris-thaumantias/src/views/webview/react/components/HelpPopup/HelpPopup.tsx
  - iris-thaumantias/src/views/webview/react/components/HelpPopup/HelpPopup.module.css
  - iris-thaumantias/src/views/webview/react/components/HelpPopup/index.ts
  - iris-thaumantias/src/views/webview/react/components/SideMenu/SideMenu.tsx
  - iris-thaumantias/src/views/webview/react/components/SideMenu/SideMenu.module.css
  - iris-thaumantias/src/views/webview/react/components/SideMenu/index.ts
  - iris-thaumantias/src/views/webview/react/components/AskIris/AskIris.tsx
  - iris-thaumantias/src/views/webview/react/components/AskIris/AskIris.module.css
  - iris-thaumantias/src/views/webview/react/components/AskIris/index.ts
  - iris-thaumantias/src/views/webview/react/components/ServiceHealth/ServiceHealth.tsx
  - iris-thaumantias/src/views/webview/react/components/ServiceHealth/ServiceHealth.module.css
  - iris-thaumantias/src/views/webview/react/components/ServiceHealth/index.ts
  - iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.tsx
  - iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.module.css
  - iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx
  - iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.module.css
  - iris-thaumantias/src/views/webview/react/components/exercise/BuildProgress.tsx
  - iris-thaumantias/src/views/webview/react/components/exercise/BuildProgress.module.css
  - iris-thaumantias/src/views/webview/react/components/exercise/index.ts
  - iris-thaumantias/src/views/webview/react/components/index.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "HelpPopup renders a toggleable popup overlay with close-on-outside-click behavior"
    - "SideMenu renders a slide-out navigation panel with menu items"
    - "AskIris renders a button that triggers the Iris chat interaction"
    - "ServiceHealth renders server health status indicators matching existing visual design"
    - "SubmissionStatus, ParticipationActions, and BuildProgress exercise components live in components/exercise/ folder"
    - "Exercise components accept typed props enabling reuse by both ExerciseDetail and ExamExerciseDetail views"
    - "Barrel index.ts at components/ root re-exports all components for clean imports"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.tsx"
      provides: "Submission result display with status colors and feedback"
      exports: ["SubmissionStatus"]
    - path: "iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx"
      provides: "Exercise participation action buttons (start, submit, etc.)"
      exports: ["ParticipationActions"]
    - path: "iris-thaumantias/src/views/webview/react/components/exercise/BuildProgress.tsx"
      provides: "Build/compilation progress indicator with log entries"
      exports: ["BuildProgress"]
    - path: "iris-thaumantias/src/views/webview/react/components/index.ts"
      provides: "Barrel re-export of all components for clean import paths"
      contains: "export.*from"
  key_links:
    - from: "iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.tsx"
      to: "Badge component"
      via: "Imports Badge for status display"
      pattern: "import.*Badge.*from"
    - from: "iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx"
      to: "Button component"
      via: "Imports Button for action buttons"
      pattern: "import.*Button.*from"
    - from: "iris-thaumantias/src/views/webview/react/components/index.ts"
      to: "All component folders"
      via: "Barrel re-exports"
      pattern: "export.*from '\\./"
---

<objective>
Create composite UI components (HelpPopup, SideMenu, AskIris, ServiceHealth) and shared exercise components (SubmissionStatus, ParticipationActions, BuildProgress) in an explicit `exercise/` folder, then create a barrel index.ts for the entire component library.

Purpose: Complete the component library by porting remaining composite components and formalizing exercise component sharing (COMP-03) so ExerciseDetail and ExamExerciseDetail views can compose from the same React components.
Output: 7 component files + barrel index.ts, completing the full shared component library.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-component-library/02-RESEARCH.md
@.planning/phases/02-shared-component-library/02-01-SUMMARY.md
@.planning/phases/02-shared-component-library/02-02-SUMMARY.md

# Existing source components to port:
@iris-thaumantias/src/views/components/helpPopup/helpPopupComponent.ts
@iris-thaumantias/src/views/components/sideMenu/sideMenuComponent.ts
@iris-thaumantias/src/views/components/askIris/askIrisComponent.ts
@iris-thaumantias/src/views/components/askIris/ask-iris.css
@iris-thaumantias/src/views/components/serviceHealth/serviceHealthComponent.ts
@iris-thaumantias/src/views/components/serviceHealth/service-health.css

# Exercise components to extract into shared folder:
@iris-thaumantias/src/views/exerciseDetail/components/submissionStatusComponent.ts
@iris-thaumantias/src/views/exerciseDetail/components/participationActionsComponent.ts
@iris-thaumantias/src/views/exerciseDetail/components/buildProgressComponent.ts
@iris-thaumantias/src/views/exerciseDetail/exercise-detail.css
@iris-thaumantias/src/views/examExerciseDetail/examExerciseDetailView.ts

# Previously created components (available for composition):
@iris-thaumantias/src/views/webview/react/components/Button/index.ts
@iris-thaumantias/src/views/webview/react/components/Badge/index.ts
@iris-thaumantias/src/views/webview/react/components/Container/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HelpPopup, SideMenu, AskIris, and ServiceHealth components</name>
  <files>
    iris-thaumantias/src/views/webview/react/components/HelpPopup/HelpPopup.tsx
    iris-thaumantias/src/views/webview/react/components/HelpPopup/HelpPopup.module.css
    iris-thaumantias/src/views/webview/react/components/HelpPopup/index.ts
    iris-thaumantias/src/views/webview/react/components/SideMenu/SideMenu.tsx
    iris-thaumantias/src/views/webview/react/components/SideMenu/SideMenu.module.css
    iris-thaumantias/src/views/webview/react/components/SideMenu/index.ts
    iris-thaumantias/src/views/webview/react/components/AskIris/AskIris.tsx
    iris-thaumantias/src/views/webview/react/components/AskIris/AskIris.module.css
    iris-thaumantias/src/views/webview/react/components/AskIris/index.ts
    iris-thaumantias/src/views/webview/react/components/ServiceHealth/ServiceHealth.tsx
    iris-thaumantias/src/views/webview/react/components/ServiceHealth/ServiceHealth.module.css
    iris-thaumantias/src/views/webview/react/components/ServiceHealth/index.ts
  </files>
  <action>
    Port 4 composite UI components that build on the atomic components from plans 01 and 02.

    **HelpPopup.tsx** — Port from `helpPopupComponent.ts` (100 lines):
    - Props interface: `children: ReactNode` (popup content), `trigger?: ReactNode` (element that opens popup, defaults to help icon), `isOpen?: boolean`, `onToggle?: () => void`, `position?: 'top' | 'bottom' | 'left' | 'right'`, `className?: string`
    - Manages open/closed state with `useState` (internal default, overridable via isOpen/onToggle props)
    - Click-outside-to-close: use `useEffect` with document click listener + `useRef` for popup container ref. Clean up listener on unmount.
    - Renders a trigger element and an absolutely-positioned popup overlay. Use `IconButton` from Button folder for the default help trigger icon if no custom trigger provided.
    - `HelpPopup.module.css` — Popup overlay styles, positioning, backdrop, z-index

    **SideMenu.tsx** — Port from `sideMenuComponent.ts` (141 lines):
    - Props interface: `isOpen: boolean`, `onClose: () => void`, `children: ReactNode`, `title?: string`, `className?: string`
    - Renders a slide-out panel overlay. Parent controls open state (controlled component pattern).
    - Include backdrop overlay that closes menu on click.
    - Import `IconButton.Close` from Button folder for the close button.
    - `SideMenu.module.css` — Port sliding animation, backdrop, panel layout. Use CSS transitions.

    **AskIris.tsx** — Port from `askIrisComponent.ts` (59 lines) + `ask-iris.css`:
    - Props interface: `onClick?: () => void`, `label?: string`, `className?: string`
    - Simple component: renders an Iris-branded button/link that opens the Iris chat.
    - Use the existing Iris icon (inline SVG or import from IconDefinitions pattern).
    - `AskIris.module.css` — Port from `ask-iris.css`

    **ServiceHealth.tsx** — Port from `serviceHealthComponent.ts` (321 lines) + `service-health.css`:
    - This is the most complex component in this task. Study the source carefully.
    - Props interface: Define based on what serviceHealthComponent accepts. Key props likely include: `services: Array<{ name: string; status: 'ok' | 'warning' | 'error' | 'unknown'; message?: string }>`, `onRefresh?: () => void`, `className?: string`
    - Renders a list of service health indicators with color-coded status badges.
    - Use `Badge` component from Badge folder for status indicators.
    - `ServiceHealth.module.css` — Port from `service-health.css`. Preserve status color variables and layout.

    All components: functional syntax, CSS Modules with clsx, --theme-* variables, index.ts re-exports.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npm run compile && npm run check-types && npm run lint</automated>
    <manual>Verify all 4 component directories exist with .tsx, .module.css, and index.ts</manual>
  </verify>
  <done>
    - HelpPopup toggles popup overlay with click-outside-to-close
    - SideMenu renders controlled slide-out panel with backdrop
    - AskIris renders Iris chat trigger
    - ServiceHealth renders service status list with color-coded indicators
    - All use CSS Modules and --theme-* variables
    - Build and type-check pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create shared exercise components and barrel index</name>
  <files>
    iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.tsx
    iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.module.css
    iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx
    iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.module.css
    iris-thaumantias/src/views/webview/react/components/exercise/BuildProgress.tsx
    iris-thaumantias/src/views/webview/react/components/exercise/BuildProgress.module.css
    iris-thaumantias/src/views/webview/react/components/exercise/index.ts
    iris-thaumantias/src/views/webview/react/components/index.ts
  </files>
  <action>
    Create shared exercise components in `components/exercise/` folder (per user decision: shared exercise components live in explicit shared folder). These components currently live in `exerciseDetail/components/` and are imported by both `exerciseDetailView.ts` and `examExerciseDetailView.ts`. Moving them to a shared React folder formalizes the ~70% code reuse (COMP-03).

    Study the existing source files carefully. The exercise components reference domain models from `src/models/` (ArtemisExercise, ArtemisParticipation, ArtemisSubmission, ArtemisResult, etc.). The React components should accept typed props rather than importing domain models directly — keep components agnostic of extension host data structures. Use interface props that describe what the component needs to render.

    **SubmissionStatus.tsx** — Port from `submissionStatusComponent.ts` (548 lines):
    - This is the largest component. It renders submission results with status indicators, score display, feedback messages, and test case results.
    - Props interface: Define based on what the component renders. Key props:
      - `score?: number`, `maxScore?: number` — Score display
      - `status: 'pending' | 'building' | 'success' | 'partial' | 'failed' | 'error' | 'no-submission'` — Overall status
      - `feedbacks?: Array<{ type: 'positive' | 'negative' | 'neutral'; text: string }>` — Feedback items
      - `testCases?: Array<{ name: string; passed: boolean; message?: string }>` — Test results
      - `buildLogs?: string` — Raw build output
      - `className?: string`
    - Use `Badge` for status indicators, `Container` for result sections.
    - Action callbacks via props: `onShowDetails?: () => void`
    - `SubmissionStatus.module.css` — Port relevant styles from `exercise-detail.css` for submission sections

    **ParticipationActions.tsx** — Port from `participationActionsComponent.ts` (298 lines):
    - Renders action buttons for exercise participation (start, submit, sync, clone repository, etc.)
    - Props interface:
      - `exerciseType: 'programming' | 'quiz' | 'modeling' | 'text' | 'file-upload'`
      - `participationStatus: 'not-started' | 'in-progress' | 'submitted' | 'graded'`
      - `hasRepository?: boolean`
      - `canSubmit?: boolean`
      - Action callbacks: `onStart?: () => void`, `onSubmit?: () => void`, `onSync?: () => void`, `onClone?: () => void`, `onOpenRepository?: () => void`
      - `className?: string`
    - Use `Button` component for all action buttons. Conditionally render buttons based on participation status and exercise type.
    - `ParticipationActions.module.css` — Port relevant styles for action button layout

    **BuildProgress.tsx** — Port from `buildProgressComponent.ts` (272 lines):
    - Renders build/compilation progress with log entries and status
    - Props interface:
      - `status: 'idle' | 'building' | 'success' | 'failed'`
      - `progress?: number` (0-100)
      - `logEntries?: Array<{ level: 'info' | 'warning' | 'error'; message: string; timestamp?: string }>`
      - `className?: string`
    - Renders a progress bar, status indicator, and scrollable log output
    - `BuildProgress.module.css` — Port build progress styles

    **exercise/index.ts** — Re-export all exercise components:
    ```typescript
    export { SubmissionStatus } from './SubmissionStatus';
    export { ParticipationActions } from './ParticipationActions';
    export { BuildProgress } from './BuildProgress';
    ```

    **components/index.ts** — Create barrel file re-exporting ALL components in the library:
    ```typescript
    // Atomic components
    export { Button, IconButton } from './Button';
    export { Badge } from './Badge';
    export { BackLink } from './BackLink';

    // Form components
    export { TextInput } from './TextInput';
    export { Dropdown } from './Dropdown';

    // Layout components
    export { Container } from './Container';
    export { ListItem } from './ListItem';
    export { List } from './List';

    // Composite components
    export { HelpPopup } from './HelpPopup';
    export { SideMenu } from './SideMenu';
    export { AskIris } from './AskIris';
    export { ServiceHealth } from './ServiceHealth';

    // Shared exercise components
    export { SubmissionStatus, ParticipationActions, BuildProgress } from './exercise';
    ```

    This barrel enables clean imports in views: `import { Button, Container, SubmissionStatus } from '../components';`
  </action>
  <verify>
    <automated>cd iris-thaumantias && npm run compile && npm run check-types && npm run lint</automated>
    <manual>Verify exercise/ folder exists with 3 components and barrel index.ts re-exports all components</manual>
  </verify>
  <done>
    - SubmissionStatus, ParticipationActions, BuildProgress exist in components/exercise/ folder
    - Exercise components accept typed props (not domain model imports) enabling reuse by both ExerciseDetail and ExamExerciseDetail views
    - exercise/index.ts re-exports all 3 exercise components
    - components/index.ts barrel re-exports all 15+ components in the library
    - Build and type-check pass
  </done>
</task>

</tasks>

<verification>
1. `cd iris-thaumantias && npm run compile` — all bundles produced
2. `cd iris-thaumantias && npm run check-types` — zero type errors
3. `cd iris-thaumantias && npm run lint` — zero lint errors
4. `ls iris-thaumantias/src/views/webview/react/components/` — All component directories present: Button, Badge, BackLink, TextInput, Dropdown, Container, ListItem, List, HelpPopup, SideMenu, AskIris, ServiceHealth, exercise
5. `ls iris-thaumantias/src/views/webview/react/components/exercise/` — SubmissionStatus, ParticipationActions, BuildProgress files exist
6. components/index.ts re-exports all components (grep for export count)
</verification>

<success_criteria>
- All 4 composite components ported to React with CSS Modules
- 3 shared exercise components in explicit exercise/ folder (COMP-03)
- Barrel index.ts re-exports entire component library (15+ components)
- Exercise component props are typed for reuse by both ExerciseDetail and ExamExerciseDetail
- All components use --theme-* CSS variables for theme compliance
- Build, type-check, and lint all pass with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-component-library/02-03-SUMMARY.md`
</output>
