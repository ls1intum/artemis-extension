---
phase: 02-shared-component-library
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.tsx
  - iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.module.css
  - iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx
  - iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.module.css
  - iris-thaumantias/src/views/webview/react/components/exercise/BuildProgress.tsx
  - iris-thaumantias/src/views/webview/react/components/exercise/BuildProgress.module.css
  - iris-thaumantias/src/views/webview/react/components/exercise/index.ts
  - iris-thaumantias/src/views/webview/react/components/index.ts
autonomous: true
requirements:
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "SubmissionStatus, ParticipationActions, and BuildProgress exercise components live in components/exercise/ folder"
    - "Exercise components accept typed props enabling reuse by both ExerciseDetail and ExamExerciseDetail views"
    - "All exercise components adapt to both light and dark VS Code themes via --theme-* / --vscode-* CSS variables"
    - "Barrel index.ts at components/ root re-exports all components for clean imports"
  artifacts:
    - path: "iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.tsx"
      provides: "Submission result display with status colors and feedback"
      exports: ["SubmissionStatus"]
    - path: "iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx"
      provides: "Exercise participation action buttons (start, submit, etc.)"
      exports: ["ParticipationActions"]
    - path: "iris-thaumantias/src/views/webview/react/components/exercise/BuildProgress.tsx"
      provides: "Build/compilation progress indicator with log entries"
      exports: ["BuildProgress"]
    - path: "iris-thaumantias/src/views/webview/react/components/exercise/index.ts"
      provides: "Barrel re-export of exercise components"
      contains: "export.*from"
    - path: "iris-thaumantias/src/views/webview/react/components/index.ts"
      provides: "Barrel re-export of all components for clean import paths"
      contains: "export.*from"
  key_links:
    - from: "iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.tsx"
      to: "Badge component"
      via: "Imports Badge for status display"
      pattern: "import.*Badge.*from"
    - from: "iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx"
      to: "Button component"
      via: "Imports Button for action buttons"
      pattern: "import.*Button.*from"
    - from: "iris-thaumantias/src/views/webview/react/components/index.ts"
      to: "All component folders"
      via: "Barrel re-exports"
      pattern: "export.*from '\\./"
---

<objective>
Create shared exercise components (SubmissionStatus, ParticipationActions, BuildProgress) in an explicit `exercise/` folder, then create barrel index files for the exercise subfolder and the entire component library.

Purpose: Formalize exercise component sharing (COMP-03) so ExerciseDetail and ExamExerciseDetail views compose from the same React components, eliminating the ~70% code duplication. The barrel index provides clean imports for all downstream views.
Output: 3 exercise component folders with .tsx + .module.css, exercise/index.ts, and components/index.ts barrel.
</objective>

<execution_context>
@/Users/liamberger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liamberger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-shared-component-library/02-RESEARCH.md
@.planning/phases/02-shared-component-library/02-01-SUMMARY.md
@.planning/phases/02-shared-component-library/02-02-SUMMARY.md
@.planning/phases/02-shared-component-library/02-03-SUMMARY.md

# Exercise components to extract into shared folder:
@iris-thaumantias/src/views/exerciseDetail/components/submissionStatusComponent.ts
@iris-thaumantias/src/views/exerciseDetail/components/participationActionsComponent.ts
@iris-thaumantias/src/views/exerciseDetail/components/buildProgressComponent.ts
@iris-thaumantias/src/views/exerciseDetail/exercise-detail.css
@iris-thaumantias/src/views/examExerciseDetail/examExerciseDetailView.ts

# Previously created components (available for composition):
@iris-thaumantias/src/views/webview/react/components/Button/index.ts
@iris-thaumantias/src/views/webview/react/components/Badge/index.ts
@iris-thaumantias/src/views/webview/react/components/Container/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SubmissionStatus exercise component</name>
  <files>
    iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.tsx
    iris-thaumantias/src/views/webview/react/components/exercise/SubmissionStatus.module.css
  </files>
  <action>
    Create the SubmissionStatus component in `components/exercise/` folder (per user decision: shared exercise components live in explicit shared folder). This component currently lives in `exerciseDetail/components/` and is imported by both `exerciseDetailView.ts` and `examExerciseDetailView.ts`. Moving it to a shared React folder formalizes the ~70% code reuse (COMP-03).

    Study the existing source file carefully. The exercise component references domain models from `src/models/` (ArtemisSubmission, ArtemisResult, etc.). The React component should accept typed props rather than importing domain models directly — keep the component agnostic of extension host data structures. Use interface props that describe what the component needs to render.

    **SubmissionStatus.tsx** — Port from `submissionStatusComponent.ts` (548 lines):
    - Import CSS Modules: `import styles from './SubmissionStatus.module.css';`
    - This is the largest component. It renders submission results with status indicators, score display, feedback messages, and test case results.
    - Props interface: Define based on what the component renders. Key props:
      - `score?: number`, `maxScore?: number` — Score display
      - `status: 'pending' | 'building' | 'success' | 'partial' | 'failed' | 'error' | 'no-submission'` — Overall status
      - `feedbacks?: Array<{ type: 'positive' | 'negative' | 'neutral'; text: string }>` — Feedback items
      - `testCases?: Array<{ name: string; passed: boolean; message?: string }>` — Test results
      - `buildLogs?: string` — Raw build output
      - `className?: string`
    - Use `Badge` for status indicators, `Container` for result sections.
    - Action callbacks via props: `onShowDetails?: () => void`
    - All colors via --theme-* CSS variables for VS Code theme compliance (COMP-02).
    - `SubmissionStatus.module.css` — Port relevant styles from `exercise-detail.css` for submission sections. Use --theme-* variables for all colors.
  </action>
  <verify>
    <automated>cd iris-thaumantias && npm run compile && npm run check-types && npm run lint</automated>
    <manual>Verify SubmissionStatus.tsx and SubmissionStatus.module.css exist in components/exercise/</manual>
  </verify>
  <done>
    - SubmissionStatus exists in components/exercise/ folder
    - Component accepts typed props (not domain model imports) enabling reuse by both ExerciseDetail and ExamExerciseDetail views
    - Uses Badge and Container from the component library
    - Uses --theme-* CSS variables for theme compliance
    - Build and type-check pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ParticipationActions, BuildProgress, and barrel indexes</name>
  <files>
    iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.tsx
    iris-thaumantias/src/views/webview/react/components/exercise/ParticipationActions.module.css
    iris-thaumantias/src/views/webview/react/components/exercise/BuildProgress.tsx
    iris-thaumantias/src/views/webview/react/components/exercise/BuildProgress.module.css
    iris-thaumantias/src/views/webview/react/components/exercise/index.ts
    iris-thaumantias/src/views/webview/react/components/index.ts
  </files>
  <action>
    Create the remaining exercise components and barrel index files.

    **ParticipationActions.tsx** — Port from `participationActionsComponent.ts` (298 lines):
    - Import CSS Modules: `import styles from './ParticipationActions.module.css';`
    - Renders action buttons for exercise participation (start, submit, sync, clone repository, etc.)
    - Props interface:
      - `exerciseType: 'programming' | 'quiz' | 'modeling' | 'text' | 'file-upload'`
      - `participationStatus: 'not-started' | 'in-progress' | 'submitted' | 'graded'`
      - `hasRepository?: boolean`
      - `canSubmit?: boolean`
      - Action callbacks: `onStart?: () => void`, `onSubmit?: () => void`, `onSync?: () => void`, `onClone?: () => void`, `onOpenRepository?: () => void`
      - `className?: string`
    - Use `Button` component for all action buttons. Conditionally render buttons based on participation status and exercise type.
    - All colors via --theme-* CSS variables for VS Code theme compliance (COMP-02).
    - `ParticipationActions.module.css` — Port relevant styles for action button layout. Use --theme-* variables.

    **BuildProgress.tsx** — Port from `buildProgressComponent.ts` (272 lines):
    - Import CSS Modules: `import styles from './BuildProgress.module.css';`
    - Renders build/compilation progress with log entries and status
    - Props interface:
      - `status: 'idle' | 'building' | 'success' | 'failed'`
      - `progress?: number` (0-100)
      - `logEntries?: Array<{ level: 'info' | 'warning' | 'error'; message: string; timestamp?: string }>`
      - `className?: string`
    - Renders a progress bar, status indicator, and scrollable log output
    - All colors via --theme-* CSS variables for VS Code theme compliance (COMP-02).
    - `BuildProgress.module.css` — Port build progress styles. Use --theme-* variables.

    **exercise/index.ts** — Re-export all exercise components:
    ```typescript
    export { SubmissionStatus } from './SubmissionStatus';
    export { ParticipationActions } from './ParticipationActions';
    export { BuildProgress } from './BuildProgress';
    ```

    **components/index.ts** — Create barrel file re-exporting ALL components in the library:
    ```typescript
    // Atomic components
    export { Button, IconButton } from './Button';
    export { Badge } from './Badge';
    export { BackLink } from './BackLink';

    // Form components
    export { TextInput } from './TextInput';
    export { Dropdown } from './Dropdown';

    // Layout components
    export { Container } from './Container';
    export { ListItem } from './ListItem';
    export { List } from './List';

    // Composite components
    export { HelpPopup } from './HelpPopup';
    export { SideMenu } from './SideMenu';
    export { AskIris } from './AskIris';
    export { ServiceHealth } from './ServiceHealth';

    // Shared exercise components
    export { SubmissionStatus, ParticipationActions, BuildProgress } from './exercise';
    ```

    This barrel enables clean imports in views: `import { Button, Container, SubmissionStatus } from '../components';`
  </action>
  <verify>
    <automated>cd iris-thaumantias && npm run compile && npm run check-types && npm run lint</automated>
    <manual>Verify exercise/ folder has all 3 components and barrel index.ts re-exports all components</manual>
  </verify>
  <done>
    - ParticipationActions and BuildProgress exist in components/exercise/ folder
    - Exercise components accept typed props (not domain model imports) enabling reuse by both ExerciseDetail and ExamExerciseDetail views
    - exercise/index.ts re-exports all 3 exercise components
    - components/index.ts barrel re-exports all 15+ components in the library
    - All exercise components use --theme-* CSS variables for theme compliance
    - Build and type-check pass
  </done>
</task>

</tasks>

<verification>
1. `cd iris-thaumantias && npm run compile` — all bundles produced
2. `cd iris-thaumantias && npm run check-types` — zero type errors
3. `cd iris-thaumantias && npm run lint` — zero lint errors
4. `ls iris-thaumantias/src/views/webview/react/components/exercise/` — SubmissionStatus, ParticipationActions, BuildProgress files exist
5. components/index.ts re-exports all components (grep for export count)
</verification>

<success_criteria>
- 3 shared exercise components in explicit exercise/ folder (COMP-03)
- Barrel index.ts re-exports entire component library (15+ components)
- Exercise component props are typed for reuse by both ExerciseDetail and ExamExerciseDetail
- All exercise components use --theme-* CSS variables for VS Code theme compliance (COMP-02)
- Build, type-check, and lint all pass with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-component-library/02-04-SUMMARY.md`
</output>
