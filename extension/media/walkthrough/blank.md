<!-- Intentionally renders nothing. The step schema requires `media` on every step, and
every available asset is a small square logo that the media panel upscales to its full
width, so the tour reads better with the right-hand panel left empty. -->
