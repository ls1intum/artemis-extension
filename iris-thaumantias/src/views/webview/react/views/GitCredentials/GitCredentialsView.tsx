/**
 * GitCredentials view component - placeholder for Task 2.
 * This will be fully implemented in Task 2.
 */

import type { GitCredentialsViewProps } from './types';

export function GitCredentialsView({ vscodeApi }: GitCredentialsViewProps) {
    return (
        <div style={{ padding: '20px', color: 'var(--vscode-foreground)' }}>
            GitCredentials View (placeholder - will be implemented in Task 2)
        </div>
    );
}
