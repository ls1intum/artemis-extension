export { BackLinkComponent } from '../backLinkComponent';
export { ServiceHealthComponent } from '../serviceHealthComponent';

// Legacy components (badge, button, card, etc.) were removed when the custom
// theming system was dropped. This stub file keeps existing import paths
// (`views/components/common`) working for the remaining shared components.
